# Python
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/
.hypothesis/
.coverage
htmlcov/
dist/
build/
*.egg-info/

# Node / frontend
node_modules/
web/dist/
web/coverage/

# Env / secrets
.env
.env.local

# Claude Code — per-machine local config (e.g. autoMemoryDirectory abs path)
.claude/settings.local.json

# Claude Code — agent worktrees (created for isolated/parallel agent work)
.claude/worktrees/

# Claude Code — local gate receipts (review PASS bound to a content hash)
.claude/receipts/

# Local discussion scratch (not shipped)
.discuss/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Playwright artifacts when invoked from the repo root (web/.gitignore only covers web/)
test-results/
playwright-report/
blob-report/

# Managed-corpus uploads written at runtime (`data/uploads/{project}/`). Nothing
# here is tracked, but it lands INSIDE the content-addressed receipt tree, so an
# unguarded `git add -A` would commit a user's uploaded documents — the S8
# receipt-hygiene rule, and a reviewer nit on two consecutive tasks (MCP18, QA1).
data/

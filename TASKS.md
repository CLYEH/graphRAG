# TASKS — graphRAG build queue

The loop consumes the **top unchecked** item (respecting dependencies). One task per
iteration; definition of done = `uv run poe check-all` green, then commit. Protocol:
[`docs/LOOP.md`](docs/LOOP.md). Design: [`docs/DESIGN.md`](docs/DESIGN.md).

Check an item off `[x]` inside its own task PR — the checkoff lands when that PR merges.
Keep items small enough to finish in one loop.

---

## Harness
- [x] Repo skeleton (`core/ api/ cli/ web/`), `pyproject.toml`, uv env
- [x] Quality gates: ruff + mypy(strict) + pytest + poe; `poe check-all` (backend + frontend)
- [x] Frontend scaffold (React/Vite/TS) + oxlint/prettier/vitest
- [x] `docker-compose.yml` (postgres/neo4j/qdrant/redis)
- [x] Test tiers: unit/contract/integration/eval/e2e markers, `test-cov` (85%), conftest service-gating, Playwright scaffold
- [x] CI (`.github/workflows/ci.yml`: backend + coverage + integration + frontend), CLAUDE.md/AGENTS.md, `.env.example`
- [x] H1 harness fixes: fail-loud CI integration gate (`--wait` + CI fail-not-skip), doc-drift cleanup (hook filename, push wording, P-numbering, checkoff rule), gate-wait pipelining in LOOP.md, reviewer model → opus, LLM default → `gpt-5.4-nano`, C1/C6 split, `gh`/`git` allowlist, CI dedupe/concurrency + qdrant pin, DR-008 (Alembic) recorded
- [x] H2 LOOP.md: Codex suggestion triage rules in step 7 (must-fix vs reply-and-resolve criteria, checkable rationale required for every resolve-without-change, same-class sweep per round; `+1` gate unchanged) + entry points aligned (CLAUDE.md gate 4, /loop prompt, memory)
- [x] H3 harness enforcement & efficiency: `scripts/watch-codex.sh` (standard 3-channel watcher) · doc-only fast lane (`docs/**` direct push, no PR/Codex — owner-approved; doc-reviewer subagent + push-gate hook + branch-protection relax) · CPU push gates (review receipts hash-bound + re-run checks) · CI `governance` job (DR-002 version-bump guard, TASKS.md checkoff lint)
- [x] H4 property-based boundary tests: add `hypothesis` (dev dep) + property tests for frozen numeric/boundary rules (`is_eval_regression` first; P5 guardrail limits and C10 scoring as they land) — retro of PR #12's float-boundary must-fix (lesson class 8: 邊界語意 × 表示誤差)
- [x] H5 per-branch review receipts: key `.claude/receipts/review` by branch (or one receipt file per tree hash) so parallel `task/*` + `docs/*` work stops overwriting each other's PASS stamps (push gate fail-closes correctly today but forces a reviewer re-stamp round-trip every time) — touch `write-review-receipt.sh` + `require-push-gates.sh` + `tests/test_receipts.py` — retro of PR #15 (receipt slot collision during gate-wait pipelining)
- [x] H8 watcher poke→watch race fix + quota probe loop: `watch-codex.sh` bootstrap scans BACKWARD at startup (supersession rule: the latest quota message governs unless a newer +1/review/poke exists) so a limits reply landing seconds after a poke — before the watcher starts — no longer polls blind to timeout; `probe-codex.sh` pokes/classifies/sleeps in rounds for quota-outage recovery — owner-reported miss during PR #28
- [x] H7 quota-aware Codex watcher: `watch-codex.sh` exit `30` when the only fresh bot response is the "reached your Codex usage limits" comment (checked before the exit-10 triage verdict) — stop waiting on an exhausted quota and re-poke after the reset time; LOOP.md step-6 exit-code doc updated — owner-directed during PR #25's quota outage
- [x] H6 migration `0005`: `pipeline_step_items.item_ref <> ''` CHECK (+ `item_kind <> ''`) — the identifier-non-empty rule applied to the P6 table it predates; an empty item_ref is a no-op identity in the §27.7 retry dedup — retro of PR #17 (deferred there as out-of-scope for an already-merged table)
- [x] H9 loop-throughput fixes (3 bottlenecks; all `*.md` → doc fast lane): the dominant cost is the external Codex round-trip, so (1) **cut rounds** — fold PR #47's config/parser completeness-matrix (input-position × level) + lenient-only-at-contract-boundary lessons into `code-reviewer.md` §7, and frame LOOP step 5 as the pre-push adversarial pass that runs the matching §6/§7 matrix to COMPLETION (round spent locally, not on Codex); (2) **batch re-stamp** — LOOP step 7 makes explicit the re-review/re-stamp is once per Codex round (after the same-class sweep), not per finding; (3) **overlap gate-wait** — already codified (LOOP step 7 "don't idle while gates run"), reinforced. Retro of PR #47's 2-round Codex (both findings one class a position apart — one matrix pass covers both)
- [x] H13 `watch-codex.sh --anchor <ts>` (2026-07-13, #73 retro; id H12 is retired — it names the pre-push-hook idea dropped with H10): the watcher treats the last `@codex review` poke as the triage-ack, so an event that was triaged WITHOUT a poke (fixed + thread resolved — the normal flow now that pushes auto-trigger re-review) makes every subsequent watch exit 10 at bootstrap on the same already-handled event, forever. Accept an explicit anchor timestamp (or read an ack file the triage step writes) so "newest unprocessed" means newer than the last HANDLED event, not the last poke; today's session hand-rolled the anchored poll inline twice
- [x] H11 integration tests must survive a LIVED-IN dev environment (2026-07-13 incident, owner-directed follow-up): the suite assumes pristine services and breaks on real dev-machine state — (a) whole-table count assertions (`test_two_builds_stay_isolated`, `test_auto_merge_cascades_and_converges` assert `count(entities) == N` unfiltered) go red the moment ANY non-test data exists (a Console QA project did it) → scope every count to the test's own project/builds; (b) arq worker tests share dev Redis db 0 — a stale `arq:in-progress:cron:*` key from an interrupted worker WEDGES the whole suite indefinitely (class 12's cross-generation key retention, now biting tests) → flush the `arq:*` namespace in a session fixture or point tests at a dedicated Redis db index; (c) Qdrant accumulates one orphan collection per eval/lifecycle test run (~300 found) → teardown must delete, plus a one-off sweep of `project_evalrun-*`/`project_lifec-*`; (d) the projects table itself holds HUNDREDS of committed `itest-`/`lifec-`/`sqltest-`/… rows — the worker/lease/eval suites commit real rows and rely on teardown, so any interrupted run leaks (today: 2 orphan jobs from killed runs made the reaper count 2 where a test asserted 1) → prefer per-test cleanup fixtures that run even on kill (or unique-scoped assertions), plus a one-off sweep. CI never sees any of this (fresh services per run) — only local `check-full` does, which is exactly where it hurts the loop

> **Per-task rule:** one task = one `task/<id>` branch = one PR. It lands with tests for its
> tier, passes local gates + the `code-reviewer` subagent, then merges only after CI **and**
> the bound Codex review are green (see [`docs/LOOP.md`](docs/LOOP.md)).

## Track 0 — Contracts & Governance  *(freeze BEFORE parallel work — DR-002)*
- [x] P0 `contracts/openapi.yaml`: response envelope, error-code enum, cursor pagination, SSE event, idempotency (§15/§27.2)
- [x] P1 `contracts/mcp_response.schema.json`: unified retrieval result + source_refs + debug (§16/§27.2)
- [x] P2 Build/activation model spec + Postgres migrations for `builds` + partial unique index (§14/§27.1) · Alembic setup (DR-008)
- [x] P3 Review state machine + `review_ledger` + fingerprint spec + `fingerprint_version` (§17/§27.3)
- [x] P4 Eval contract: `golden.yaml` schema + metrics incl. path_validity/relation_hit_rate/groundedness (§20/§27.5)
- [x] P5 Query safety policy schema (`query_policy`) + SQL(sqlglot)/Cypher strategy (§21/§27.6)
- [x] P6 Observability schema: pipeline_runs/steps/items + item_ref rules (§18/§27.7)

## Track 1 — Core engine  *(depends on Track 0)*
- [x] C1a PG migrations for core tables (documents/chunks/entities/relations/evidence/reports/merge_candidates; `builds` landed with P2, `review_ledger` with P3, observability with P6)
- [x] C1b **BuildScopedRepo** over Postgres (active-build lookup + build_id injection, DR-006)
- [x] C1c Neo4j adapter + projection repo (build_id-filtered, DR-004)
- [x] C1d Qdrant adapter + projection repo (build_id payload filter)
- [x] C2 Ingest (structured + document connectors) + clean/chunking
- [x] C3a Graph build — structured rule-mapping extraction → entities/mentions/relations/evidence (deterministic, no LLM)
- [x] C3b Graph build — LLM document extraction (schema-guided, §27.4 quote-span evidence) + LLM factory (§3: LlamaIndex 抽象)
- [x] C3c Ontology proposal pool (LLM-proposed new types + `ontology.proposal_policy`)
- [x] C4 Entity resolution + apply `review_ledger`
- [x] C5 Index: embeddings → Qdrant; project entities/relations → Neo4j
- [x] C6a Retrieval: semantic (Qdrant kNN, §16 contract)
- [x] C6b Retrieval: sql (NLSQL + sqlglot guardrail per P5, §27.6)
- [x] C6c Retrieval: graph (parameterized Cypher templates + guardrail per P5, §27.6)
- [x] C6d Retrieval: global (community_reports — needs C7)
- [x] C6e Hybrid router + fusion + routing trace (§8, §16 debug)
- [x] C7 Global summary (Leiden communities + reports)
- [x] C8 MCP server (per project) exposing the tool set
- [x] C8b MCP HTTP transport — expose the per-project FastMCP server over streamable HTTP (owner 2026-07-10: the no-code agent platform consumes external MCP over HTTP — the museum-guide integration seam; see memory graphrag-goal-museum-guide): entrypoint/CLI transport option (stdio stays the default) + host/port via core.config (never os.environ) + tests; tools/policy untouched (server.py:24 promised transport-additivity — verify it holds); flag the §23 auth placeholder implication (an HTTP-exposed MCP will need real auth → future DR-002-adjacent owner decision, NOT this task)
- [x] C9 builds/activate/rollback/diff/prune (CLI + core)
- [x] C10 Eval harness runner
- [x] C11 Observability wiring + drift detection

## Track 2 — Console backend (FastAPI + arq)  *(needs Track 0 P0; C-items as they land)*
- [x] BA0 API skeleton + generated OpenAPI matching contract + auth placeholder
- [x] BA1a projects/sources registry — schema + core CRUD
- [x] BA1b projects/sources endpoints — routers + idempotency + opaque cursor
- [x] BA2a jobs table + core job repo + delete-project active-jobs guard
- [x] BA2b builds→projects FK RESTRICT + fixture sweep (close the delete TOCTOU structurally)
- [x] BA2c-1 registry-aware build creation + pipeline orchestrator control flow (six §5 stages injected as a seam; step recording, §22 abort, cooperative cancel, resume; fake stages, hermetic Postgres-only tests)
- [x] BA2c-2a build-config loader — `projects.config` JSONB → typed `TextOntology`/`StructuredMapping`/`ResolutionConfig`/chunk params (reuse dataclass validation, no frozen contract; lenient top-level, strict leaves; unit-tested)
- [x] BA2c-2b sources→connector resolution + `default_stages` + the six stage adapters (shared-conn writer/projectors); component (shared-conn spy) + integration (real stores, fake LLM/embedder) tests
- [x] BA2c-2c two-lane real-LLM test — hermetic + real `chat_model()`/`embedding_model()` over a tiny corpus, key-gated skip-only (no CI secret)
- [x] BA2d-1 **execution lease** (Codex BA2c-1 P2, DB heartbeat-lease): `jobs.lease_owner`/`lease_expires_at` + acquire/renew/release primitives (atomic conditional UPDATE, DB-clock expiry — the guard lives in the write) + `run_build_leased` wrapper (heartbeat renews while run_build runs, release on exit incl. failure, a crashed holder's expired lease is reclaimed by the next dispatch). Closes the gap that BA2c-1's FOR UPDATE lock serializes build *creation* but not concurrent *execution* of the same building build; the lease is a liveness layer over the convergent-idempotency safety floor. Component (fast-lane) + concurrency/reclaim integration tests.
- [x] BA2d-2 arq worker + Redis wiring — `arq` dep + `WorkerSettings` (Redis pool from `settings.redis_url`, on_startup/shutdown dep bundle à la MCP lifespan) + a `@func` task that runs `run_build_leased` + enqueue helper (`_job_id=job_id` for arq's own dispatch dedup). Real-worker integration test. (BA2e adds the HTTP trigger that create_job + enqueues.)
- [x] BA2d-3 **lease reaper** — decouple crash recovery from arq's `job_timeout` (Codex BA2d-2 P1): with a generous job_timeout (so arq never cancels a live build → no stranded SoR row), a periodic arq cron (`reap_stuck_builds`, 2×/min, `unique=True`) sweeps jobs whose heartbeat-lease has expired while non-terminal (crashed/starved worker) and re-enqueues them under a deterministic per-stale-lease arq id (`reap:<job>:<expiry>` — re-ticks over the same stale lease dedup instead of piling duplicates; the job's own id would collide with the crashed dispatch's 24h in-progress key) so the fresh dispatch re-acquires the now-free lease and resumes — fast crash recovery (~1 min) independent of job_timeout. Completes the BA2d-1 DB-lease design as the SOLE build-liveness authority.
- [x] BA2e-1 ingest/build triggers + job endpoints (GET /jobs/{id} + cancel) — `create_job_exclusive` (single active job per project → 409 JOB_CONFLICT, serialized on the projects row FOR UPDATE like delete_project) + `enqueue_build` in-band BEFORE the request commit (a crash leaves nothing or a harmless orphan dispatch, never a committed-but-unenqueued job); the class-12 residuals (Redis loses an acked enqueue / a dispatch races the commit and no-ops) are recovered by the reaper's queued-sweep (`find_unenqueued_jobs`: never-leased `queued` older than `job_enqueue_grace_seconds` → replay `enqueue_build` under the job's own arq id, dedup-safe for mere backlog); `IngestRequest.source_ids` / `BuildRequest.reason` loudly rejected (400) until the pipeline can honor them (owner decision 2026-07-10)
- [x] BA2e-2 SSE job events — `GET /jobs/{id}/events` (`text/event-stream`; frozen JobEvent: `job.update|job.done|job.failed`, full shape always present — step/message null not absent; `cancelled` → `job.failed` with exact status in the payload, the frozen event set has no job.cancelled); polls the jobs SoR via short-lived per-poll connections (`get_job_at`: row + DB clock in ONE statement — single-clock ts), NEVER arq (worker `keep_result=0`) and NEVER the request's `db_conn` (a yield-dep stays open until the RESPONSE completes → idle-in-transaction for the stream's life; the 404 precheck observes through the same poll seam); emits initial state + change-only updates, ends after the terminal event; a row vanishing mid-stream (terminal job → project CASCADE) ends the stream without fabricating a terminal frame; poll cadence 🔧 `sse_poll_interval_seconds`
- [x] BA3a inspection: active-binding seam + documents/chunks — list+get × documents/chunks over the ACTIVE build via the DR-006 repo (`resolve_active_binding` per request: project 404 first, else 409 NO_ACTIVE_BUILD; `meta.build_id` stamped from the binding — the API's first active-build consumer); `BuildScopedRepo.fetch_page` (ordered+capped read, ORDER BY expressions pass the same raw-SQL rejection as predicates); keyset cursors: documents (id desc — no created_at on the table; recency lands with Sort later), chunks (document_id asc, ordinal asc); `Document.raw` on detail GET only (contract-licensed conditional key); missing-resource 404 = framework 404 + coarse code (GAP: frozen enum has no inspect not-found code — owner/DR-002 follow-up, registry_errors precedent). **"reports" dropped from BA3 (owner decision 2026-07-10): the frozen contract has no community-reports inspection endpoint/schema — reports stay reachable via query/global; an inspect endpoint later = DR-002 addition**
- [x] BA3b inspection: entities/relations — list+get × entities/relations over the same binding seam; `Relation.evidence[]` on detail only (getRelation "with evidence"; deterministic id order, no silent cap — §27.4 dedup bounds the set; dangling chunk_id preserved by design); keysets = (id desc) via the shared `decode_id_cursor` (created_at is nullable on both tables — the #40 NULLS trap avoided, recency lands with Sort later); per-field nullability audit applied at design time (#55 rule): `created_by`/`relation_signature` omit-when-null (optional non-nullable over nullable columns), `attributes`→{}, internal `embedding_point_id` never emitted
- [x] BA3c inspection: graph subgraph — `GET /projects/{p}/graph/subgraph` over the id-seeded `core.query.graph.subgraph_context` (additive: `_neighbor_entities` gains a `seeds` param; traversal/SoR-reachability/caps/deadline all shared with the hardened C6c path; edges keep the §16 citability bar — active SoR relation + ≥1 valid evidence; every emitted value read from the SoR, projection = topology only). **query_policy source = registry config, strict (owner decision 2026-07-10)**: `projects.config["query_policy"]` validated by the SAME frozen schema (`query_policy_from_mapping`, additive refactor of the MCP file loader); absent/invalid → loud 400, no invented §21 defaults; hops beyond `max_graph_hops` rejected not clamped; client `limit` narrows (never widens) the §21 row ceiling. Neo4j seam = lazy driver in api/deps (zero-I/O at resolution, sessions per request); graph-store outage → 503 STORE_UNAVAILABLE, never a silent empty subgraph
- [x] BA4 cleaning preview/rules — the owner-approved DR-002 round (2026-07-13, DESIGN §26 **DR-009**): contract v1.0→**v1.1** adds `POST /projects/{project}/clean/preview` — the §10.2 「含抽樣預覽」 piece that could not exist without a contract change; "rules" (writing chunking params) needs NO new endpoint, it is the existing `PATCH /projects/{project}`. Exactly ONE source per request (owner-frozen shape): `document_id` reads the raw text through the DR-006 build-scoped repo from the ACTIVE build (409 without one; a document parked in a non-active build is invisible — 404 with the same status-not-code GAP as the inspect reads), while `text` chunks the given string directly, which is what makes preview usable BEFORE any build exists. Omitted `max_chars`/`overlap` fall back to the project's configured chunking then engine defaults — the same chain builds walk, so the no-parameter preview shows what a build would ACTUALLY do; the pair relation (0 ≤ overlap < max_chars) is runtime-enforced by `chunk_text` itself (two-field numeric comparison is genuinely inexpressible in JSON Schema — the contract documents this rather than pretending). Read-only RPC over POST: NO Idempotency-Key (nothing to replay; a key would falsely signal write semantics — the contract test's read-RPC category now names `/query/` and `/clean/preview` explicitly). Pure function, never persists: pinned by a recording repo that fails on any non-`fetch_all` call, and end-to-end by a chunks-table row-count-unchanged assertion against real Postgres. Unknown body keys are rejected (`extra="forbid"` mirroring the schema's `additionalProperties: false` — a misspelled `max_char` silently previewing default chunking would be trusted and copied into config)
- [x] BA5 merge-candidate review endpoints — list (ACTIVE-build scoped, BA3 binding/cursor pattern) + approve/reject/defer via `core.resolve.decisions.decide_merge_candidate`: ONE transaction locks the candidate FOR UPDATE, validates the frozen §17 transition under the lock (pending→approved|rejected|deferred, deferred→approved|rejected, terminal states refuse — 400 with machine-readable details, GAP: no frozen review-conflict code), writes the DR-003 carry-forward ledger entry keyed by the SAME `fingerprints.merge_key` resolve reads (defer included — #28 R4a: a deferred pair must block auto-merge), and stamps the audit trail (ledger+candidate share ONE per-decision clock_timestamp — txn-stable now() would tie batched re-decisions and break §27.3 latest-wins, Codex #59 R2); `decided_by="console"` (§23 placeholder, never "auto" — §27.3 curator precedence); Idempotency-Key with every precheck inside produce (#53 R2); shared null-body guard extracted to _query.py
- [x] BA6a query playground: binding seam + semantic/sql/global — POST /query/{semantic,sql,global} over `run_bounded_query`, a NEW PUBLIC seam in core/mcp/server wrapping the SAME §21 deadline + per-call binding + §22 degradation envelope the MCP tools use (one machinery, two facades — class 5); per-request `ProjectContext` off the API's lazily-held clients (qdrant/embedder/llm join engine/neo4j in api.deps, all created at first use — #53 R3); policy from the registry config (BA3c seam); the §16 dict reprojects onto the frozen QueryResult (mode/build_id/results/graph_context/warnings/debug; nil-uuid build sentinel when the deadline fires during binding — MCP precedent); QueryRequest.filters/options loudly rejected while unsupported (BA2e owner rule); LLM-unconfigured → typed 503 (GAP note)
- [x] BA6b query playground: graph/hybrid — POST /query/{graph,hybrid} on the same seam; graph params ride the contract's `options` channel as a typed vocabulary (`GraphOptions`, extra=forbid — unknown keys/partial invocations 400 at the shape layer, the same layer as the MCP tool's typed params) while template vocabulary + hop ceiling stay core's in-envelope guardrail (200 + GUARDRAIL_BLOCKED, facade parity); graph rejects `top_k` while unsupported (the MCP tool exposes no per-call cap — never accept-and-ignore, #60 R1); hybrid threads `top_k` through `hybrid_policy` and its pacer starts from binding's REMAINDER of the §21 budget (`latency_budget_ms=remaining_ms` — the C8 class-11 face, sentinel-pinned); options absent → the router skips graph with an in-envelope reason (MCP parity)
- [x] BA7 health/metrics/eval endpoints — GET /projects/{p}/{health,metrics,eval}; /health and /metrics share ONE producer (`core.observability.health.health_report` — class 5: the snapshot can never disagree with the light computed from the same numbers; /metrics reprojects the report's metrics dict); /eval serves the new `latest_eval_payload` (newest build carrying an eval block, started_at desc NULLS LAST): `passed` = the §14 gate's own predicate (failed==0, malformed→null), `regression` = the §20 fingerprint-comparable comparison vs the ACTIVE build (vacuous→null, measured facts only), stored per-case counts ride as cases_passed/cases_failed (the stored int `passed` must not clash into the contract's boolean); deliberately NOT the query surface's 404→409 chain — observation reports on bootstrap/broken states, so no-active-build is a 200 report (the precedence-belongs-to-the-concept lesson cuts both ways); new lazy `qdrant_client` dep in api.deps (zero-I/O at resolution, event-loop-atomic — the neo4j_driver pattern)
- [x] BA8 builds endpoints — GET list (id-desc keyset, BA3 conventions)/get + POST activate/rollback on the NEW public core seam `activate_in_caller_txn` (the run_bounded_query precedent: core's own `activate` wraps the same seam; preflight → project lifecycle lock → `_promote_in_tx`, all in the CALLER's transaction) — composed with §27 idempotency the reservation+lock+promotion commit or roll back ATOMICALLY in the request txn (replay survives the state change it caused; a preflight failure RAISES so it never poisons the key; one pool connection, no #60-R2 convoy); rollback is TARGETED (the contract names the build — core's parameterless rollback() is the CLI convenience) with the §20 gate exempted ONLY for an archived history-restore target (a ready target through /rollback keeps the gate — else it's a §20 bypass; both sides discriminating in the e2e); precedence project 404 → BUILD_NOT_FOUND 404 → BUILD_NOT_READY 409 (failures+deferred in details); stores acquired at the use point after the gates (class 13); Build DTO field-for-field vs the frozen schema (all optional fields contract-nullable → emit null); BuildInfo extended + `get_build_info`/`list_builds_page` readers; prep audit ran BEFORE first push (the #62 lesson) and caught the targeted-rollback, txn-ownership, and gate-bypass traps at design time

- [x] BA9 canonical source-uri enforcement at the SoR + cancel idempotency (FE1/#70 follow-up, owner 2026-07-13) — `_local_path` raises `SourceResolutionError` on any file uri whose DISPLAYED path is not what the worker reads (edge whitespace / control chars urlsplit strips, host-bearing, query/fragment, NUL, empty/root path, //-leading incl. %2F-sprung, raw+encoded dot segments; one trailing slash stays the accepted directory form) — closes the display≠read misread family at the source of truth for CLI/API/MCP-triggered builds the Console gate can't reach (recommended 4× by local review on #70); rider: `useCancelJob` gains the per-attempt Idempotency-Key the other console writes got in #70 (lost-response retry replays the stored cancellation)

## Track 3 — Console frontend (React)  *(needs BA0 contract; v1 = health/jobs/review/playground)*
- [x] ~~H10 browser-QA receipt hook~~ — **DROPPED (owner 2026-07-12; PR #64 closed unmerged).** The browser-QA *rule* stays: FE tasks run the Claude in Chrome pass after e2e (LOOP.md step 4, FE-only) with the evidence linked in the PR — but enforced as an **honest-agent step** (the pass runs; its screenshots/console evidence go in the PR body for the owner and Codex to audit), *not* a push-gate hook. Mechanical enforcement (the receipt + a `gh pr create`/push gate on `task/FE*`) is what dragged in engagement on the PR-creation verb and a permanent command-text `--head` ban whose shell-grammar surface is **unbounded** (~24 review rounds, each fix correct, the surface never bottoming out); the owner paused the loop and dropped it rather than keep chasing spellings. `main` keeps its proven push-gate untouched, and there is no FE task yet, so nothing regresses. Its structural follow-up (H12 pre-push hook) is dropped with it — it existed only to retire that gate's complexity. If mechanical FE enforcement is wanted later, do it **minimally**: a receipt check on the FE branch's `git push` only, with no PR-verb engagement and no command-grammar surface. Rationale + the "wrong layer / unbounded surface — know when to stop and drop" lesson: see [[graphrag-loop-paused-pr5]] class 14.
- [x] FE0 app shell + OpenAPI codegen client + project switcher
- [x] FE7 Project Health home
- [x] FE8 Pipeline/jobs dashboard
- [x] FE5 Entity-resolution review UI
- [x] FE6 Query playground UI
- [x] FE3 檢視 Inspect — read-only browser over the BA3 build-scoped reads, scoped to what DESIGN §10.2 names: **文件/chunks**, one tab each (entity/relation detail + evidence is §10.2's *separate* 圖譜互動探索 page = FE4 — building it here too would leave two edge-evidence surfaces to keep in step). First page + "Load more" over the OPAQUE keyset cursor (these lists are a whole corpus, so the small lists' page-to-exhaustion pattern doesn't transfer), and NEVER a `sort`/`filter` param: `reject_unsupported_query` 400s every `filter[...]` and every non-default sort — on chunks (`sort_field=None`, the default order being the compound `(document_id, ordinal)`) it rejects *every* explicit sort. The invariant is asserted over EVERY recorded request from BOTH tabs, since each fetcher builds its own query object — asserting it for one tab is false-green. Each request re-resolves the active build, so the page pins `meta.build_id` across pages and fails LOUD on a swap rather than showing one table spliced from two corpora; a failed *next* page keeps the good rows and says so. A row click fetches the detail read, which is where `Document.raw` lives at all (the list omits the key — verified against a real build). A missing row answers 404 with `error.code = "VALIDATION_ERROR"`, and `code_for_status` maps EVERY 4xx to that code — so the client branches on the HTTP **status**, not the code, and says what it means: gone from the *active* build.
- [x] FE1 匯入 Import — shipped in #70
- [x] FE2 清洗 Clean — the §10.2 清洗(含抽樣預覽) page, consuming BA4's v1.1 preview RPC and the EXISTING `PATCH /projects/{project}` for rules (no new endpoint — BA4's investigation). Two halves on one page: PREVIEW (text paste — works before any build — or an ingested document from the active build via the FE3 documents list; omitted knobs are OMISSIONS in the body, never 0/null, because the server's config→default fallback chain only runs for absent keys and v1.1 rejects explicit null) and SAVE, whose one load-bearing behavior is **spreading the CURRENT config**: PATCH replaces the whole config column server-side (read from core/registry/store.py, not assumed), so `{config:{chunking}}` alone would silently wipe ontology — pinned by component AND e2e tests asserting the ontology block survives in the PATCH body, revert-probed. The page fails CLOSED while the config read is loading/failed (a form without the real config saves a wipe — class 17's fail-closed applied to a WRITE precondition); the preview is a useMutation (no cache = no stale-while-revalidate window) but its RESULT still answers the inputs it was made with, so any edit marks it stale explicitly rather than letting old chunks impersonate new parameters. The pair rule (0 ≤ overlap < max_chars) is mirrored client-side over the EFFECTIVE values (typed knob → configured → engine default — the composition matters: a typed overlap can be illegal against a configured max_chars), justified by the class-15 criterion: a bad pair SAVED to config fails LATE at the next build's config load, and preview alone can't catch what an operator saves without previewing
- [x] FE4 圖譜互動探索 Graph explorer — the §10.2 three-column page over the EXISTING `GET /projects/{p}/graph/subgraph` (BA4's investigation confirmed: no contract change). Left = entity list (id-keyset load-more) with a client-side filter HONESTLY labelled as covering loaded pages only (the API has no entity search — reject_unsupported_query; FE3's false-affordance lesson). Middle = deterministic radial SVG layout (BFS depth rings from the picked center; nodes the edge set never reaches sit on an outer ring rather than vanishing — the row ceiling can cut their edges); double-click re-centers; hops are sent as chosen and the server's REJECTION (not clamping — C6c) surfaces verbatim. Right = §10.2's named fields from SETTLED detail reads (FE3's three-way exclusive discipline): an edge shows type/confidence/evidence(quote+來源 via the §27.4-denormalized source_uri)/created_by/review_status — evidence rides ONLY the relation detail GET, so the edge click is a real fetch. The §21 precondition is a NAMED state: a project without query_policy gets the machine-readable `details.query_policy="missing"` 400 rendered as configuration guidance (PATCH hint), pinned discriminatively against a non-policy 400 showing the plain message. Subgraph refetches fail closed (class 17)

## Track 4 — Console UX 翻新  *(owner-approved 2026-07-14; design source: `.discuss/ux-redesign/PROPOSAL.md` — 12-screenshot audit on real nmmst data. Root diagnosis: the Console leaks "how the system stores" as "what the user sees"; target user is a non-technical knowledge operator. Owner-confirmed pains: 名詞太艱深/沒有判斷上下文/無意義 id/缺操作說明. Phase C's contract bump (eval + upload endpoints) is owner-approved per DR-002 — record in DESIGN §26 when UXC1a lands.)*
- [x] UXA1 Review 頁重設計 — the P2 fix (the decision surface must carry the decision's basis; today the Candidate column renders a UUID prefix — the reviewer literally cannot see the two NAMES). One-case-at-a-time flow (progress `n/<current queue length>`), names+types from the candidate's OWN snapshots (already in the list payload — no new reads for the headline), each side backed by real context sentences fetched lazily per case from EXISTING endpoints (1-hop subgraph → relation detail evidence quotes — evidence rides only the relation detail GET, FE4's lesson; when an entity has no evidenced edges, say so honestly instead of an empty box). Plain zh-tw copy: the on-screen question is 「這兩個是同一個東西嗎?」, buttons 「是,合併/不是,分開/跳過」; approve/reject are TERMINAL (BA5) so both get an explicit two-step confirm stating irreversibility, and defer's one-shot semantics (deferred → approve/reject only) is labelled before use, not discovered after. Score translated to words; `features` JSON collapses into a 進階 fold. Tests encode the two whys: terminal actions are never one click away (a misclick is unrecoverable by design, so the UI must make it recoverable by interaction), and the case card renders names — never ids — pinned discriminatively (a case whose snapshot name differs from its id prefix)
- [x] UXA2 總覽 Overview 落地頁 — the P5 fix: the first screen answers 「現在什麼狀態,下一步做什麼」 instead of a diagnostics dump. New index route (landing redirect → 總覽; Health stays as the diagnostics page). Status one-liner + knowledge-scale strip (documents/entities/relations in words, not snake_case), a pending-review card that DEEP-LINKS to Review (today's "Needs review" chip is dead), and the four-step setup checklist (① 匯入 ② 建置 ③ 品質 ④ 上線) where each step's done/todo state is DETECTED from existing reads (sources list / builds list / active build's eval block / builds.status=active) — the checklist is a projection of server state, never a client-side wizard flag that can drift (class 17's cache-trust discipline applied to onboarding). Step ④ ships the missing **activate button** (POST activate + Idempotency-Key, confirm step, §14 preflight errors rendered as plain guidance — "沒有評測分數的版本不能上線" with the CLI eval command copyable until UXC2 exists). e2e covers the checklist against mocked states: fresh project / built-not-evaled / ready-to-activate / active
- [x] UXA3 全站翻譯層 sweep — the P1/P4/P7/P9/P10 fix, one pass over every page with a single principle: no UUID, no snake_case, no cursor/pagination mechanics, no engineering explanation on screen (raw values live behind hover/複製/進階 folds). zh-tw across all pages (UI chrome; data stays as stored). Health: label map + the number-overflow layout bug (149 renders vertically) + every non-zero warning count links to its page. Inspect: rows lead with the document TITLE (derive from content heading; fall back to basename — the full file:// path moves to detail), status words translated, total counts shown when knowable. Playground: source_refs collapse behind a count-labelled fold (the 17,450px wall becomes scannable), warnings translated — the resolved reference CARDS this line originally claimed shipped later as SS2 (#88), not in UXA3. Jobs: builds table humanized (短版本號+相對時間, UUID on hover), the "paste a job id from the CLI" affordance reworded to its actual use. Graph: detail panel labels humanized, entity_key/hash into 進階. Import: New project moves to the top as the first-run step, file:// helper text in zh-tw with a copyable example, Ingest/Build collapsed to one 建置 action (they run the same pipeline — the current two-button-plus-apology is the API leaking through). The test that matters: a static sweep asserting the rendered chrome of every page contains no `[0-9a-f]{8}-` UUID pattern and no snake_case label from the known field list — the translation layer's invariant, not per-page snapshots
- [x] UXB1 設定 Settings 頁 — ontology (entity/relation type chip editors) + chunking + query_policy (the §21 fields that matter to an operator: default_mode/max_top_k/max_graph_hops/require_sources; the sql/cypher blocks stay 進階-folded read-only until asked) as FORMS over the EXISTING `PATCH /projects/{project}` — no contract change. Inherits FE2's two load-bearing disciplines verbatim: spread the CURRENT config so a section save never wipes its siblings (pinned by tests asserting the untouched blocks survive in the PATCH body), and fail CLOSED while the config read is loading/failed (a form without the real config saves a wipe). Validation mirrors the server's rules client-side for early feedback but never replaces the server verdict; PATCH errors surface verbatim. This closes the「ontology/policy 只能 curl」hole (FE1's deferred ontology-UI follow-up lands here)
- [x] UXC1a 契約 v1.2 — eval + upload + 通用文件 metadata envelope — **DR-002 bump, owner-approved 2026-07-14**; schema_version 1.1→1.2 + DESIGN §26 record in the SAME PR. Contract + contract-tests ONLY (runtime is UXC1b). Three additions: **(a)** `POST /projects/{project}/builds/{build_id}/eval` — async job (202 + job envelope, SSE-watchable) because a golden set is unbounded work; idempotent per (build, golden-fingerprint); the result lands where activation preflight already reads §14 scores, so Console gating works with zero new coupling. **(b)** `POST /projects/{project}/uploads` — multipart file(s), registers/updates a canonical `file://` source (BA9's canonical-uri rules stay the SoR's — server owns uri normalization); size/extension allowlist from config; NEVER trust client filenames for paths (sanitize to a generated name, keep the original as metadata); 415/413 refusals in the error envelope. **(c)** 通用文件上下文 **metadata envelope** (external review 2026-07-15 §P0#4 — define BEFORE upload freezes, else a second bump right after): a STABLE-core envelope `{schema_version, system, context, governance}` where `system` is connector-generated & user-immutable, `context` fields are **project-defined** (a per-project metadata schema names type/required/display/filterable — NOT a global field enum, so it stays 不失一般性), `governance` (visibility/classification) MUST NOT enter embedding text; plus a **project-level exposure allowlist** so a sensitive field is never auto-leaked to an Agent just because it lives in JSONB (§23-adjacent security boundary — designed in, not retrofitted). Upload/source-register carries per-file `context`/`governance`; `system` is stamped server-side. Contract tests: envelope schema, exposure-allowlist projection, idempotency, 415/413. **Downstream metadata stages** (chunk-local metadata, filterable-field indexing + search-BY-metadata, embedding participation, auto-extract namespace, entity promotion) are EXPLICIT roadmap follow-ups — NOT this task
- [x] UXC1b 後端:eval + upload + stage-1 metadata 傳遞 *(needs UXC1a)* — implement the three UXC1a endpoints + the FIRST metadata stage end-to-end: `capture (upload/register) → validate (against the project metadata schema) → persist (documents.metadata) → snapshot into build → enrich source_ref.metadata on read (chunk→document via the Postgres SoR — review §P0#4 rule 6: no per-chunk duplication, no cross-build drift) → expose through the allowlist (rule 7)`. eval runs the project's golden set against the NAMED build (same core path `cli/main.py eval` walks). Human/connector/system metadata never silently overwrite each other (rule 4). Integration test drives upload→build→eval→activate; metadata verified across **three unrelated document scenarios, no meeting-specific code path** (review §P0#4 completion criterion = the 通用 gate)
- [x] UXC2a 評測頁(品質 tab)*(needs UXC1b; UXC2 切片 1/3 — class-25 輪次經濟學:每個新有狀態面一個 PR,先枚舉狀態機再推第一版)* — 品質 tab grows 評測: run eval on a chosen build (UXC1a's eval endpoint, 202+job envelope), live progress via the job SSE, per-case PASS/FAIL table with the question text as the row identity (scores in words: 通過/未過+分數), and the result feeding UXA2's checklist step ③ automatically (same read — builds list 的 eval block,不開新耦合). 狀態機先行:job queued/running/done/failed/cancelled × SSE in-flight/settled × build 仍存在/已換 active — 每格指定語意(class 17/20/25 的讀寫述詞紀律)
- [x] UXC2b 上傳 UI(資料 tab)*(needs UXC1b; UXC2 切片 2/3)* — 資料 tab grows 上傳: drag-drop/multipart to UXC1a's upload endpoint with per-file success/refusal shown honestly (a refused extension is a stated refusal, not a silent drop — manifest 的 oneOf(accepted, rejected+reason) 逐檔渲染), then the registered source appears in the existing sources list. 含 UXC1a 遺留 follow-up:`format:binary` codegen 出 `string[]` 對不上 FE `File`/`Blob` — gen transform 或 wrapper(runtime openapi-fetch 送 FormData 不看編譯型別,契約語意已正確)
- [x] UXC2c 檢索改名 + 全流程 e2e *(needs UXC2a+UXC2b; UXC2 切片 3/3)* — **Retrieval-first rename (review §P1#5, owner-decided 2026-07-15):** Console「問答測試」→「檢索測試」across the Playground/eval surface — QueryResult stays ranked hits (no answer synthesis), and the UI stops implying per-ref = answer groundedness. e2e: the full no-terminal path — create project → upload → build → eval → activate → query — the Phase 目標 (「不離開瀏覽器」) as an executable assertion

## Track 5 — 功能完整化與長期維運  *(source: external functional review `.discuss/codex_review/FUNCTIONAL_REVIEW_2026-07-15.md` + hakeguan gap ledger `.discuss/hakeguan/GAPS.md` (both gitignored, local-only); owner 2026-07-15「化作 TASKS」. This is the near-term actionable slice — the larger P2 roadmap is a note at the bottom of this file, promoted to tasks item-by-item only on owner ratification per the GAPS accumulate→ratify→promote protocol. The two former ⚠️ owner-confirm flags (CFG1 direction, SRC2 contract bump) were both approved 2026-07-17 — no items are owner-blocked; contract-touching items ride the CTR1 packaged round (DR-013).)*
- [x] CFG1 (owner 2026-07-17 核准,⚠️ 解除;gateway 形狀同日追加)統一 Console/MCP query-policy SoR (review §P0#2, flagged highest-risk) — today the API reads `projects.config` (Postgres) while MCP/CLI reads `projects/<name>/config.yaml` (`core/mcp/policy.py:106`), a split made deliberately 2026-07-10; the review warns the same project can now diverge between human (Console) and Agent (MCP) query policy. Unify to ONE SoR + a generic `serve-mcp --project <name>` (no hand-written Python entrypoint per project — `projects/nmmst/mcp_entrypoint.py` is the current per-project file) + Console showing the MCP URL / connection health / copyable Agent config. **Revisits the prior owner decision → confirm the direction before implementing.**
- [x] SRC1 xlsx 原生 connector (GAPS G1) — new source kind following the pilot-validated render (FAQ / 導覽 families); column mapping (which column is title/body/location/category) lives in the source's `metadata` (contract already `additionalProperties:true` → NO DR-002), never hard-coding 海科館 column names (不失一般性). Defends the known xlsx dirt: template-tail empty rows, a lying sheet dimension (區域探索廳 self-reports ~1M rows), int-vs-float `編號`, annotated headers (`問題(必填)`→`問題`). Retires `.discuss/hakeguan/preprocess.py`.
- [x] CTR1 DR-002 打包契約回合(owner 2026-07-17 核准「一個回合議定全部端點」,仿 DR-009/DR-010)— `openapi.yaml` 1.2→1.3 + DESIGN §26 DR-013,contract + contract-test + web codegen only:SRC2 `PATCH /sources/{id}`(soft-disable,uri 不可變)、GOV2 `/entities|relations/{id}/approve|reject`、GOV3 `/ontology-proposals[/{id}/accept|reject]`、RB1 `/builds/{id}/retry` + `steps[/{s}/items]` 下鑽、SS1b `q` + PageMeta totals、DR-012 rider `GET /projects/{p}/mcp`;ErrorCode +3。各任務 runtime 落地時不再動契約。
- [x] SRC2 來源生命週期 (GAPS G2)(契約=CTR1/DR-013;runtime 落地:PATCH /sources/{id} soft-disable、enabled 欄 migration 0017、build/ingest 排除停用源、SOURCE_NOT_FOUND) — sources have no PATCH/DELETE/disable today; a registered URI is stuck forever, so swapping corpus has no API path. GAPS-recommended **option 2: soft-disable (`enabled:false`) + immutable URI** (swap corpus = disable old + register new; historical builds' provenance is never rewritten — the BA9 canonical-uri spirit). schema_version bump + DESIGN §26.
- [x] GOV1 型別穩定 / 跨型別 resolve (GAPS G4 + full-run DR-003 root cause) — the scale-up 主病根: the same real entity splits across types (`區域探索廳` exists as EXHIBIT/LOCATION/FACILITY/ORGANIZATION) and resolve only pairs WITHIN a type → cross-type twins are never proposed, a real thing stays permanently split; worse, the DR-003 ledger fingerprint includes the type, so type drift makes a prior approve/reject stop matching on the next build (白審 — verified: 1/3 of the full-run carry-forward broke exactly here). Design-first (may touch `fingerprint_version` / DESIGN §17): cross-type resolve candidates and/or a type-stable fingerprint.
- [ ] GOV2 品質治理中心 (review §P1#6 + G4 補強)(契約=CTR1/DR-013,本任務=runtime;因 endpoint+清單+UI 多面切 GOV2-api/GOV2-fe)— DESIGN §17 names four review kinds (entity / relation / merge_candidate / ontology proposal); API/Console handle only merge candidates. Add entity/relation approve-reject, a low-confidence-relation list, a missing-evidence list, per-decision source evidence + impact preview + audit trail, and a project-level publish gate (pending count / missing-evidence rate / extraction-failure rate can block activation). Completion: every non-zero quality signal on Health deep-links to an actionable list.
- [x] GOV2-api entity/relation approve-reject endpoint(GOV2 切片 A;backend 面)— `/entities/{id}/approve|reject`、`/relations/{id}/approve|reject`:§17 unreviewed→approved|rejected,決策寫 review_ledger(DR-011 type-free v2 key=下次 build 讀的同一鍵,carry-forward)+ 更新 active build 列的 status/review_status(approve 復原 active、reject 排除);relation 鍵取自兩端點 ledger key。**留待 GOV2-fe/後續**:治理中心 UI、low-confidence/missing-evidence 清單(既有 SS1a filter 面)、publish gate(activation 既有機制)。
- [ ] GOV2-fe 品質治理中心 UI(GOV2 切片 B;FE 面,與 GOV3-fe 同落地)— Console 治理面:entity/relation 審核操作、low-confidence relation 清單、missing-evidence 清單、per-decision 證據/影響/audit、publish gate;Health 非零品質訊號深連結入對應清單。
- [ ] GOV3 ontology proposal 審核 (review §P1#6)(契約=CTR1/DR-013,本任務=runtime;因 endpoint+UI 兩面切 GOV3-api/GOV3-fe)— 採納/拒絕 UI + endpoint for the C3c ontology **proposal pool** (LLM-proposed new types awaiting review — distinct from UXB1's ontology editing, which edits the CONFIGURED types). Feeds the same governance surface as GOV2.
- [x] GOV3-api ontology proposal 審核 endpoint(GOV3 切片 A;backend 面)— `GET /ontology-proposals`(預設佇列=proposed,filter[status] 審計面)+ `/{id}/accept|reject`;accept 把型別加入 projects.config.ontology(下次 build 生效,鎖 projects 列序列化);reject 只翻狀態;§17 終態守衛;PROPOSAL_NOT_FOUND。**留待 GOV3-fe**:Console 採納/拒絕 UI(與 GOV2 治理面同落地)。
- [ ] GOV3-fe ontology proposal 採納/拒絕 UI(GOV3 切片 B;FE 面,與 GOV2 治理面同落地)— Console 上 proposal pool 的檢視 + 採納/拒絕操作;Health 的 pending_ontology_proposals 深連結入此清單。
- [x] GOV4 merge-candidates `Filter` fail-loud (GAPS O4) — the contract declares a `Filter` param the impl silently ignores (`api/routers/review.py:85`), so `filter=status:approved` returns 200 + pending rows and misleads the consumer (this adaptation was misled one round). Until the filter is implemented, reject unsupported filters with 400 (fail loud) — or implement the `status` facet. Small.
- [x] QP1 自然語言 → 自動安全 graph plan (review §P0#3) — hybrid query skips graph mode unless the caller passes a template + entity seeds, so a plain-NL user gets vector/global only, never the GraphRAG core (`core/query/hybrid.py:13`). Add question entity-linking + 消歧, auto-select an EXISTING safe template (`neighbors`/`path`/`subgraph`), keep the parameterized-template guardrail (NO free Cypher), surface chosen entities/template/reason in the routing trace. Golden cases needing a relation path must select graph mode with no graph options passed, beating the pure-semantic baseline.
- [x] SS1a server-side 篩選 facets(SS1 切片 1/2,owner 2026-07-17 核准拆分)— GOV4 的 filter allowlist 機制推廣到 inspect 列表:entities/relations `filter[type]`(open vocab)+ `filter[status]`/`filter[review_status]`(封閉詞彙=DDL CHECK,contract test 釘 parity)、documents `filter[status]`;免 DR-002(deepObject Filter param 本就凍結存在)。搜尋(`q`)/total/sort 擴充=SS1b。
- [ ] SS1b server-side 搜尋 + totals + sort(SS1 切片 2/2;契約=CTR1/DR-013,本任務=runtime;因跨 backend/FE/perf ≥2 面,再切 SS1b-api/SS1b-fe)— entities/documents 的 `q` 搜尋參數、可用的 total/estimate、sort 欄位擴充;Graph 頁的 client-side filter(`web/src/pages/Graph.tsx:29`)改接 server-side;metadata filterable-field indexing 的 search 半邊(review §P0#4 rule 8)。
- [x] SS1b-api server-side `q` + totals(SS1b 切片 A;backend 面)— `GET /entities`(搜 canonical_name)、`GET /documents`(搜 source_uri)的 `q` 子字串搜尋 + `PageMeta.total`(精確 count,`total_estimated=false`);未支援 `q` 的 list 端點對 `q` fail-loud(GAPS-O4 紀律)。**留待 SS1b-fe/後續**:Graph.tsx 改接 server-side、sort 欄位擴充(需 composite keyset)、大表 total 估算 + metadata filterable-field indexing(`total_estimated` 已預留)。
- [ ] SS1b-fe Graph 頁改接 server-side `q`(SS1b 切片 B;FE 面)— `web/src/pages/Graph.tsx` 的 client-side filter 改用 SS1b-api 的 `q`;更新 false-affordance 說明;瀏覽器 QA。
- [x] SS2 引用卡片 resolve (review §P1#8) — Playground `source_refs` still render raw `source_type` + id + uri inside the fold (`web/src/components/QueryResults.tsx:94-108`); UXA3 shipped the collapse but NOT the name resolution its checkoff claimed. Resolve refs to reference cards (document title / snippet / location, names via existing detail reads, batched+cached) and correct the UXA3 over-claim (TASKS.md UXA3 line). Small.
- [ ] RB1 retry attempt / lineage (review §P1#7)(契約=CTR1/DR-013,本任務=runtime;因 drill-down 讀 + retry 編排 + UI 多面切 RB1-api/RB1-retry/RB1-fe)— a terminal `failed` build can't resume (`core/builds/orchestrator.py:97` `BuildNotResumableError`) and `retry_failed_only()` (`core/observability/spec.py:73`) has no production caller. Add a retry that opens a NEW attempt/build recording `parent_build_id`, reuses successful artifacts, reruns only failed items; Console gains step/item drill-down + a "retry failed only" action. Terminal build records stay immutable (audit integrity).
- [x] RB1-api step/item drill-down 讀端點(RB1 切片 A;backend 讀面)— `GET /builds/{id}/steps`、`GET /builds/{id}/steps/{step_id}/items`:讀 §27.7 pipeline_runs→steps→items,以 run 的 (project,build_id) scope(非 DR-006 build-scoped 表,直讀);filter[status] facet;step 不屬本 build=404。**留待 RB1-retry**:`POST /builds/{id}/retry`(新 build 記 parent_build_id、重用成功產物、重跑失敗項,retry_failed_only 接上 production);**RB1-fe**:Console step/item 下鑽 + 「只重試失敗項」動作。
- [ ] RB1-retry retry attempt 編排(RB1 切片 B;orchestrator+worker;owner 2026-07-18 核准再切 core/skip 兩片,因發現「重用成功產物+只重跑失敗項」需跨庫 clone + 逐項 compute-skip,且與 §27.7「併回同一 build_id」舊語衝突——凍結契約(新 build+parent_build_id+父不可變)為準,§27.7 散文順修)。
- [x] RB1-retry-core lineage + 端點 + documents clone(RB1 切片 B-1)— `POST /builds/{id}/retry`:僅終態 failed 可(否則 409 BUILD_NOT_RETRYABLE)、`builds.parent_build_id` 欄、開子 build 記 parent_build_id、**clone 父的 documents(新 id)**進子 build 後跑既有 pipeline(resume 語意收斂:ingest 以 content_hash 去重、clean 重切、graph 以下重導);父終態紀錄不可變;kind="retry" job 走既有 worker/reaper build 路徑(免新 task)。**刻意只 clone documents**:clone chunks 遇 chunk-config 變動會 InconsistentChunksError 失敗;clone graph-layer 再重跑 graph 會讓 dedup 之外的抽取漂移/增生——故 chunks/graph 以下由子 build 就地重導(fresh),graph-layer 重用留 skip 片。
- [ ] RB1-retry-skip 逐項 compute-skip + graph-layer 重用(RB1 切片 B-2)— 以父 `retry_failed_only` 集合驅動:ingest/clean/graph 只重跑失敗項(成功項不重呼 LLM=省成本+消漂移+凍語料),clone graph-layer 成功產物;`retry_failed_only()`(`core/observability/spec.py:73`)接上 production。完成契約「re-running ONLY its failed items」語意。
- [ ] RB1-fe 失敗恢復 UI(RB1 切片 C;FE 面)— Console step/item 下鑽視圖 + 「只重試失敗項」動作;失敗畫面判因並安全重試。

---

## 路線圖 — P2 擴張與正式營運  *(functional review §P2 + roadmap; NOT queued tasks — the accumulate-in-ledger → owner-ratify → promote-to-task protocol from GAPS applies. Detailed here so nothing is lost; promoted to Track 5 tasks item-by-item on owner sign-off.)*
- **連接器擴充** (review §P2#9) — text is `.txt/.md` only, structured is CSV only; no URL/DB connector (`core/ingest/connectors.py`). Staged: PDF/Office/HTML/OCR (keep page/layout position) → S3/Azure Blob → HTTP crawler + incremental site update → DB incremental sync → scheduled build + change detection + connector health. Data model should split content-blob from source-occurrence so identical content across sources keeps all provenance. (SRC1 xlsx = the first slice.)
- **正式對外:auth/整合/部署** (review §P2#10) — bearer token is extracted but NOT validated; anonymous passes (`api/auth.py`); MCP HTTP has no transport guard (`core/config.py`). OIDC/API-key + RBAC + project membership + MCP service token; mutation/review/activate audit + rate limit; build-level JSONL/CSV/GraphML export + manifest; job/build/review webhooks; full API/worker/web images + Compose + readiness/migration/secrets/TLS/limits; structured logs + Prometheus/OTel + alerts + 3-store backup-restore drill. **Spec gap:** DESIGN §11 (部署) says Compose should include API/worker but `docker-compose.yml` has only the 4 stores; README still needs 3 manual terminals.
- **答案合成** (review §P1#5) — **direction decided 2026-07-15: retrieval-first** (QueryResult stays ranked hits; the rename ships in UXC2). Re-open only if the product later targets human knowledge workers needing cited synthesized answers + claim-level eval.
- **metadata 下游階段** (review §P0#4 rules 4/5/8/9/10, after UXC1b's stage-1) — chunk-local metadata population (clean/chunking), filterable-field indexing + search-BY-metadata (with SS1), embedding participation (opt-in per project), auto-extract namespace + provenance, optional entity/relation promotion.
- **O3 job 詞彙 verify** (GAPS O3) — terminal state is `done` not `succeeded`; confirm the contract enum matches the impl, upgrade to a task only if they diverge.

# Memory Index

- [PR 標題一律英文](graphrag-pr-titles-english.md) — owner 2026-07-17 指示;`<TASK-ID>: <english summary>`,body/commit 維持慣例
- [RB1-retry-skip 設計定案](graphrag-rb1-retry-skip-design.md) — owner 2026-07-19 定案:pin 父設定(凍語料完備)+ v1 最小刀(只跳成功文件 graph LLM)+ 選擇性 clone(整圖 clone 會生鬼影)關鍵洞見;**已實作並 merge(#103,Codex 4 輪;實際加 fork-C/config-pin/chunk_id-remap/entanglement 四守衛,見 lesson catalog #103)**
- [Track 5 owner 全數核准](graphrag-track5-owner-approvals.md) — 2026-07-17「全部同意」;CFG1(#93)、SS1a(#92)、CTR1(#94,DR-013,v1.3)、SRC2(#95)、SS1b-api(#96)、GOV3-api(#97)、GOV2-api(#98)、RB1-api(#99 drill-down)、RB1-retry-core(#100 lineage+端點+documents clone,Codex 5 輪)、SS1b-fe(#101 Graph 伺服器 q 搜尋)、RB1-fe(#102 失敗診斷 UI,Codex 9 輪)、RB1-retry-skip(#103 選擇性 clone+compute-skip+config-pin+entanglement 守衛,Codex 4 輪)runtime 皆已 merge=**RB1 線收官**;尚餘 GOV3-fe/GOV2-fe(治理中心 UI,同落地)
- [Memory 去蕪存菁指示](graphrag-memory-pruning-directive.md) — owner 2026-07-17:retro 附帶 memory 盤點(過時拿掉/重要保存);首輪全掃 2026-07-18 執行完畢,之後 stale 即修
- [懸置 follow-ups 集中帳](graphrag-open-followups.md) — useCancelJob 冪等鍵/ontology 首次設定 UI/UXC2 codegen binary/教學位置/MCP auth 將成真需求/policy.py text= 孤兒參數/server.py dispose 不對稱/untracked data/config 樂觀併發/run-level 失敗成因未曝露到 Console(RB1-api)/RB1-retry-skip entanglement 保守退全導(只重抽糾纏成功 docs 為精細解)
- [graphRAG 架構定案](graphrag-architecture.md) — 多專案 hybrid RAG 平台:Python + LlamaIndex + Postgres/Qdrant/Neo4j,一專案一邏輯 MCP server 經單一 gateway `/mcp/<project>` 供應(CFG1/DR-012),DESIGN.md v0.5(實作凍結版)
- [graphRAG 工作方式](graphrag-working-style.md) — agent 全開發追求最完整、使用者握定案權、可與 ChatGPT 交叉討論
- [Codex +1 才能 merge](codex-plus-one-merge-gate.md) — 合併硬門檻=Codex 👍(+1),無例外;已用 PreToolUse hook 機械性強制
- [Loop 流程教訓 + lesson classes](graphrag-loop-paused-pr5.md) — watcher 三管道;Codex 意見先判讀;同類一次掃完;**LOOP step 8 post-merge retro 比對本檔的 29 個 lesson classes**(契約驗值/入口一致性/規則自洽/工具語意/檢查者分岔/spec 對齊/執行級驗證/邊界語意×表示誤差/防護面完整性×過度阻擋/綁定時檢查≠不變量TOCTOU/請求級不變量掃全生命週期/框架生命週期機制×自有SoR-liveness/eager-acquisition 耦合非依賴路徑/錯層×無界表面-知道何時丟棄機制/等價-已處理論證紅旗×gate失敗時機判準/讀消費者源碼關閉有界表面×雙gate-parity共用corpus×別調弱自己的oracle/快取信任述詞-枚舉快取狀態機-additive-enum配allowlist/契約投影疊層-schema×runtime×codegen×指派語意×編譯旗標各自pin/多query頁面-導出DAG×verdict位置×判別pin構造/寫入生命週期宿主×keyed瞬時卸載-哨兵狀態機枚舉-revert-probe實跑/mock對齊真實錯誤信封-details非message×CLI提示三層shell安全/翻譯即斷言-譯詞讀producer×聚合gauge≠可行動計數×共用CSS枚舉消費者×oracle閉合路由表×會crash的檢查不是gate/寫入端不驗證的靜默磚-每子塊晚炸-client鏡像唯一飛行前守衛×修復即未存狀態×三塊對稱硬化配雙gate-corpus×跨身分keyed-remount×同頁併發鎖≠跨寫者版本鎖/散文承諾必須結構凍結-每條never-always-must-only配結構pin×dead-component無$ref×關聯鍵required×無法靜態表達的策略凍shape配runtime-enforce×prose對齊schema/寫入授權矩陣-修法即新面×跨≥2新面計畫期切片/隱含預設不跨時間與交接-殘留狀態gate身分×世界狀態stub掛觀測邊界/放寬可達集必須重推導下游每道守衛的隱含前提-三元件聯合自洽抽一即壞×判準下沉到值×不可證同fail-closed/身分切段在未解碼表示-編碼分隔符不產生結構×段內單獨unquote/同一身分兩投影同源單讀-記錄≡pin-by-construction×鎖序不反轉寧事後rekey);即時狀態查 TASKS.md/GitHub;anyio-cancel-scope綁task=host-task-per-child×fake-child帶真task-group才判別(#93);簽名遷移枚舉全部stub站點×雙向mutation-probe證oracle未降級(#93);actionable守衛先於會因無關原因失敗的資源建構(#93);class24補兩面(#94):每個新request-body固定語意配additionalProperties:false-一body抓到≠掃完×廣告的response狀態必須端點路由形狀可產生-required-but-nullable分支不可達即narrow或改非path-keyed面;打包契約回合=每新schema過class-24檢查表一次(#94 CTR1單輪收斂);runtime落地=契約管不到的兩面(#95 SRC2):gate狀態欄用strict布林對齊契約type×gate-2的parity-nit當該欄gate變更動作時應主動當must-fix;加「參與述詞」(enabled_only)枚舉全部評估站-backend執行器+FE-predictive-gate鏡射(class27收窄對偶)×gate用enabled子集-display用全集;測正規化述詞fixture須含被正規化維度的變異(#96 SS1b-api:大小寫不敏用CJK=caseless假綠→ASCII-cased×ilike→like-probe;萬用字元當字面反例列須「若當語法會被誤中」a%b-vs-axyzb;count與page同scope同述詞-cursor除外);「我的閘=別處的閘」能重用就別重寫-重用不了才雙gate-corpus且含弱式漏角(#97 GOV3-api:accept閘直接走build的_load_ontology-消滅第二閘;R2驗證前正規化=修補壞輸入-先驗原值再改別讓.get()or[]/list(str)洗合法;R3新寫者參與既有無版本併發缺口≠新bug-reply-and-resolve附update_project無版本檢查之結構事實);gate-2與Codex對「是否bug」分歧時凍結契約散文是仲裁者-實作須對齊自己在契約寫的散文(#98 GOV2-api:decide_entity沿用merge終態拒但契約載「re-deciding appends never conflicts」→移除終態改append-only-ledger更正§27.3-latest-wins;鏡射函式逐處問「語意真同模板嗎」-驗review_status非status×relation鍵取端點ledger-key×無終態;整合.one()/count斷言查詢必project-scope=H11);改list端點「序」牽五連動-序×keyset×cursor×sort-affordance×index一起想(#99 RB1-api:keyset依契約宣稱序非物理id-newest-run-first×測排序兩列鍵須人為構造錯實作會排錯-隨機值假綠;改序即回查reject_unsupported_query的sort_field=否仍等新序-複合序用None;public-core-seam必自我強制簽名的scope參數-pyright未用參數在seam=洩漏漏洞非風格-list_step_items加build-scope;新migration更新head-pin+明確git-add);pre-commit資源+async消費=枚舉consumer每條失敗路各回收(#100 RB1-retry-core,Codex-5輪:retry-child先建building-worker-preflight失敗須一併mark_build_failed否則孤兒阻塞專案×父0-doc→409-all-or-nothing階段令0產出成精確信號×reuse≠重跑收斂-凍語料須讓ingest短路否則重讀live-sources失敗漂移-full-rerun才POST-build×compose-txn三序-便宜guard先於昂貴clone/replay先於存在gate入produce/強鎖FOR-UPDATE先於create_build的FK-share消鎖升級死結×衝突+大新能力先AskUserQuestion上報取切片決策別猜×gate-2每輪PASS仍Codex-5輪-深端點pre-create+async+並發+多失敗態須多輪×rowcount當gate輸入驗2-doc與0-doc兩端);退場false-affordance要grep全部實例-註解與實作分處(#101 SS1b-fe:漏Graph.tsx檔頭第二處FE4-note×debounced搜尋UI標籤鍵在applied值非raw-input否則debounce窗內標籤與身旁結果不符×輸入值流向有約束server參數就鏡射-錯誤路徑會移除輸入時尤為可用性必需-maxLength=256×browser-QA用network-request檢視確定性驗前端-別因本地後端stale誤判×shared-hook以optional尾參加寬對既有較窄callback向後相容);class17寫入側=快取世界觀滲入write path(spread/fallback/驗證全對同一次fresh read導出);鍵家族遷移=枚舉全部mint-lookup站點×測試種子用spec公式非抄實作(#86);「禁入band」=結構旗標非分數竄改-x±epsilon寫回再比band邊界即smell(#86);深診斷FE=多失敗表示面逐層審-Codex-9輪(#102 RB1-fe):隱藏失敗的警語必無條件-gate在「無較早失敗項」會讓較晚崩潰躲在較早項失敗後(R4)×等特異度覆蓋須排在base規則「之後」-暗色@media置於base之前輸source-order敗(R4-CSS)×verbosity依賴語料的診斷視圖必filter到目標子集(sampled/all另存成功項×id序埋失敗)-測stub鍵在真query參數filter[status]非path-only×mutation-probe抽filter即失敗×false-affordance=導向流程取不到的識別碼-先驗API真曝露(Build無job_id×無build→job/jobs清單)×grep全部實例×分「缺陷修復」(範圍內移除假導向)與「能力新增」(凍契約→具名後端follow-up)別在FE切片建跨面能力(P1)×讀DTO帶成因欄(BuildStep.error)必呈現非丟棄-尤其彙總失敗無逐項訊息×新增呈現面即回頭改與其矛盾的散文說明(step-error)×react-query分頁render鍵在data在否非isSuccess-fetchNextPage失敗翻isError卻保留pages-keyed在isSuccess會清空已載入診斷+移除retry×分initial-load錯(無data)與next-page錯(有data=保留+內嵌錯+重試)(next-page)×每輪must-fix=fix+判別mutation-probe測(revert實跑證oracle未降級)+re-stamp+reply-resolve為標準;深跨庫「重用」特徵=多重「忠實性」危害逐輪暴露-Codex-4輪(#103 RB1-retry-skip,設計先行:Plan-agent+AskUserQuestion定案 pin父設定+v1最小刀,5-fork-surface-2-ratify):選擇性clone-失敗doc有「部分已提交」列故只clone成功doc產物×失敗doc整份重抽×id-remap用凍結entity_key/relation_signature免物化映射×embedding_point_id不clone(子重嵌)×merged子圖==全新rebuild為oracle;fork-C(R1)§22容忍門檻下graph失敗故父可「於graph失敗仍跑完resolve再於後階段失敗」-resolve合併實體(loser→merged稽核列+mention重指向+relation降null-sig)故clone無法重用post-resolve圖-守衛須辨「是否跑過resolve」非僅「failed_docs非空」→退全導;config-pin端點pin不足-graph階段須「確認pin成立」(父config==子config,None=legacy→退全導)-為此移EVAL_JOB_KIND入core讓graph階段可查(R2-P1);cloned-chunk_id須remap到子重切chunk(config-pin保證ordinal吻合故remap必中)-兩修相扣(R2-P2);first-write-wins-entanglement(R3-relation+R4-entity):entity/relation列scalar為首寫者定、後不更新-失敗doc於失敗前首寫某列留部分scalar被clone保留+preload凍-偵測「同列被失敗+非失敗doc同時觸及」→退全導×實體檢查subsumes關係檢查(chunk-evidence-relation端點必被同doc-text-mention);oracle略去非確定欄(relation-confidence因doc迭代無ORDER-BY非確定→只斷言identity/evidence集非confidence-記considered-residual);保守「無法忠實選擇性重用即退全導」為每危害之安全方向;follow-up:只重抽糾纏的成功docs非全建置
- [CI 先於 Codex](graphrag-ci-before-codex.md) — push 後先 `gh pr checks` 修 CI 紅燈再 triage Codex(**gate 命令絕不過管線吞 exit code**);等 Codex 一律 `scripts/watch-codex.sh`(exit 0/10/20/30;triage-without-poke 後下次 watch 帶 `--anchor <handled-ts>`);**PR 寫入前同腳本 fresh-state check**(+1 須晚於 head);切片分支名對到子項
- [BA 真實 LLM 兩 lane](graphrag-ba-real-llm.md) — 快速 hermetic(CI)vs 真實 LLM(本地 pre-push/真資料 QA);key local-only;只在動到 model 的任務花呼叫
- [初期落地場景:博物館導覽](graphrag-goal-museum-guide.md) — 外部 no-code agent 平台經 MCP(HTTP)接本平台當場所知識服務;硬約束=不失一般性;auth 將隨對外曝露成真需求
- [FE 瀏覽器操作測試](graphrag-fe-browser-qa.md) — FE 任務 e2e 綠後、開 PR 前,用 Claude in Chrome 真瀏覽器走 UI 流程(console/截圖證據附 PR);補充驗證步非機械 gate(owner 2026-07-11 定案)
- [海科館真實資料](graphrag-sample-data-hakeguan.md) — nmmst 全量 425 列已建置(1409實體/1158關係);GAPS 帳本已全數化任務或了結;job 終態=done;dev 庫資料踩全表計數測試=H11
- [UX 翻新(Track 4,已完成)](graphrag-ux-redesign.md) — 九項全 merge(#76-#84,教訓在 lesson catalog classes 20-26);留存協議=GAPS accumulate→ratify→promote、P2 A 方案、retrieval-first

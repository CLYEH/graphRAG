---
name: graphrag-loop-paused-pr5
description: loop 流程教訓 + 審查 lesson classes 目錄(LOOP step 8 retro 用)——PR/任務即時狀態一律查 TASKS.md 與 GitHub,不要信任本檔的狀態敘述
metadata: 
  node_type: memory
  type: project
  originSessionId: 3cfe1f1b-4e4e-4d2f-940f-12a9f9d7def4
---

**PR/任務的即時狀態請一律查 `TASKS.md` 與 GitHub(`gh pr view`/`gh pr list`)。這份筆記只保留跨任務可重用的流程教訓,任何「目前狀態」敘述在下次 push/merge 後就會過期,不可當作現況依據。**

- **教訓(監聽 Codex)**:Codex 的「changes wanted」走 **`pulls/N/reviews`(PR review + inline threads)**,不是 issue comment;+1 走 issue reaction。watcher 必須同時輪詢 reactions、reviews、comments 三個管道。
- **merge 授權(使用者 2026-07-03 定案,取代先前「由使用者跑 merge」教訓)**:agent 可自行 `gh pr merge`——mismerge 由 PreToolUse hook(`require-codex-approval.sh`:要求 head commit 之後的 +1、無 `eyes` 殘留、無未解 Codex thread,查詢失敗即擋)機械性防止。auto-mode 分類器若仍擋,向使用者轉述此授權即可,不要繞過 hook。此為「+1 已存在」的正常路徑;+1 拿不到時仍走下方「額度用盡」bullet 的 stop-and-ask,由使用者本人決定。
- **Codex P2/P3 意見判讀政策(使用者 2026-07-03 定案;已由 H2 寫入 LOOP.md step 7,以該文為準)**:P2/P3 級意見先判讀必要性——**必要**=違反 DESIGN 凍結保證/契約或文件內部矛盾/會誤導未來實作/真 bug;**非必要**=超出 DESIGN 文字的假設性強化、風格偏好、無互通性依據的凍結要求 → 回覆給出**該準則對應的可查核理由**(LOOP step 7 逐條列出:無條文要求/無行為差異則明述;可調項引 🔧 條文並指出無互通性依據;過度收緊引定義該合法情境的 §/DR)後 resolve 不修;講不出理由=該修。判讀模糊時 stop and ask。+1 門檻不變。
- **教訓(壓 review 輪次)**:Codex 傾向一輪只給一條;收到意見後要**主動掃整個 diff 的同類問題**一次修完,否則會一輪一條擠牙膏(P1 因此走了 7 輪)。
- **教訓(review race)**:push 後 Codex 可能仍對「舊 head」留 thread、隨後才對新 head +1;判讀 thread 前先確認它評的內容是否已被現 head 修掉,是則回覆指向修正 commit 後 resolve。
- **Codex 額度用盡時的收斂路徑(使用者行使過,H3/PR #9)**:規則的逃生閥是「stop and ask」——當其他 gates 全綠、剩餘未經 Codex 的 delta 小且已被本地執行級審查驗證時,由**使用者本人**決定 merge(從 web UI 或自己的終端;本機 hook 只認 +1,會擋 agent)。這是 owner 決定,不是 agent 例外。
- **教訓(執行級審查)**:純推理的 code review 連過兩輪都沒發現收據機制根本跑不起來(git 拒絕零位元組 index);審 shell/hook 這類基礎設施時 reviewer 要**實際執行**,並補執行級測試(tests/test_receipts.py 模式)。
- 慣例備忘:TASKS.md 勾選一律含在該任務 PR 內;PR 等 gates 期間可從 main 開下一個獨立 task 分支(LOOP.md 已明文)。
- **doc-only fast lane(H3 起)**:純 `*.md` 變更不開 PR、不經 Codex —— `docs/<id>` 分支 → doc-reviewer(sonnet)PASS → CI 綠 → fast-forward 進 main;push-gate hook 機械擋非 .md。等 Codex 用 `scripts/watch-codex.sh <pr>`(exit 0=+1/10=有意見/20=timeout/30=Codex 額度用盡——別再等,額度重置後再 `@codex review`),不要手寫 watcher。
- **並行分支的兩個坑(PR #15 實測)**:(1)review receipt 是單一槽位——task/* 與 docs/* 並行時互相覆蓋 PASS 章,push gate 會 fail-closed 擋下(對:再請 reviewer 蓋章;錯:自己蓋)→ H5 立案改 per-branch;resume 的 reviewer 給了 PASS **不會自動蓋章**,要明說「蓋章並回報 tree hash」。(2)doc lane 先進 main 會讓等待中的 PR 變 not-up-to-date → update-branch 產生新 head → 需要**新的 +1**(一輪 Codex 來回);等待中 PR 快 +1 時,doc-lane 的 main push 晚一點再做。

## 審查發現的 lesson classes(LOOP step 8 post-merge retro 的比對目錄;首次盤點=PR #2–#11,#11 當時尚未合併、僅計入其本地審查發現)
1. **契約驗值**(P1 七輪;**#11 再犯**:空白 disambiguator 鑄出不同鍵;**#12 再犯**:`must_have_valid_paths: false`／`groundedness_min: 0` 可當一個 case 唯一的「斷言」,讓 ≥1-assertion 下限被無作用值虛滿;**#13 再犯(跨欄位亞種)**:`default_mode: sql` × `text_to_sql.enabled: false` 自相矛盾的 config 通過驗證——同 PR 作者自己抓到了 enabled×空白名單這對,卻漏了 default_mode×enabled 這對同種矛盾):schema 只驗結構不驗值 —— 每個 required 欄位都要有型別/範圍/長度約束;識別子禁空字串;各 result 型別有內容最低要求;**每個 optional 欄位(不限字串)都要問「有沒有哪個值等於什麼都不主張?」**——no-op 值(false、0、空集)要嘛禁掉(const/exclusiveMinimum 通常就夠)要嘛明定語意;**跨欄位也要掃:列舉所有 (selector, 被 gate 的能力) 欄位對——default/toggle、enabled/whitelist、mode/子塊——逐對驗證矛盾組合被 if/then 拒絕,抓到一對≠掃完整類**。防線:contract 測試必含 rejection cases(mutator 模式)+ optional 參數的空白/no-op 等價測試 + 每個欄位對的矛盾組合 mutation;**reviewer 想以「schema 表達不了」為由把值級風險延後到 runtime 前,必須先實際試寫 schema 證明表達不了**(#12 的兩例都一行就表達了;#13 的跨欄位矛盾一個 root allOf if/then 就表達了)。**#15 再犯(DB CHECK 亞種)**:pipeline_runs 的 kind×build_id——作者以「kind 是開放詞彙、CHECK 無法列舉」為由只凍結 §27.7 點名的 ingest 一對;Codex 指出 §27.7 的 null 邊界是**窮盡式**(僅純來源驗證可 null)→ 修正=改從**例外側**表述(`build_id IS NOT NULL OR kind='source_validation'`),開放詞彙異議即消失。追加兩條:(a)spec「邊界」條目點名允許例外時,點名的例外就是唯一例外,約束照窮盡讀;(b)「表達不了」的舉證義務同樣適用 DB constraint——先試著把規則從例外側/反面寫一行。
2. **入口一致性**(#7/#8 多輪;**#11 再犯**:§17 的 `needs_review` vs `review_status` 的 `unreviewed` 詞彙分家;**#12 局部再犯但本地 gate 咬住**:新增第三個凍結契約時 DESIGN §26/CLAUDE.md 的 DR-002 列舉沒同步,code-reviewer 在 push 前抓到):規則改了但舊敘述殘留在其他入口(CLAUDE/LOOP/memory/索引)——改規則時 **grep 全部 tracked *.md** 掃同款措辭;**對凍結集合「新增成員」時,grep 既有成員的名字**(如 `mcp_response.schema.json`)找出所有列舉點一併補;**DESIGN 對同一概念給了兩套詞彙時,實作要同時接受/映射兩者並註明**。防線:doc-reviewer checklist 第 2 條。
3. **規則自洽**(#8 兩輪):meta 規則自身矛盾(要求提出不可能存在的證據=實質改寫分類)。防線:寫規則時逐分支驗證「每個允許情境都給得出所要求的證據」。
4. **工具語意**(#9 兩輪 + alembic cp950):CLI/API 輸出語意陷阱 —— rename 摺疊(`--no-renames`)、分頁預設 30 筆(`--paginate`)、文字比對≠值比對、locale 編碼。防線:凡依賴工具輸出做判斷,先實測該工具的邊緣行為。
5. **檢查者/消費者分岔**(#10 + H1 CI-skip):探測器檢查的目標與實際消費者的行為不一致(probe port ≠ driver port;skip 造成綠燈)。防線:checker 的參數一律從消費者同一來源導出。
6. **spec/實作逐字對齊**(P3 本地 blocker,同 PR 兩例:relation `norm(type)`、disambiguator 空白語意):凍結規格(fingerprint、契約)在 DESIGN 與程式要逐字一致並互相引用;凡「版本化凍結物」變更即升版。防線:code-reviewer checklist 第 5 條 + DR-007 模式;**審凍結規格模組時,先把 DESIGN 條文和 docstring 並排 diff**。
7. **執行級驗證**(H3 mktemp 死鎖):見上方「執行級審查」教訓 —— 基礎設施腳本要跑過才算審過。
8. **邊界語意 × 表示誤差**(#12:`is_eval_regression` 的「恰好等於門檻可容忍」被浮點減法打破 —— `0.8-0.5=0.30000000000000004`,naive `<` 把合約明文容忍的 exact-threshold drop 判成 regression;本地 reviewer 有執行驗證但只驗了幸運組合 `0.9-0.1` 就概括「不脆弱」):凍結規則帶邊界語意(恰好等於、off-by-one、上下限)時,**驗證要用對抗性取值**(二進位不可精確表示的小數、極端值),單一順手樣本驗過≠邊界成立;實作面比較浮點分數要帶明確 tolerance。防線:reviewer 對邊界宣稱至少驗兩組非精確表示組合;機械化=property-based tests(→ H4)。
9. **防護面完整性 × 過度阻擋**(C1b 立案,7 輪中的 3 輪:round 4–6):建一道「拒絕危險構造」的 guard 時,危險構造常有多個 sibling API 產生同效果 —— raw SQL 逃逸走 `text()` → `literal_column()` → 巢狀在 `or_`/`and_` 內 → `op()`/`bool_op()` 自訂運算子字串,同一道 guard 走了三輪(round 4 已一次收 `text()`+`literal_column()` 兩個 sibling,仍漏了巢狀深度與 custom_op)。這是 class 1「枚舉整個面、一個實例≠整類」母題換到程式 API 防護層。**但多一個 class 1 沒有的對偶**:收緊 guard 會**過度阻擋**合法 sibling —— round 6 一刀砍掉所有 `custom_op`,連每張核心表都要用的 JSONB `->>`/`@>`/`?` 都擋掉,只因加了「安全運算子仍通過」的正向測試才抓到。防線:(a) 看到一個被禁的 constructor,grep library surface 找出所有 sibling 並在測試裡組合/巢狀它們(× 所有巢狀深度);(b) 拒絕型 guard 的測試**必含正向 acceptance case**(Rule 9)—— 只驗「攻擊被擋」對過度阻擋是 false-green;(c) 判別 gate 在「值/字串」而非「型別」(round 6 修正=用 PG §4.1.3 運算子字元集判 opstring 安全,而非一律禁 custom_op)。已寫入 code-reviewer checklist 第 7 條。
10. **綁定時檢查 ≠ 不變量(TOCTOU)**(C1b round 7 立案):在 construction/binding 當下驗過的前提,若底層狀態會被並發改變,對「之後的」操作並不成立 —— writer 綁定時 build 是 building,activation 後它還能寫進已成 live 快照的 build。修法:把 recheck 摺進**產生變更的那句 statement**(原子)或持正確的鎖,別「綁定驗一次、之後信任」。防線:(a) 凡「驗證後回傳一個之後會被重複使用的 capability 物件」,問「這前提會不會在兩次使用之間被別人改掉?」;(b) 並發交錯要在**真基礎設施上實測**(C1b 用 `INSERT..SELECT..WHERE EXISTS(status='building' FOR SHARE)`:FOR SHARE 與 activation UPDATE 的 row lock 互斥,activation 阻塞到寫入交易結束 —— execution-verified,非推理);(c) MVCC 下「純 recheck」仍會 race 未提交的 activation(讀不擋寫),要嘛原子語句、要嘛鎖。已寫入 code-reviewer checklist 第 7 條。
11. **請求級不變量要一次掃完整個生命週期**(C8/PR#37 立案,deadline saga 5 輪:R1 latency reconcile→R3 hybrid wall-clock→R4 standalone sweep→R5 binding 納入→R7 剩餘預算貫穿):當需求是「對一個 REQUEST 的不變量」(延遲預算、配額、追蹤完整性),「每段各自有 cap」≠「請求有 cap」——各段跑滿自己的預算、或在段落交接時**重新起算**新預算,總量即複利爆表。防線:設計時列舉請求生命週期的**每一段**(binding/acquisition、selection、各 mode/phase、discovery、assembly),逐段對照不變量一次掃完;跨段交接傳「剩餘量」,不傳「全額」。已寫入 code-reviewer checklist 第 7 條。
12. **框架生命週期機制 × 應用自有 SoR/liveness**(#51 立案候選,**#52 R2/R3/R4 再犯 → 正式化**;BA2d saga):把工作跑在框架的執行生命週期上(dispatch/retry/timeout/dedup key/result retention/cancel)時,每個機制都有精確語意,必須**讀源碼列成「框架出口清單」再實作**——框架 timeout 會在 SoR 終結化之前取消 handler 且未必重試(#51 P1);dedup/result key 的存活期超出直覺(in-progress key=job_timeout+10s 擋重派;已完成 job 的 result key 保留 custom id 達 keep_result 秒,#52 R4)。防線(各花一輪學來):(a) crash 復原**絕不耦合**框架 timeout——自有 liveness(DB lease+reaper),框架 timeout 只當寬鬆 backstop(#51 P1);(b) liveness 標記**包住整個 handler**(進 handler 第一件事=取租約;unmarked 程式碼裡的 crash 對 recovery 隱形,#52 R2);(c) **recovery channel 自身也是一個生命週期**:deterministic per-generation id(stale-lease expiry=世代標記)讓 re-tick dedup 而非堆積(#52 R3)、框架的 key 保留不可跨世代扣住該 id(#52 R4)、掃描要索引(#52 R5);(d) recovery 撞上「活著但慢」的原 worker 要經 SoR 原子狀態檢查降級成良性 no-op(#52 R1)。審查問:「這個框架機制觸發時,SoR 列被留在什麼狀態?recovery channel 看得見且能收斂嗎?」已寫入 code-reviewer checklist 第 7 條。
13. **eager dependency acquisition 把非依賴路徑耦合到資源可用性**(#53 R3 立案候選(arq Redis:`Depends(arq_redis)` 在路由入口開 pool,Redis 掛掉連 replay/409/404/400 都 500);#54 設計期預防(SSE poller seam 生而 zero-I/O);**#62 R1 再犯 → 正式化**(health 的 Vectors/Graph route deps 在 404 gate 之前解析——連 missing-project/bootstrap 回應都被 Neo4j/Qdrant 建構期 config 驗證綁架)):框架 DI=隱形 eager 點——dependency 解析發生在 handler 邏輯(gate、replay、404)之前,任何「解析時做工」(開連線、驗 config)都把不需要該資源的路徑耦合到它的可用性。防線:(a) provider/dep 一律 zero-I/O at resolution(建構會驗 config 的 client 也算「做工」——延到 use point);(b) 資源在**恰好需要它的分支內**取得(pass providers,probe 分支內 invoke——#62 修法);(c) 測試=判別性「壞資源 config + 非依賴路徑」pin(raising provider + missing project → 404 非 500)+ 結構性 must-not-acquire pin(bootstrap/failed-newest 分支給會炸的 provider)。已寫入 code-reviewer checklist 第 7 條。
14. **錯層 / 無界表面 — 停止逐一修補,改走「已解析」接縫,並知道何時把機制整個丟掉**(#64/H10 立案,~24 輪 Codex + 多輪本地 FAIL、額度燒盡;**owner 2026-07-12 介入喊停、砍回範圍、最終 DROP H10+H12、PR closed unmerged**):一道 guard 建在「命令文字分析」層——PreToolUse hook 讀 **bash 命令字串**去預測 git/gh 會做什麼。bash × git 文法**無界**(aliases → alias chains → `--config-env` → option abbreviations → refspec/tag spellings → process substitution → quoting → `$'…'` ANSI-C/locale quoting → attached shorthand `-Rowner/repo` → hex escapes…),每關一個拼法就露下一個;hook 83→669 行、tests 145→1629、表面從未見底。**每個 Codex finding 都正確、每個修法都成立——病不在 finding,在層級**。這是 class 9(防護面完整性「枚舉整個面」)被推到斷裂點的**對偶**:面**無界**時枚舉本身就是錯工具。診斷信號:(a) 同一道 guard 連 3+ 輪、每輪一個新拼法/sibling;(b) 修法都是「再加一個 pattern」而非「換判準」;(c) 你在**預測另一個解譯器**(bash/git)怎麼讀輸入,而非檢查它**已讀完**的結果。結構性答案＝找「已解析」接縫(git pre-push hook 收 already-resolved ref tuples、allow-list 比對,~50 行無文法可繞)——**allow-list 有限 > deny-list 無界**。**四個對偶**:(甲) **不是每個表面都有下游接縫**:`gh pr create --head` 從 already-remote ref 開 PR 且**跳過 push**(gh manual)→ 沒有 pre-push hook 看得到 → 這塊只能留文字層;「走結構性」≠「刪文字層」,是「能下推的下推、下推不了的那小塊誠實留下並標明」。(乙) **留在文字層的用『建模 interpreter 的一步』不用『列拼法』**:刪可移除字元(quote-strip `"'\`+`$'…'` sigil)一次收所有可移除字元引號;把 flag-token 建模成 whitespace-delimited word(`[^space]+`)一次收所有 attached-shorthand(`-Rowner/repo`,不論 `/`:`=`)——`[A-Za-z-]+` 停在 `/` 曾繞過 engagement(P1)。但 filter/token-model 非完整 interpreter:hex-escape / expansion / 值內空白是 residue,**明講**非假完整。(丙) **deny-rule 必配 accept pin**(class 9 對偶):只 ADD matches 的正規化,過度阻擋是 false-green;且判別性 pin 要真的驗得到——受 receipt 齊全的 repo 對 engaged/非 engaged 都回 0＝**盲於 engagement**,得用「engaged 才會 DENY」的取值(attached shorthand ＋ head flag)。(丁) **最重要:知道何時停手、把機制整個丟掉**——owner 的判斷不是「再找更好的拼法防線」,而是「這條規則值不值得用**機械 hook** 強制?」;H10 的 browser-QA 規則改回 **honest-agent 紀律**(證據附 PR、owner/Codex 抽查)就夠,機制連同其無界表面一起 DROP。教訓:**站錯層時先問『這個不變量非得用會引出無界表面的機制強制嗎?降級成紀律 gate 會怎樣?』**——常常答案是「紀律就夠,別蓋那座 hook」。**流程對偶**:(1) live-hook 自我干擾(push-gate grep 自己的 Bash payload → commit message/reply 避開 push 動詞、`--head`、`gh pr create`、`task/FE`、`:main`;危險拼法 probe 寫進 script 檔跑);(2) 誠實對 reviewer(revert ~24 輪「fixed in <sha>」時公開留言說明);(3) watcher(詳 [[graphrag-ci-before-codex]])——eyes present 絕不 poke(owner mandate,已寫入 LOOP step 7)、push 後 Codex 自動 re-review 故不 poke、手寫 poll 要 `--paginate`、跨時區時間戳別 lexical 比較。

15. **「等價/已處理」論證紅旗 × gate 取捨的失敗時機判準**(#70 立案,兩次翻案+一次成功 decline):(a) 用「兩個衍生視圖收斂」(%2e%2e:browser 讀 /etc == backend 讀 /etc,故放行)或「伺服器已有 dedup」(create_job_exclusive 409)免除 gate 是紅旗——前者漏掉不變量其實是「**儲存形式**透明命名其效果」(顯示 %2e%2e 而讀 /etc 就是 display≠read,兩個衍生視圖收斂不救),後者漏掉 **lost-response retry**(running 時 409 無 job id 可看;terminal 後重試=雙跑 full pipeline)——「server already handles it」必須對掉包回應的重試情境檢查,不只並發。修法形狀:直接結構性驗證 backend-derived 值/送 per-attempt idempotency key,不做等價推理。(b) 對偶(decline 判準,finding 22):要不要建 client mirror gate,判準=**失敗時機×透明度**——失敗 LATE(已花實際工作)或 SILENT(錯資料)→ gate;失敗 INSTANT+LOUD+零花費(worker preflight raise、訊息 inline 可見)→ fail-loud 即 sanctioned UX,補 mirror 只是 class 5 checker-fork 稅(每個後端 normalization 步驟都是 divergence point)。mirror 只在**小、封閉、失敗晚**的面上值得;無界面→推給後端 SoR raise(follow-up 模式)。decline 必須可查核(引 call site+失敗路徑)且經 reviewer 獨立驗證「無 late/silent 逃逸路徑」。

16. **有界表面的收斂法:讀「消費者的原始碼」枚舉其結構決策 × 雙 gate parity 只能用共用 corpus 證明 × 別把自己的 oracle 調弱到閉嘴**(#71/BA9 立案,6 條 Codex P2 + 5 輪本地 FAIL,全部同一 class;**另有 5 個同族缺陷是本地 reviewer 先抓到、Codex 從未看到**——改讀源碼之後才輪到我們跑在 Codex 前面,這就是額度經濟學的正面示範):guard 驗證的值,和真正消費它的程式所依據的值,**在不同層**——`_local_path` 驗 decoded path,但 `nturl2path.url2pathname` 的結構決策全做在 **encoded** 字串上(第一句就是 `url.replace(':','|')`)。每次用**舉例**關掉(拒 `|` → 拒 `:` → 拒 `%3A` → 拒 malformed `%` → 拒 invalid UTF-8),下一個拼法就從同一個機制冒出來。**這是 class 14 的對偶但結局相反:表面其實有界,只是你沒證明**——證明的方法不是跑更大的 corpus,而是**讀消費者的原始碼、把它的結構決策列成表**,關完就 by construction 關上。BA9 最終的三軸表(可直接複用的 artifact):**resolver**(`nturl2path` 對 encoded 字串做的結構決策:`/`、`:`、`|`,加 decode 後的 `\`)/ **decoder**(`unquote` vs `decodeURIComponent` 恰好差兩處:malformed escape、hex 合法但非 UTF-8——`unquote` 預設 `errors="replace"` 靜默換 U+FFFD,**讀到被竄改的路徑**)/ **parser**(`urlparse` vs WHATWG:在已接受的定義域上**根本沒有人在 parse**——triple-slash 規則把 netloc 釘成 `''`、`?`/`#` 兩側都拒,path 就是 raw 後綴)。**三個新防線**:(甲) **兩道 gate 必須接受完全相同的集合,而 per-side 測試對此結構性失明**——各自自洽、彼此不合,就會出現「API/CLI 建得起來、Console 永遠建不起來(且把整個 project 的 build 全擋掉)」。唯一能把它變成不變量而非期望的東西=**兩套 suite 共讀的同一份 corpus**(`tests/fixtures/canonical_file_uri.json`,pytest + vitest;複製兩份會 drift,drift 的 parity corpus 什麼都沒證明)。(乙) **純 differential 對「兩邊共有的缺陷」失明**(`file:///C:` 兩邊都收,但 `Path("C:")` 是 drive-relative=worker 的 cwd)→ **必須配一個 display==read oracle**,兩者缺一不可。(丙) **oracle 響了就別調弱它**:reviewer 自己的 oracle 早四輪就報了 336 筆 driveless read,他把判準從 `is_absolute()` 窄化成 `drive and not root`、用「`\data\corpus` 只是 POSIX 路徑在 Windows 的正常樣子」解釋掉——**這個窄化正是藏住 finding #7 的原因**。信號響了想讓它安靜的那個衝動,就是 bug 本身。**另**:唯一合法的 gate 不對稱=**只有 worker 知道自己的 OS**(driveless path 在 Windows worker 上 drive-relative)→ 該規則只能活在 SoR、且必須**失敗得大聲**;方向很重要——「SoR 拒 / Console 收」= loud build error(可接受),「SoR 收 / Console 拒」= 專案永遠建不起來(致命)。deny-rule 一律配 accept pin(class 9),且 pin 要**驗得到**:`"C:" in resolved` 對 driveless `\C:\corpus` 也成立=false-green,得驗 `resolved.drive == "C:"`(drive 有沒有**錨定**),並用 revert-probe 實際證明它會紅。
17. **快取信任述詞:先枚舉快取狀態機、推導單一「何時可示人」述詞,再讓所有分支共用它**(#72/FE3 立案;#70/FE1 的 fail-closed gate 演化鏈=同一條路第一次走 → 從 class 10 亞種升格):渲染 server 快取(react-query 的 `data`)的元件,其正確性不是「**哪個請求**失敗了」的函數,而是「**這份快取此刻是否仍被一個 settled、成功、同 scope 的回答背書**」。FE3 沿狀態機逐格修,每格一輪:isError beside data(refetch fail)→ sibling 元件 Detail 同款 → load-more 的 scope-gone(Codex:409 NO_ACTIVE_BUILD 走了 keep-rows 分支)→ PROJECT_NOT_FOUND sibling(本地抓到;`_bind` 的**第二個** exit——delete_project 拒絕留 build,project 亡=builds 亡,只修 Codex 點名的拼法就會漏)→ 未 pin 的 code-less-body 分支(本地 blocker:unguarded read 丟 TypeError→非 ApiError→落 transport 分支=silent keep,13 綠著回歸)→ **in-flight 窗口**(Codex ×2:stale-while-revalidate 在 refetch 未回時照渲染舊 build 的列且可點,hung request=永遠;所有 settled-state guard 對此失明)。防線:(a) 初見「渲染快取」元件,先寫下述詞(render iff settled ∧ success ∧ scope-alive),再列(fetch 種類 × in-flight/settled × success/error × scope alive/gone/unreadable × 每個消費元件)全矩陣逐格對照——只修被點名的格,下一格就是下一輪;(b) **additive-only 詞彙表配 allowlist、不配 blocklist**(§27.2 ErrorCode 之後新增的碼不得默默繼承 keep-rows 分支;fail-closed by default;Set typed 對凍結 enum,拼錯成員=編譯錯);(c) 述詞**算一次、所有分支共用**(guard 與 message 各自 restate 同條件就會 drift);(d) 述詞的**每個 verdict** 都要有判別 pin+revert-probe——本輪四個 probe:pre-fix 述詞(兩個 scope-gone 格紅)、只修 409(PROJECT_NOT_FOUND 格紅=證明抓的是機制非拼法)、over-block(503 keep-rows 格紅=fail-closed 不得擴到清掉合法表)、unguarded read(code-less-body 格紅);unpinned verdict 的回歸是 silent。與 class 16 的關係:那裡是「讀**消費者**源碼枚舉結構決策」,這裡是「讀**快取生命週期**枚舉信任狀態」——同一個收斂動作(枚舉整個有界面),對象從 parser 換成 cache state machine。
19. **多 query 頁面:先畫導出 DAG、每節點 settled-success 閘、每條跨 query 蘊含先定 verdict 位置**(#75/FE4 立案,7 輪 Codex=每輪一格):一頁擁有 N 個 query(FE4=list/subgraph/entity-detail/relation-detail)時,正確性不是 N 個各自的 class-17 述詞,而是它們之間的**蘊含圖**:哪個 query 的哪種失敗**證明**其他 query 的快取已死(scope 證明→頁面級 verdict)、哪些只是局部事實(使用者輸入 400、scope-neutral 503→留局部+over-block 對偶 pin)、哪些**導出值**(selection、layout)依賴哪個 query 的 settled-success。動工前把這張 DAG 畫出來並逐邊決策,否則每條邊就是一輪 review。判別 pin 的構造規則:讓「被測的閘」是唯一能擋住的閘(hang 全部=其他 gate 遮蔽=非判別;probe-of-pin 必跑)。與 class 17 的關係:17 是單 query 的信任述詞,19 是述詞之間的組合結構——單格對 vs 整圖對。
18. **契約物件的投影疊層:一個 schema 有五個投影面,每面各自驗證、各自 pin**(#73/BA4 立案;5 條 Codex P2=每 push 一輪、每輪恰好一個投影面):契約物件同時活在(一)schema 文本、(二)runtime 驗證器、(三)生成的 client 型別、(四)型別系統的指派語意、(五)讓前述有意義的編譯器旗標——「契約說 X」在每一面都要獨立成立。BA4 的五輪依序=lax int 硬轉 bool(runtime≠schema;`strict=True`)→ 顯式 null 被 `| None` 當省略(runtime≠schema;present-and-null 拒絕,**掃全部四欄**非 Codex 點名的兩欄)→ 裸 required/not combinator 生成 `unknown | unknown`(codegen≠schema;改具體 closed variants)→ 結構性 union≠精確 union(excess-property check 只蓋 literal;OpenAPI 3.1 `false` property schema → codegen 出 `?: never`,變數也擋)→ `strict` 根本沒開(旗標層;**全 FE 零錯誤開啟**=每個檔本來就 strict-compatible,只是旗標從沒設)。防線:凍結新 schema 時**一趟走完整疊層**(已寫入 code-reviewer checklist 第 8 條);等價性用 jsonschema 逐案驗證非宣稱(12→20 bodies,含 over-block 方向);型別面用 `*.typetest.ts` 的 `@ts-expect-error` pin,它同時是 strict 的絆線(關掉 strict → TS2578 → tsc 紅)。**probe-of-probe**:mutation 型 revert-probe 必須先斷言 mutation 真的落進檔案(BA4 兩次假陰性:strict-removal replace 默默 no-op 回報 0;裸 bool case 被預設 overlap 的 pair-relation「碰巧」400)——「0 個錯」在未驗證 mutation 落地前什麼都不證明。與 class 5(檢查者/消費者分岔)的關係:投影疊層是它的**契約特化**——五個「消費者」各自读同一份凍結物,任何一對之間都可能分岔。

**#17 大規模再犯(class 1 的 DDL 亞種;9 輪 Codex + 1 輪本地 FAIL,把 review 額度燒到用盡)**:整個 class 1 目錄原樣轉移到 DB constraint 層 —— nullable escape(evidence_hash NULL 逃過 dedup unique index:PG 把 NULL 視為互異)、每型別 provenance 最低要求、凍結身分鍵未設 unique(entity_key/merge_key=sorted pair/(document,ordinal) 位置鍵)、FK 未帶 scope 欄位(跨 build 混版可表示,DR-006 該結構性禁止 → **composite FK (id, build_id[, project])**,父表補 unique 目標)、contract minimum 的值域(offsets ≥ 0、span/ordinal 不反轉、quote ≤ 契約凍結的 512 —— DESIGN 標 🔧 但契約已凍值時**契約優先**)、hash 公式輸入完整性(evidence_ref 是 §27.4 hash 的來源分辨成分 → 全型別必填)、每個 child FK 都要 supporting index(PG 不自動建)。**防線=事前掃表清單(寫 schema/migration 時、first push 前逐格過,見 code-reviewer checklist §DB)**:(a) 每欄:NOT NULL 對齊契約 required、enum CHECK 對齊凍結詞彙、識別子/hash 輸入/契約 minLength 禁空、契約 minimum→值域 CHECK、定義性 sanity(end≥start、位置≥0);(b) 每表:每把凍結身分鍵一個 unique(含對稱鍵用 LEAST/GREATEST 表達式索引);(c) 跨表:composite FK 對齊 build/project、父表 unique FK 目標、每個 child FK 一個 supporting index;(d) 條件型:每個 (type, 依型別必備欄位) 對逐一 CHECK;(e) 發射路徑:對每個 result_type 把凍結 source_ref 需求逐欄回溯到 NOT NULL 儲存欄。**另兩個新教訓**:宣稱全稱的測試(「every child FK…」)必須枚舉全部實例——漏掉反例的全稱測試=false-green(本地 reviewer FAIL 抓到);**額度經濟學**——同類問題一輪一條會把 Codex 額度燒乾,事前清單掃完再 first push,把 N 輪換成 0-1 輪。

**#19(C1b,7 輪 Codex + 1 輪本地 FAIL,新增 class 9/10)**:DR-006 Postgres repo 層的能力邊界硬化 —— 連線私有化(round 1–2)、writer factory 驗證(2)、讀寫型別分家(3)、raw-SQL predicate 拒絕面(4–6)、寫入 TOCTOU 原子化(7)。**上方「教訓(壓 review 輪次)」的「擠牙膏/主動掃同類」再犯**:predicate 注入面走了 4/5/6 三輪;round 4 我已主動掃 sibling(text+literal_column 一起修)卻漏了「深度」(巢狀,round 5)與「另一 sibling」(custom_op,round 6)—— 完整的首輪掃應同時涵蓋 {所有 sibling constructor} × {所有巢狀深度} × {過度阻擋的正向測試}。**本地 FAIL 一次**(round 6 blanket custom_op 過度阻擋),靠「新增正向 acceptance 測試」的要求(class 9 對偶)抓到 —— 且我與 reviewer 各自獨立抓到同一 JSONB 假陽性類。收斂良好處:round 7 TOCTOU 修法在寫 code 前先於 live PG 驗四個性質(語句被接受、building 可寫、active 拒絕、activation 阻塞),避免推理型修法再開一輪。

**#20(C1c,0 輪 Codex,首輪 +1)**:class 9/10 + checklist 第 7 條在**設計期**預先套用(Cypher 面=結構性消除 splice point 而非 guard;跨庫 TOCTOU=PG row lock 錨定,live 實測)+ reviewer 唯一 actionable nit(模板掃描鍵在 clause 關鍵字會漏未來 CALL/UNWIND 模板)在 first push 前修掉 —— C1b 7 輪 → C1c 0 輪,額度經濟學與 lesson-class 目錄的預防效果首次得到量測驗證。無新 lesson class。

**#21(C1d,4 輪 Codex、6 個 finding,全 must-fix)**:與 #20 的 0 輪對照出核心教訓——**「family pattern 轉移」≠「per-surface 掃描」**:C1c 的面與 C1b 一一對應所以 0 輪;C1d 有四個「新面」,每個 finding 都是目錄裡**已有的 class** 套在新面上被漏掉:(1) `ensure_collection` 跳過 revalidation 的 deliberate-gap 理由(「不寫 build-tagged 資料」)被推翻——collection 建立會**凍結共用 schema**,副作用超出資料本身 → class 10 精煉:「write license 過期=停掉全部副作用」,deliberate-gap 理由必須列舉**完整 effect set**;(2) raw project key 直接當 Qdrant collection 名 → class 1 值域缺口新亞種:**從 contract 字串組合 identifier**(契約只要求非空,store 有自己的字元/長度限制)→ sanitize+content-hash 派生;(3) 40-bit hash 後綴 → **class 1 值域再一亞種「派生值的值域要按 threat model 定大小」**:守 INVARIANT 的派生值,其輸入是 contract-valid=攻擊者可影響,標準是**對抗性非意外性**(local reviewer 用意外模型審過它;Codex 供出真實 forged 碰撞對,40-bit 在暴力搜尋下數秒即破)—— 審 review 時先明講這值抵抗的是意外還是對抗,再判強度;(4) (point_type × source-id key) 跨欄位對 → class 1 跨欄位亞種再現,修法=unrepresentable(單一 source_id、key 由 type 派生);(5) ensure_collection 只驗存在不驗 schema 相容 → class 5 再現(存在≠消費者需要的性質);(6) source_id 用 str 承接 row id → 型別化(uuid.UUID)讓錯值變 type error。**local reviewer 同型失誤 ×4**(它自己承認:每次都「驗了狹窄的局部性質而非消費者面向的完整保證」)——Codex 每輪冷眼進場恰好抓的就是被排除的 surface/threat model。防線已寫入 code-reviewer checklist 第 7 條新增三條(per-surface inventory / name-the-threat-model / preflight 驗消費者性質)。無新 class;class 1(值域:identifier 組合、threat-model 定值域大小、跨欄位對、row-id 型別化)、class 5(存在≠消費者需要的性質)、class 10(完整 effect set)各得新亞種。

**#22(C2,1 輪 Codex、2 個 P2,皆 must-fix;pipeline 首個任務、首個消費 C1 repo 家族的一步)**:額度經濟學持續生效——C1d 4 輪 → C2 1 輪(兩 P2 同輪一起掃),per-surface inventory「事前答完八格」把輪次壓下來;但兩個 finding 都是**目錄已有的 class 打在 inventory 尚未點名的面上**:(1) `report.documents` 把「已存在(skipped)」的 document 從交給 `clean_document` 的 tuple 裡漏掉 → **class 5 的資料 handoff 亞種**:crash 後重跑(doc 已 commit、chunk 未寫)ingest 把 doc 判 skipped 卻不轉交,clean 收不到 → build 永久留一個沒切塊的 document,而 clean 的收斂式冪等正是為這個 retry 而建。差別於原 class 5(probe/consumer 埠位)——這是**生產者→消費者的資料轉交完整性**:「消費者能處理收到的」≠「消費者收到全部該收的」,skip/no-op 分支最容易漏轉;(2) `read_csv_rows` 守了 pk(缺/空/重複)卻放行空白 `table` → **class 1 複合識別子亞種**:§27.2 citation 是 (table, pk),守一個成員≠守整組,半個 citation 不可溯源。**local reviewer 在 C1d/C2 兩個「有新面」的任務連續同型失誤**(#20 無新面故無此失誤;它承認:驗了狹窄局部性質——clean 能收斂、pk 有驗——而非完整的生產者→消費者 handoff 與 sibling 欄位完整性)。防線=checklist 第 7 條 per-surface inventory 補**複合識別子逐成員**子句 + 新增**handoff 完整性(每個分支都要把消費者要的往下轉,尤其 skip 分支)**一條。無新 class;class 5(資料 handoff 完整性)、class 1(複合識別子逐成員)各得新亞種。

**#23/#24(C3a,2 輪 Codex、各 1 個 P2,皆 must-fix;#23 因分支改名被 GitHub 關閉,#24 為同 diff 後繼)**:兩個 finding 都是 **class 1 打在「函式參數形狀」這個 inventory 沒點名的面上**(inventory 只掃過 store/DB 面):(1) `source_ref = f"{table}:{pk}"` 天真拼接可碰撞(`('a:b','c')` vs `('a','b:c')`)且 mention/evidence 都以它去重 → 碰撞=靜默掉資料 → **class 1 複合識別子再一亞種「無損編碼」**:fingerprint `_join` 早解過同一題,新消費面又漏——凡 join 出來的值被拿去 dedup,就必須 length-prefix;(2) mappings dict key(路由 document)× `StructuredMapping.table`(命名 citation)兩欄位主張同一事實,typo 對=整表誤引 → **class 1 跨欄位亞種再現(#13 species)**,修法=進門先驗 key==table(拒絕勝於默選一邊)。防線=checklist 第 7 條 per-surface inventory 增「**模組自己的 API 參數形狀也是面**」+ 無損編碼與同事實雙欄位一致性兩子句。**流程教訓三則**:(a) governance CI 要求 `task/<id>` 分支恰勾 TASKS.md 的 `<id>` 項——**切片任務先把分支名對到子項再開 PR**(C3a→`task/C3a`);(b) **GitHub 分支改名不會 retarget 已開的 PR,會直接把它關掉**(#23 陣亡,Codex 重審一次)——改名要在開 PR 前做;(c) 使用者定案:**push 後先 `gh pr checks` 修 CI 再 triage Codex**(CI 快、Codex 慢,見 [[graphrag-ci-before-codex]])。另:一次誤把 C3a commit 打在 main(push-gate fail-closed 擋下,乾淨回復)——commit 前先確認 `git branch --show-current`;一次誤自蓋 receipt(H5:要請 reviewer 蓋)——已回頭請 reviewer 重蓋。無新 class;class 1 得兩亞種(無損編碼、參數面跨欄位)。

**#25(C3b,5 輪 Codex、各 1 個 P2,皆 must-fix——C1b 之後最貴的 PR;中途額度用盡(bot 連覆兩次 limits 訊息)直到重置)**:五個 finding 全部打在**同一個新面「不受信 LLM 輸出」**上,且每一個都是目錄已有 class:(R1) envelope 欄位錯型別逃出 failure boundary(一個壞 chunk 中止整個 pass)→ class 9(boundary 列了 JSONDecodeError 卻漏了同一輸入的 sibling 失敗模式);(R2) 欄位缺席被當「沒找到東西」→ skipped 非 failed,躲過 §27.7 retry → class 1 no-op 值亞種(缺席=另一種 no-op);(R3) 葉層 scalar 被 str() 強轉 → Python repr 鑄進 canonical_name/fingerprint → class 1 葉層值域;(R4) 純空白 quote 通過 truthiness+DB `<>''` CHECK → 不可稽核的證據 → **class 1 最老亞種(#11 空白≠空)再犯**——模組對每個 name/type 都 strip-檢查了,唯獨 quote 用 truthiness;(R5) endpoint join 用精確 tuple 而 store 用 entity_key 正規化 → class 5 再現,修法=同一把身分函式(join 鍵直接用 fingerprints.entity_key)。**元教訓:R1–R4 是同一棵值樹被一輪掃一層**(envelope→陣列→葉→葉語意)——首輪就深度優先掃完整棵樹+每個葉套完整值域清單 {缺席, 錯型別, 空, 空白, 詞彙外, 不可定位},四輪縮成一輪;R5 補一條:凡從不受信輸入導出的 join/dedup 鍵,必須用 store 自己的身分函式。防線=checklist 第 7 條新增「untrusted-input value tree」bullet。**流程收穫**:owner 指示下 watcher 增 exit 30(額度用盡偵測,執行級驗證過)+ 排程在額度重置時間自動 re-poke——等待不再盲目;CI-first 全程維持(每輪 poke 前 CI 先綠)。無新 class;class 9/1/5 各得亞種(class 1 ×3:no-op 缺席、葉層強轉、空白 quote)。

**#27(C3c,0 輪 Codex 首輪 +1;新表+migration)**:預防鏈首次在「新表」任務上全程生效——DDL 清單 first push 前掃完,local reviewer 一次 FAIL 擋下兩個 blocker,Codex 零輪。兩個 blocker 都值得目錄化:(1) **class 1 條件對亞種「IFF 要寫成雙分支 disjunction」**:`(status='proposed') = (by IS NULL AND at IS NULL)` 在 decided 側只要求 NOT(兩者皆 NULL)=至少一個非 NULL——匿名決策(有時間無人)與無時決策(有人無時間)都過;正確寫法=顯式雙分支 `(proposed AND 皆 NULL) OR (非 proposed AND 皆 NOT NULL)`(sibling 先例:review_ledger 兩欄直接 NOT NULL)。凡把 IFF 綁在「合取」上,false 分支就被弱化——寫 CHECK 時逐 corner 展開(2^n 格)驗證;(2) **#17「全稱測試要枚舉全部實例」再犯但被本地攔下**:我的「both directions」測試只驗了弱 CHECK 恰好會拒的兩個 corner,對「不會匿名」的宣稱是 false-green——修正後的四 corner 測試在弱 CHECK 下確實 FAIL(可辨別性驗證過)。防線:checklist 第 6 條條件型子句補「IFF 顯式雙分支」。另:migration 已在本地套用過再改 CHECK 時,要 downgrade→upgrade 重套,否則整合測試打在舊約束上(執行級驗證又抓一次)。無新 class;class 1 得一亞種。C3 家族完結:C3a 2 輪、C3b 5 輪、C3c 0 輪——per-surface 儀式跑完整的任務收斂到 0。

**#28(C4,5 輪 Codex、8 個 finding 皆 must-fix——實體解析的語意接縫)**:全部落在「ER × 凍結身分/審核模型」:(R1a) 有 disambiguator 的同名實體被 1.0 分自動合併 → **class 1 新亞種「鍵的組成本身是斷言」**:entity_key 內嵌外部 id 就是在說「這兩個是刻意分開的」,消費者只拿鍵做相等比較、卻無視組成所斷言的語意;修法=`entity_key(type,name) != stored_key` 精確判定 disambiguator 存在(公式凍結所以判定精確),同名異鍵對僅限 ledger 明令合併;(R1b) 同 build 內 approve 無法救回先前 reject → class 5(precedence 函式與套用程式碼分岔);(R2a) **class 9 對偶再犯**:R1a 的保護把單邊 id 的同名對也擋死(id-less mention 本來就該併進 id 實體)——收緊 guard 的同一輪必須掃 positive acceptance,目錄早有此規則,語意層再犯一次;附加語意:單邊合併時 id 側強制為 canonical(身分強度>提及數);(R2b) relation ledger 在 re-mint 前跑 → class 5 時序亞種(checker 跑在 consumer 的值誕生之前)→ re-mint 內重套;(R3) rejected 關係跳過 re-mint cascade → 修法原則「**身分無條件、狀態才有條件**」;(R4a) defer 沒擋 auto-merge → class 1 詞彙×分支矩陣缺格;(R4b) 舊簽章的裁決跨 re-mint 靜默沿用 → class 6 spec 對齊(§27.3:裁決鍵在指紋上,指紋死了裁決不轉移)→ needs_review;(R5) **本 PR 最貴的教訓**:R4 自己引入 needs_review 狀態卻沒重掃所有 status 分支——approve 無法把 needs_review 救回投影。**我在 R3 總結裡親筆寫下「加狀態要重走所有分支」、下一輪就違反**:mid-PR 筆記≠防線,防線=機械性的 state × verdict 全矩陣表(已入 checklist)。無新 class;class 1 ×2、5 ×2、6、9 各得亞種+一條 meta 規則。

**#29(H8,3 輪 Codex;起因=owner 現場抓到 watcher 沒偵測到額度用盡)**:poke→watch 競態——Codex 秒回,limits 回覆落在 poke 與 watcher 啟動之間,對所有 `> START` 過濾器隱形,盲輪詢到 timeout。修法演進:bootstrap 向後看+**取代語意**(最新 quota 訊息 governs,除非更新的 +1/review/poke/非 quota bot 留言取代它;15 分鐘視窗第一版被實況數據幾分鐘內推翻——**中斷比任何視窗都長,取代才是對的規則**)→ 三輪 Codex 全是 bootstrap 自身的邊界:只抑制不呈報(同競態的另一半→bootstrap 全面化:poke 後最新 bot 事件按 poll loop 同規則分類,+1 帶 burst 優先權——approval 附帶的 all-clear 留言晚 1 秒被實測抓到)、無 poke 錨(空錨=epoch:零 poke=零 ack,一般化而非特例)、同秒 tie(created_at 一秒解析度,tie=回覆)。**工具程式碼吃到與產品程式碼相同的 class 1/8 邊界審查——理所應當**;執行級驗證自己先抓掉兩個 bug(視窗謬誤、主機沒有獨立 jq——本 repo 慣例=gh 內嵌 --jq)。probe-codex.sh:額度中斷恢復迴圈(poke→分類→睡→重試,封頂,真回應立即傳播),實戰接住 00:12 reset(2 分鐘內)。

**#31(C6a,4 輪 Codex、各 1 個 P2,皆 must-fix;首個「檢索讀取/發射面」任務——讀不受信投影(Qdrant)、發射凍結契約 §16)**:四個 finding 全是目錄已有 class 打在**新面「retrieval read/emit」**上——per-surface inventory 只掃過 write/store 面,沒點名「從投影讀值→對 SoR 覆驗→發射契約」這條讀側面。(R1)payload 的 chunk_id/entity_id 非 UUID → `uuid.UUID()` raise 中止整個 query → **class 9/1 讀側「不受信投影 value tree」**:#25 的 untrusted value tree 在 write 側(LLM 輸出),這是同題的**讀側對偶**——投影因 §19 drift/corruption 不可信,每個 payload 值到輸出前都要 validated-or-DROPPED(丟棄不 raise),丟棄計入 PARTIAL_RESULTS(§22);修法=單一 `_payload_uuid` 三處解析點共用。(R2)空 build 從沒 embed → index 的 lazy collection 從沒建 → semantic query 對缺失 collection 的 Qdrant 404 直接 propagate 成 500 → **class 5 生產者狀態亞種**:lazy-creation 生產者合法產出「無 collection」狀態,消費者必須降級為空而非 500;修法=vector repo 對 404 回 `[]`/`0`(非 404 re-raise,別把 outage 洗成空答)。(R3)壞 canonical_id/text(非字串 truthy)直接進 `RetrievalResult.id`/`text`,dataclass 不強制 annotation → `to_dict()` 發出非字串 → **整個 §16 response schema-invalid** → **class 1/9 讀側值域→契約**:身分欄位一律取 validated uuid(不取 raw payload 字串)、optional 顯示欄位 `_payload_str` 壞值 coerce None(hit 仍可引用,只丟顯示欄)——一輪掃完**所有** payload→輸出的值(不只 Codex 點名的 canonical_id)。(R4)實體被 resolution 改成非 active 後 forward-only 投影留下 stale point → `mentions_by_entity` 只按 project/build/id 濾、沒濾 status → 非 active 實體被當有效結果發射 → **class 5 SoR 覆驗不對稱**:chunk hit 已覆驗 SoR 存在(row 不在就丟),entity hit 卻沒覆驗 active status;修法=join 加 `status=='active'`,非 active → 零 mention → `_entity_result` 當 drift 丟。**元教訓:retrieval enrichment 讀不受信投影、發射凍結契約——每個跨 projection→output 的值都要 validated-against-SoR-or-dropped,且要對**所有 result_type × 所有 SoR 不變量(存在、status)**對稱掃完**;forward-only 投影意味 derived store 會留 SoR 已排除的 row。**local reviewer 通過原始 C6a 並漏掉全部四個**(同 C1d/C2 的「驗窄局部性質而非消費者面向完整保證」失誤)——讀側面是新 inventory face。防線=checklist 第 7 條新增「retrieval read/emit surface」bullet。**流程**:watcher bootstrap 向後掃在每輪重新浮出「已解決的舊 comment」(exit 10 假陽性,因 resolved thread 的 comment 早於 watch 起點)——benign,直接改用 reaction/unresolved 直接輪詢繞過。無新 class;class 9/5/1 各得讀側亞種。C6 家族開場:C6a 4 輪(新面),預期 C6b–e 隨面對齊收斂。

**#32(C6b,19 輪 Codex + 2 輪本地 FAIL——史上最貴,遠超 C1b 的 7 輪;NL→SQL guardrail,被守面=整個 SQL 文法)**:class 9「防護面完整性」的語言級最大再犯——reject surface 的 sibling 不再是 library API 而是**查詢語言的每個文法子句**,一輪一條擠了 19 輪:column-alias list(`FROM t AS a(c1,…)` 可把資料欄改名到 `__row_pk` 偽造 citation)、TABLESAMPLE(WHERE 前任意抽樣=靜默漏列)、OFFSET(靜默跳列)、非整數 LIMIT(`2::int` 繞過列數上限)、positional ORDER BY(`ORDER BY 1` 排到隱藏 citation 欄)……防線=**文法驅動的事前掃描**:守查詢語言時按文法子句清單逐格過(FROM 修飾:sampling/alias-col-list/qualification;SELECT 修飾:DISTINCT/INTO/locking;列選:WHERE/OFFSET/LIMIT 運算式形;排序:positional/named;set-op/CTE/subquery/join;函式;placeholder)× 每格的過度阻擋對偶正向測試。**對照 C1c:參數化模板結構性消除整個面=0 輪;C6b 的 NL→SQL 依 §27.6 凍結必須走 guard,面=全文法——面越大,事前掃的報酬越高**。R16(casted LIMIT)、R17(positional ORDER BY)兩個 base P2 皆 Codex 發現;但**我的首版修法各含一個 gap,由本地 reviewer 於 push 前 FAIL 攔下、修正併入同一 commit**——所以 commit 標題只見「(Codex P2)」、git/GitHub 上無獨立的本地 FAIL 痕跡;本地審查的證據只存在 session 內,retro 記載這類事實時必須寫明「證據在哪、為何 trace 上看不到」,否則事後查核者只能推翻它(本條初稿正因此被 doc-reviewer FAIL 一次):R16 我用 `not is_string` 當 gate,放行 float/scientific literal(`LIMIT 2.5` → `int()` 500)=class 5 proxy 檢查(要守 `is_int` 這個消費者真正依賴的性質,不是「非字串」);R17 我用 bare isinstance,漏 `Paren(Literal)` 括號 ordinal(PG 認 `ORDER BY (1)` 為 positional)=class 9 nesting-depth 規則在語法樹上再犯。對照 #31(本地 reviewer 漏掉全部四個),這次它在 fix-review 層真實生效。其餘亞種:R18 非字串 pk 被 `->>` 強轉 text,`isinstance(pk,str)` 跑在強轉之後=看不穿 → **檢查要放在強轉發生的層**(SQL 內 `jsonb_typeof='string'` gate,不是 driver 端 isinstance)=class 5;R14 `statement_timeout` 是 per-statement 不是 per-phase(class 4 工具語意)→ N 表 N 倍 deadline,批次成單一 statement(附帶 `AS t` 撞 SQLAlchemy `Row.t` 屬性=另一則工具語意,整合測試抓到);R15 `SET LOCAL` 自身失敗發生在 try 安裝前 → aborted txn 無 rollback 毒化連線池=class 9 failure boundary 要涵蓋 setup 自身失敗(#25 R1 同型);R13 >63-byte JSON key 被 PG 靜默截斷成錯欄名=**store 的表示極限也是值域**(class 1);R19 probe row 只在 policy cap 綁定時有意義,超取讓 PG 對「抓回但未被請求」的列評估 WHERE/cast(class 4 執行語意——與 R14 同主題:工具在邊界上的實際行為≠直覺)。R8 使用者定案 connection ownership(per-phase txns,loaned-clean)。流程:owner 於 19 輪後介入「太多輪,收斂」→ 授權邊際意見 reply-and-resolve(實際每條皆真 bug 皆修);merge 由 owner 明示同意後執行(auto-mode 分類器誤攔一次 +1 已在的 merge——依上方 merge 授權 bullet 轉述即解)。執行級驗證全程生效且多次推翻直覺(jsonb 函式 rendering、`Row.t`、Paren AST、identifier 截斷皆先在 live PG/sqlglot 驗過才修)。無新 class;class 9(文法面、failure boundary)、class 5(proxy 檢查、強轉層)、class 4(工具語意×3:statement_timeout scope、Row.t、probe-row 執行語意)、class 1(表示極限)各得亞種。C6 家族:C6a 4 輪 → C6b 19 輪(SQL 文法=史上最大面)→ C6c(Cypher 模板,C1c 已證 0 輪路徑)預期回落。

**#33(C6c,5 輪 Codex、8 個 P2 皆 must-fix;參數化 Cypher 模板=結構性消除語言文法面)**:預測部分兌現——C6b 19 輪的 guard-文法 finding 在 C6c 是 **0 個**(無 API 收 query text,唯一動態槽=validated-int hop 插值),8 個 finding 全部落在「不可信投影的 read/emit 覆驗」與「資源上限」兩層:(R1a) subgraph 只 cap entities、edges 不 cap 且 fetch 無界=**§21 cap 的防護面不對稱**(class 9:cap 也是 guard,每個 result 來源都要枚舉);(R1b) path 只試第一對 endpoints(多 disambiguator 名字=多 seed 要窮舉)——修法預先套用 R14:全 pair 共用單一 deadline;(R2) SoR 驗證在 pair 迴圈**外**→ stale 候選終止整個搜尋(checker 位置錯:驗證必須在迴圈內、失敗 continue);(R3a) neighbors 只驗**目標**實體、不驗**走過的邊**——path 模板驗每條邊而 neighbors 不驗=read/emit 對稱缺口(#31 R4 的 SoR 覆驗不對稱,再深一層:值→status→**邊存在→可達性**)→ 修法=SoR-active 節點+關係上重算 BFS 可達性,投影距離不信;(R3b) **同 PR 內的同 class 再犯**:R1b 才修過 path 的 per-pair deadline,neighbors 的 per-seed 迴圈同型漏掃(seed_count × timeout)——**修 finding 時要先問「同 class 還打在哪些 sibling 面/參數」**;(R4a) R2 的深化:stale 的「最短」路遮蔽同 pair 較長的 active 路 → shortestPath 模板加 rel/node-local 排除謂詞(live 驗證 pushdown:排除 stale 邊後回較長 active 路)+ 同 pair 重試迴圈(排除集單調增→終止);(R4b) **round-3 本地 reviewer 的 optional nit 被 Codex 升級成 P2**(TRUNCATED 在 drift+clip 並發時被吞;fetch 時判斷)——此為 session-only 事實(本地審查的 waive 不留 commit/comment 痕跡;trace 上唯一佐證=修正落在 round-3 修復 commit 90ed539 的下一個 commit 80638c5)——教訓:指向正確性/精度的 nit 一律立即修,「no change required」不是不修的理由,是 reviewer 客氣;(R5) 非字串 entity 在 `.strip()` 先於 isinstance → 500——hops 才修過同型,**同函式的 sibling 參數沒掃**(class 9 最小再現)。**元教訓兩則**:(a) 「stale 投影」的完整覆驗面={值 corrupt、節點 status、邊存在、可達性、最短性}——#31 的 read/emit bullet 只列到 status,traversal 級(後三項)是 C6c 補上的新深度,已寫入 checklist;(b) 修 finding 的同時掃 same-class siblings(跨模板、跨參數)——R3b/R5 各多花一輪,都是掃一下就能省的。無新 class;class 9(cap 對稱、sibling 參數)、class 5(checker 位置、覆驗深度)、class 4(per-phase deadline ×2)各得亞種。C6 家族:C6a 4 → C6b 19 → C6c 5(文法面消除生效;殘餘=投影不可信的深度)。

**#34(C7,1 輪 Codex、1 個 P2;本地 reviewer 2 輪 FAIL 先攔——Leiden 社群摘要)**:核心 class=「**錨定冪等/可重現性的派生值必須是集合的純函數,不能依 fetch 順序**」(class 4 工具語意:PG fetch order 非 rerun-stable × class 1 值域):(本地 FAIL 1)leidenalg 對頂點編號順序敏感——reviewer **實證** 20 次重標記中 4 次同圖同 seed 分割不同——而頂點編號來自無序 fetch → rerun 重算出不同 member set、skip 失效、重複報告;修=排序後編號。(本地 FAIL 2)我的「強化」測試仍 false-green:雙三角形圖是**排列不變**的(reviewer 枚舉 720 種排列=同一分割;revert sort 測試照過)——**決定性/排序類主張的測試 fixture 本身要有鑑別力**(模稜圖:環+弦),且**提交前先跑 revert-probe**(sort 註解掉→測試必須 FAIL;本次在 session 內執行級證明後才過)。(Codex R1)prompt 的 relation 樣本未排序就切片——同 class 的 sibling(頂點編號才修過、同函式旁的切片沒掃),**sibling-sweep 規則寫進 checklist 的同一天就再犯**:規則要在「實作/修復當下」執行,不是等 review 提醒;修=穩定 tuple 排序後切片,測試=prompt 在 relations 反序輸入下 byte-identical。兩次本地 FAIL 為 session-only 事實(FAIL 與修正皆折入 push 前的單一初始 commit 7462d36,trace 上無法區分修前/修後版本;20 次重標記與 720 種排列的實證數據僅存在本次 session——Codex R1 的修正則是可見的 commit ec8095f)。結構面收穫:圖來源=PG SoR(完全不碰投影,C6c 的 staleness 整類結構性迴避);LLM value tree/失敗邊界/§27.7 stable ref 首輪即過(C3b 模式移植成功)。無新 class;class 4/1(fetch-order 純函數化 ×2)、#17 全稱測試(fixture 鑑別力+revert-probe 前置)各得亞種。輪次:C6a 4 → C6b 19 → C6c 5 → C7 1——本地執行級審查(20 重標記/720 排列枚舉)攔住了 Codex 會抓的最大項。

**#35(C6d,2 輪 Codex、各 1 個 P2;global mode——SoR 直讀,無投影層)**:兩個 finding 都在修正我的「SoR 可信」推理邊界:(R1) `member_entity_ids` 是**無 FK 的裸 uuid[]**(僅非空 CHECK),發射 entity refs 前未對 build-scoped entities 接地——手寫/畸形列可鑄出 ungrounded 或跨 build 引用 → **信任邊界是逐欄位的,不是逐 store 的**:SoR 內無約束的參照欄(裸 uuid[]/text ref)與投影值同等不可信,發射前必須接地(class 1「映射他表的 id 要驗證」#21 亞種);伴隨教訓:**整合 fixture 用隨機 uuid 當 member=fixture 沒滿足受驗性質**(Codex 由此看穿)——檢查驗什麼,fixture 就要供什麼;(R2) 接地查詢單一 IN 對全部 claims=每 id 一個 bind,撞 PG 32767 bind 上限 → 大 build 整個 global 掛掉 → 分批(1000/批,決定性排序,seam 處 union)——**協定/表示極限也是值域**(#32 的 63-byte identifier 教訓在 bind 協定上的 sibling)。測試紀律的落地:R1 修復即用 predicate-honest fake(fake 只回答 IN 實際點名的 id,union-at-seams 斷言不可能 false-green);上一輪本地 nit(id tiebreak 覆蓋)按新規則立即修並跑 revert-probe(session-only:修正折入 push 前的單一初始 commit 6cb2f4d,trace 上無法區分修前/修後版本)——#34 的兩條新紀律首次全程執行,C6d 收在 2 輪。無新 class;class 1(逐欄位信任邊界、fixture 滿足受驗性質、bind 協定上限——表示極限即值域,與 #32 的 63-byte identifier 同 class)各得亞種。輪次:C6a 4 → C6b 19 → C6c 5 → C7 1 → C6d 2。

**#36(C6e,1 輪 Codex、1 個 P2;hybrid router+RRF 融合+routing trace——C6 家族收官)**:finding=**doc/impl 內部不一致**(LOOP 準則的 must-fix 類):docstring 承諾「任何 out-of-vocabulary selector 輸出 → fallback 全模式」,實作卻只在有效交集為**空**時 fallback——混合答案(`["semantic","teleport"]`)保留有效半邊、靜默縮窄檢索。修=**部分有效=整體失敗**(untrusted 輸入的 value tree 新格:MIXED membership):答案中任一模式不在 offered available 集(幻覺或 real-but-gated 皆屬不遵約)→ 全答案不信任 → 全 available 模式執行、extras 記入 routing reason;gate 絕對(distrust 回傳=available 集,結構上排除 gated)。教訓:對 untrusted 選擇型輸出,「交集後繼續」是靜默縮窄——**寬度優先的 fallback 必須以整個答案為單位**,且 doc 承諾的失敗規則要逐字對照實作(這正是 doc/impl 分岔被 Codex 一眼抓到的原因)。結構面:C6e 首輪即過的部分——selector 值樹 7 個壞形狀、gating 先於選擇、mode 崩潰降級(§22 逐模式邊界)、RRF 決定性、debug/trace 真實性、fixture 的 §27.2 合規(chunk ref 帶 offsets——schema 驗證在單元測試裡自己抓到)——#31-#35 的教訓全部前置消化。無新 class;class 9(untrusted 值樹的 MIXED-membership 格)得亞種。輪次:C6a 4 → C6b 19 → C6c 5 → C7 1 → C6d 2 → C6e 1。C6 家族完結:檢索五模式+summarize 全數落地,per-surface 儀式的收斂曲線成立。

**#37(C8,9 輪 Codex、12 個 P2、2 個本地 blocker;per-project MCP server——§9 facade)**:收斂曲線在**新 surface class** 上回彈(檢索模式的教訓不自動涵蓋 facade/wiring/lifecycle 面)。主敘事=**deadline saga,5 輪才收斂**(R1 latency reconcile→R3 hybrid wall-clock→R4 standalone sweep→R5 binding 納入→R7 剩餘預算貫穿):同一個邏輯需求「§21 cap 約束整個 MCP 呼叫」被 Codex 一輪一格地逼出來。**新 class #11:請求級不變量要一次掃完整個生命週期**——當需求是「對一個 REQUEST 的不變量」(預算/配額/追蹤),設計時就列舉請求生命週期的每一段(binding、selector、per-mode、discovery、assembly)逐段對照不變量,而不是等審查者逐格開票;「per-phase 有 cap」不等於「整體有 cap」(各段各自滿額+重新起算=總量爆表)。其他 findings:R1A 各 factory 獨立查 active=綁定級 TOCTOU(class 10 亞種;修=單次 lookup);R2 wheel 不含 contracts=部署形態分岔(class 7 執行級驗證:實際 build wheel 驗證);R6 成功 payload 漏 build_id(staleness 可偵測性;class 2 亞種:同一工具的 success/error 形狀不對稱=同面多路徑的入口一致性);R8 store 例外家族→typed STORE_UNAVAILABLE(class 9;「NOT Exception」守住 bug 照樣 loud);R9 **便利工廠收裸 uuid=圍籬→紀律的降級**(class 9 亞種:對受圍籬面新增旁路入口,旁路必須攜帶等強證明;修=ActiveBinding capability proof,InitVar 封 dataclasses.replace 偽造——本地 reviewer 驗證過的硬化)。**Process failure(owner 抓到,非流程抓到)**:integration CI 連紅 5 push——call-through 測試進真 lifespan 要 OPENAI_API_KEY,本機 .env 有、CI 沒有=**本機綠≠CI 綠的環境分岔**;而 watcher 印了 `integration fail` 卻只等「非 pending」就去看 Codex(CI-first 規則被腳本邏輯繞過)。修=factory fake(測 seam 不測 vendor client)+watcher **fail-stop on CI red**(紅燈即 exit 1,絕不進 Codex 段)(watcher 行為屬 session 記錄,attribution 以 commit `82b7983` 訊息為 trace 錨點)。本地 reviewer 亦攔 2 blocker(semantic top_k 門檻缺口=sibling 不對稱;round-5 Fix B 零測試)。輪次:C6a 4 → C6b 19 → C6c 5 → C7 1 → C6d 2 → C6e 1 → **C8 9**。教訓總結:新 surface class 開工前,把催化的不是「同 class 檢查」而是「這個 surface 的不變量是什麼、它的 scope 是哪個生命週期」先寫下來。

**#38(C9,8 輪 Codex——1 P1+9 P2、3 個本地 blocker;build lifecycle:activate/rollback/diff/prune)**:又一個新 surface class(狀態機×並發 admin 面),曲線再度回彈。主敘事=**class 10 在同一 PR 連開五格**:R4 prune snapshot TOCTOU(修=FOR UPDATE 認領,單一 tx 涵蓋投影+PG 刪除)→R5 crash 窗口(preflight 是 bind-time 知識;修=鎖內重驗 drift)→R6 rollback 目標選擇在序列化之外(修=pg_advisory_xact_lock 專案級生命週期鎖,選擇+晉升同鎖同 tx)→R7 非終態 victim(building=live pipeline;修=_PRUNABLE 終態白名單,兩處檢查同步)→R8 排序 proxy 偏差+時鐘混源(修=archive 時 backfill 位移時刻+**單一時鐘源**:排序性時間戳全走 PG now(),app clock 與 DB clock 混用會被 skew 重排歷史)。**class 10 深化:狀態機×並發面要做「操作×狀態×交錯」矩陣掃描**——單點修一格,Codex 就逐輪開下一格;設計時自掃矩陣(每個操作對每個狀態的裁決+每對操作的交錯)能壓掉大半輪次。其他:R1 **P1**=REL 邊謂詞不 mirror 投影 write shape(class 5 檢查者/消費者分岐:自寫 Cypher 而非對齊正典 _RELATION_COUNT;acceptance 缺「有投影關係」cell 讓零計數謂詞溜過→**guard 的 acceptance 要覆蓋每個 population cell**)。R2=diff 跨專案混算(build-only 表需成員驗證)+merge_candidates 漏列(class 9 枚舉;修時全面盤點 build-scoped 表並記錄排除理由)。R3=NULL activated_at 謂詞(class 1 亞種:值域 nullability cell)。本地 reviewer 3 blocker:**drift 母體=投影母體而非原始列數**(class 9 over-block dual 的最佳例——merged/rejected 留在 SoR 是設計;acceptance=真投影 resolved build 必過)、round-4 Fix 零測試、docstring 殘留被棄方案(換方案要回頭掃敘述)。輪次:C6a 4 → C6b 19 → C6c 5 → C7 1 → C6d 2 → C6e 1 → C8 9 → **C9 8**。與 #37 合看:新 surface class 首 PR 輪次高是結構性的——開工前寫下「surface 的不變量+生命週期+狀態機矩陣」是唯一已知的壓縮手段。

**#39(C10,17 輪 Codex——3 P1+22 P2、5 個本地 blocker 觸發點;eval harness——史上最長 PR)**:裁判面(scoring gates production)的每個啟發式格都是 gate 完整性問題,收斂失敗的解剖:(a)**關係匹配語意鏈**(R2 方向→R4 連接段→R7 端點恆等;R2 同輪另有不相干的 answer_regex casefold 修正)與(b)**targeted-lookup 鏈**(R11 建立→R14 雙庫→R15 精確探針→R17 降級圍堵)——每修一格就開下一格,反應式補格是裁判面的反模式。**教訓(class 11 深化之二):評分/裁判面先寫完整語意規格——身分模型(什麼算同一個端點/邊)、雙庫一致性(SoR∧投影)、降級行為(§22)、可比性(suite+模型指紋)——否則語意是被審查者逐格逼出來的**。其他要點:R16A builds.eval 專欄早在 migration 0001(class 6:寫碼前先讀 schema——我發明了 metrics['eval']);gate 決策表被逼到 fail-closed 全格(unscored/bootstrap/failed>0/指紋缺失全擋,唯一 vacuous=無 active 時的回歸比較);answer_similarity 誠實不發(golden 無參考答案欄);本地 reviewer 5 blocker 多為判別性測試缺失(revert-probe 紀律持續發威)。**Process 事故(owner 抓到 #2,trace 錨=PR#39 comment 18:08Z)**:Codex 核可有**兩種格式**——push 觸發的自動審=+1 reaction;poke 觸發的審=「Didn't find any major issues」comment(無 reaction)——watcher 只盯 reaction/threads,對乾淨 comment 全盲,還在 PASS 後繼續 poke 觸發新輪。修=三態 watcher(+1/新 threads/乾淨 comment 指名 head)+**乾淨 review 後絕不再 poke**。**Owner 收斂指令(常設)**:輪次過多時逐條判「必修 vs reply-and-resolve」,劃清 task 邊界,不無限上綱;must-fix=gate 完整性/資料正確性,邊角遞進→resolve+記 follow-up。輪次:4→19→5→1→2→1→9→8→**17**。

**#40(C11 observability §18/§19,8 輪 Codex——11 P2、1 本地 blocker + 2 次本地 reviewer 攔假綠、1 邊界 resolve;Track 1 最終任務)**:兩個新 surface(recorder 寫路徑、health 讀路徑)都用「語意規格先行」開場(C10 教訓落地——module docstring 開頭即完整語意),但 Codex 仍逐輪逼出**兩條完整性鏈**:(a)**pending_review 佇列**——round4 補 §6 待審池(ontology proposals)→ round8 補 §17 全狀態(needs_review 實體/關係 + deferred merge;第四項 needs_review_relations 首版無判別測試——status/review_status footgun 會 false-green——**同樣被本地 reviewer FAIL 攔下**,補判別 cell + revert-probe),**教訓:「X 佇列」型指標的分母是 spec 定義的整個狀態機,不是你先想到的那張表——開工前把 §17 待審狀態全列出,否則同一類 false-dark 會分兩輪逼出**;(b)**recorder 寫入完整性**——JobStatus 詞彙(**唯一的本地 blocker**——succeeded 違反凍結 enum,成功路徑必炸卻被「每個測試都注入 failure」遮蔽,Codex 首審前即由本地 reviewer 攔下)→ dedup 撞唯一索引 → verbosity 讀 settings(廣告了卻沒接線的 dead config)→ loaned-clean 圍籬(無條件 rollback 會毀呼叫方未提交寫入——改用 C6b sqlreader refuse-loud idiom)→ build 綁定 FOR SHARE(無 FK,cross-project/pruned build_id 誤掛——class 10 TOCTOU 直接套用)→ failed 支配去重(§27.7 retry 讀持久化 failed 列,first-wins 會讓 skipped 蓋掉 failed 而漏重跑)。其他:health status_light 抽純函式 + 16 格決策表全釘;drift 重用 lifecycle 同一 checker(class 5 不分岔);drift 探針對 Build-failed 短路 + 對 store 故障降級成 STORE_UNAVAILABLE warning(「不可達=未測量,非 drift」,同 eval 燈的只報實測事實原則);**NULLS LAST(Codex round 6 找到的 bug):coalesce(now()) 讓未啟動 build 排最新、遮蔽較新回歸候選;我的修正卻踩 class 8 false-green——假 conn 繞過 SQL ORDER BY,revert 不破任何測試,被本地 reviewer FAIL 攔下 → 補真庫判別 + 親自 revert-probe;同類掃到 list_builds 一併修**。收斂紀律奏效:round3(orchestrator 是 BA1/BA2 交付物)以 task 邊界 reply-and-resolve 不動碼;其餘全是真資料正確性 bug,無边角過度上綱。**里程碑:Track 1 核心引擎全數完結**(C1–C11);下一站 Track 2 BA0(API skeleton)。輪次:4→19→5→1→2→1→9→8→17→**8**。

**#41(BA0 API skeleton,4 輪 Codex——6 P2、0 本地 blocker;Track 2 起點)**:第一個 FastAPI 任務,Codex 全 6 條開在**同一個 surface——error-envelope 邊界**,是一條完整的「例外處理器完整性」鏈:(1)HTTPException 漏 Starlette 的 `{detail}`(內建 handler 比我的 Exception handler 更 specific,繞過它)→(2)validation error 的 `exc.errors()` ctx 帶原始 ValueError(非 JSON)→ JSONResponse 序列化炸成 500 →(3)ApiError.details 帶 UUID/datetime 同樣炸 →(4)5xx 回顯 `exc.detail` 洩漏後端故障資訊 →(5)丟棄 `exc.headers`(405 Allow / 未來 WWW-Authenticate/Retry-After 協定提示)→(6)保留 HTTP status 卻發 coarse code(503 配 INTERNAL,但契約保留 503 給 STORE_UNAVAILABLE——client 依 error.code 分派會看到與 status 矛盾的類別)。**教訓(新 lesson class 候選:框架邊界的預設行為完整性)**:當你在框架的錯誤/回應路徑上「宣稱一個不變量」(這裡=「沒有預設 shape 會到 client」),必須列舉框架能產生的**所有**出口——多型 handler 優先序(specific > generic)、內建序列化的隱藏失敗(非 JSON 值)、框架附加的 side-channel(headers)、以及你自己的映射與更底層契約的一致性——否則同一宣稱會被逐一出口逼破。收斂手法:第 2 輪起把序列化**集中到 `_error_response` 一處**(jsonable_encoder 整個 body),一次覆蓋所有 handler,而非逐 handler 補;狀態→碼用單一來源 `_HTTP_STATUS` 反導（唯一才映射,歧義 fallback coarse)。**正面**:契約即發布 schema(app.openapi 直吐凍結 yaml,DR-002 結構性防漂移);ErrorCode 與契約 enum、status map 與契約 prose 都做 lockstep 測試;auth placeholder 不 reject（凍結 enum 無 auth code，避免 DR-002 破例）。0 本地 blocker——skeleton 邏輯簡單,但 error 出口是我開工時沒完整列舉的（本可靠「框架出口清單」在實作前避免這 4 輪）。輪次:…→17→8→**4**。

**#42(BA1a projects/sources registry,3 輪 Codex——4 P2、0 本地 blocker;BA1 首切片)**:先做了兩個**任務邊界**判斷——(a)BA1「+trigger ingest/build」與 DESIGN line175「BA2 arq/jobs/SSE」衝突,用 AskUserQuestion 讓 owner 定案「triggers→BA2」;(b)BA1 本身太大,切成 BA1a(schema+core CRUD)/BA1b(routers+idempotency)。**核心教訓——引入控制面父表(projects)覆蓋既有裸-text-keyed 資料(builds/review_ledger/… 無 FK)時,Codex 會把「這個父/子關係的每個破口」逐輪掃完**,全是同一資料完整性類:R1 delete 只刪 projects 列→builds 及 build-scoped 資料被孤兒化+同名重建會撈到 stale active build(active_build_id 由 builds 解析)→加 ProjectHasBuildsError refuse;R2 同理 review_ledger(DR-003)/ontology_proposals(DR-007)/pipeline_runs 也 carry-forward→delete 時 purge(builds 仍 refuse,因多店清理=C9/BA8);R3 migration 建空 projects 表→既有 project 從 registry 消失但 builds 仍解析→**backfill**(從四張非-build-scoped project-keyed 表 UNION 回填)+ delete guard 的 **count-then-delete TOCTOU**。**收斂判斷(對齊使用者「劃邊界」指示)**:in-scope 的破口(delete guard/purge/backfill)當輪修+revert-probe;結構性根因=FK `builds.project→projects.name`(RESTRICT)一次關掉 orphan+TOCTOU,但 retrofit 會炸掉所有「直接插 builds 無 projects row」的既有 build 測試,且需 build-creation 變 registry-aware=**BA2** 的事→reply-resolve 為可查核 deferral,存 graphrag-ba2-registry-fk(已刪) 追蹤(backfill 已備妥 FK 前置)。**正面**:自審抓到自己的 false-green(offline DDL `"ON DELETE CASCADE" in ddl` 恆真,因既有 C1a 表都有→改斷言整條 `REFERENCES projects (name) ON DELETE CASCADE`);非-build-scoped 控制面用 plain async funcs(非 build-scoped repo,後者會 reject 這些表);PATCH sentinel 抽純函式單測。**新 lesson class 候選:引入父表覆蓋既有無-FK 資料時,先列「delete 孤兒/carry-forward + upgrade backfill + 寫入 TOCTOU/FK」三面完整性清單**。輪次:…→8→4→**3**。

**#43(BA1b projects/sources routers + §27 idempotency + opaque cursor,0 輪 Codex 首推 +1;本地 reviewer 2 輪:1 FAIL(2 blocker)→ PASS)**:BA1 第二切片,本地執行級審查在 push 前攔下兩個 blocker、Codex 零輪。**核心技術教訓(class 8 表示誤差最佳例):我加的結構性 CHECK 揪出一個所有行為測試都放行的潛伏 bug**——reserve-first 冪等的預留列以 `response=None` 寫進 JSONB 欄,SQLAlchemy 預設把 Python None 編成 JSONB `'null'` **字面值(一個 JSON 值)** 而非 SQL NULL,於是 `response IS NULL` 為 false、`status IS NULL` 為 true;我為「reserved(雙 NULL)xor committed(雙填)」加的 `CHECK ((status IS NULL)=(response IS NULL))` 立刻在每次 reserve 上爆(3 個原本綠的冪等測試同時紅)。這既證明 CHECK 是 load-bearing(結構約束抓到行為測試漏的表示層錯),也暴露真 bug:未加 CHECK 前,並發的 replay-after-reserve 會讀到 response→None/status→None 而 `int(None)`→500。修法=`postgresql.JSONB(none_as_null=True)` 讓 None 綁成 SQL NULL(真 response body 恆為物件,絕不存裸 JSON null)。**教訓:凡 nullable JSONB/JSON 欄用 Python None 表「缺席」,None 預設是 JSON null 不是 SQL NULL——欄型要 `none_as_null=True`(或 insert 用 `sa.null()`);而「用結構約束把不變量寫死」會回頭替你抓到 ORM 表示層的沉默錯譯**(#27 的 IFF 雙分支 CHECK 同源:CHECK 是設計期抓 bug 的槓桿,不只運行期防線)。**Blocker 2(class 6 spec 對齊 / class 1 值域 nullability cell):PATCH `{"config": null}` → 500**——DTO `config: dict|None` 放行 null,`model_dump(exclude_unset=True)` 產 `{"config": None}`,registry `SET config=NULL` 打在 NOT NULL 欄 → IntegrityError(create_project 有 catch、update_project 沒有)→ 通用 handler → 500,把 client 錯誤誤標成 server fault。根因=**凍結契約的 per-field nullability 要逐欄對齊**:契約裡 config 是 `type: object`(非 nullable)、display_name/description 是 `["string","null"]`(nullable-clearing)——我讓三者共用同一 `X|None` 形狀,抹掉了 config 與其他兩欄的差異。修法=共用 `NonNullConfig = Annotated[dict|None, AfterValidator(reject null)]` 套在 create+update(omitted 走 default 不觸發 validator→保持 unchanged;explicit null→400);**registry 自己的簽章早已寫明 `config: dict | _UNSET`(從不 None)——DTO 沒對齊到 registry 的型別合約**。兩個 blocker 都由本地 reviewer round-1 FAIL 抓到(config-null 是 client-triggerable 500;poisoned-key 不變量無測試——reserve-first 全設計所繫的「失敗寫入不留毒鍵」竟無測試守著,補整合測試:keyed POST 失敗→斷言 0 列殘留→同鍵重試 re-run 而非 replay NULL-status 列)。**流程/harness 教訓(可重用):async TestClient × live PG 的整合 harness**——`httpx.AsyncClient + ASGITransport` + override `db_conn` 為單一連線、每請求一個 `begin_nested()` savepoint、外層 txn teardown rollback(單一 event loop,零殘留);BA2+ 直接沿用。**覆蓋率門檻的結構張力**:fast-suite 的 85% coverage gate 不含 integration,而 BA1b 的 api 層本質是 integration-tested,純加 integration-only 碼會把總覆蓋壓破 85→補**component 測試(stub registry、真 app,測 router 編排/狀態碼/envelope/錯誤映射,快)**+ 純邏輯 unit 測試把 gate 拉回。**flagged 未決(劃清邊界、不上綱)**:ProjectExists/HasBuilds → VALIDATION_ERROR(400) 仍是佔位——凍結 ErrorCode enum 無合適的 409/conflict 碼,加碼是 DR-002 契約變更=BA1b 範圍外;隔離在 registry_errors.py 單點、已 flagged 待 owner 定案(reviewer 確認不阻擋)。無新 class;class 8(JSONB null 表示誤差)、class 6/1(per-field nullability 對齊契約與 registry 型別)、#17 全稱測試(poisoned-key 不變量補測)各得亞種。輪次:…→8→4→3→**0**(BA1a 的父/子完整性清單移轉+本地執行級審查把 Codex 壓到零輪)。

**#44(BA2a jobs table + core job repo + delete-project active-jobs guard,1 輪 Codex——2 P2 皆 must-fix;0 本地 blocker;BA2 首切片;code-architect 出 4-slice 藍圖後開工)**:BA2 是全案最大整合任務(把 Track 1 的 C1–C11 串成可跑的 build pipeline 並由 HTTP 驅動——全案無人建過端到端 orchestrator),依藍圖切成 BA2a(本:jobs 表+repo+delete guard)/BA2b(orchestrator+builds FK+registry-aware build 建立)/BA2c(arq worker)/BA2d(triggers+job endpoints+SSE)**(此為當時 4-slice 藍圖;#45 前已重切為 5-slice——BA2b=builds FK+sweep、BA2c=orchestrator+registry-aware build、BA2d=arq worker、BA2e=triggers+endpoints+SSE;一律以 TASKS.md 為準)**。兩個 Codex P2 都是**目錄已有 class 打在「切片邊界畫錯」的面上**:(1) **class 2 完整性再犯(新增領域錯誤要追到所有消費者)**:我在 `delete_project`(一個 BA1b router 早就在呼叫的函式)新增了 `ProjectHasActiveJobsError` guard,卻沒接 `translate_registry_error`+router 的 except → DELETE /projects 撞到新 guard 直接掉進 uncaught handler 變 **client-triggerable 500**(同屬 client-triggerable 500 症狀,但根因不同 class——BA1b config-null 是 class 6/1,此處是 class 2 完整性)。**教訓:對「共用函式」新增領域錯誤時,該函式的每個既有入口(尤其 HTTP)的翻譯/處理都要一起補——我對 local reviewer 說「本切片無 HTTP」劃錯了邊界,因為 delete_project 本身就被 HTTP 消費;新錯誤的完整性面=該函式的所有呼叫點,不是我這次改的檔案集合**。(2) **class 10 TOCTOU(count-then-delete)+ class 8 假綠測試(鎖測試的窗口遮蔽)**:delete_project 先 count active jobs=0 再 DELETE,中間窗口有並發 create_job commit 會被 CASCADE 靜默刪掉(jobs.project 是 CASCADE,BA2b 的 builds-FK-RESTRICT 關不掉這條);修法=delete_project 首句 `SELECT … FOR UPDATE` 鎖 projects 列,並發 create_job/add_source 的 FK 插入取 FOR KEY SHARE 會被擋到 delete txn 結束(兩序皆序列化,無殘窗)。**但我第一版鎖測試假綠**:在 delete_project **回傳後**才用第二連線 probe——此時 DELETE 自身的列鎖已在,無論修法在不在都 55P03,revert-probe 拿掉 `.with_for_update()` 測試照過(class 8 假綠當場被 revert-probe 抓到)。**教訓:鎖/TOCTOU 測試必須 probe 在「修法所保護的那個窗口內」,不是操作完成後——後續的附帶鎖(此處=DELETE 自身)會遮蔽 bug**;修正版用 monkeypatch 把 `count_active_jobs` 暫停在「取鎖後、DELETE 前」,在該窗口 probe `FOR KEY SHARE NOWAIT`→55P03,revert-probe 確認可辨別(拿掉鎖→窗口內列仍空→probe 成功→測試失敗)。**流程正面**:jobs.error 首用即帶 `none_as_null=True`(BA1b 教訓前置生效);delete guard 用 FOR UPDATE 序列化(C1b/C9 的 row-lock TOCTOU 修法移植);Codex 兩條當輪一起收(額度經濟學),reply+resolve+revert-probe 後 +1。無新 class;class 2(新錯誤→所有消費者翻譯完整性)、class 10(count-then-delete→取鎖前置)、class 8(鎖測試窗口隔離)各得亞種。輪次:…→4→3→0→**1**。

**#45(BA2b builds→projects FK RESTRICT + fixture sweep,1 輪 Codex——1 P1 must-fix;1 本地 nit;BA2 第二切片)**:兌現 BA1a deferred 的 FK(存 graphrag-ba2-registry-fk(已刪)),把 orphan+TOCTOU 面結構性關掉。**主教訓(新:收緊型 migration 要先和解既有列,且 fresh-DB CI 遮蔽此失敗)**——Codex P1:在已到 0009 的庫上,`builds.project` 一直到本 migration 才受約束,故 0007 backfill 之後、0010 FK 之前插入的 build 可能引用無 `projects` 列的 project → `ADD CONSTRAINT` 撞 FK 違反、擋住升級。**CI 打在全新空庫,永遠不會有這種孤兒列,所以檢查看不到**(class-5 亞種:檢查者的 DB 狀態=空庫 ≠ 消費者的既有 dev/prod 庫;M→N 兩個 migration 之間是無約束窗)。修法=0010 在 `create_foreign_key` 前**重跑** builds→projects backfill(`INSERT … WHERE project<>'' ON CONFLICT DO NOTHING`,同一 alembic 交易內原子);空-project build 刻意排除(projects.name 有非空 CHECK、無碼產生、FK 正確地讓真正無效資料 fail-loud)。測試=downgrade 到 0009→插孤兒 build→upgrade 到 head 必須成功且已註冊該 project(revert-probe:拿掉 backfill 則失敗;sync 測試因 alembic env.py 自跑 asyncio.run 不能巢狀在 async loop,DB 操作走 asyncio.run,finally 無條件還原 head)。**本地 nit(#17 再犯但被本地審查咬住)**:`builds.project` FK 無 supporting index——`one_active_build` 是 partial(WHERE status='active'),擋不住 projects DELETE 的 RESTRICT RI-check(任何 status);#17 的「每個 child FK 一個 supporting index」早在 checklist §DB,**本地 reviewer 在 first push 前抓到**(review 層預防生效),但我實作當下沒主動套 checklist——**教訓重申(#34 母題):DB 清單要在實作/首推當下逐格過,不是等 review 提醒**;修法=加 `builds_by_project` index(表定義+migration+offline-DDL 三處)。**流程正面**:本地 nit 修完 re-stamp、Codex P1 修完 reply+resolve+revert-probe,額度經濟學維持(單輪收);sweep(22 檔補 `ensure_project` seeding)委派 general-purpose subagent、以 check-full 為正確性 oracle,零 FK 失敗;唯一副作用(committing 測試在 dev DB 留 seeded projects 列)判 benign(CI 每次乾淨庫、ensure_project 冪等、其他測試皆 self-relative,pagination 測試已改 self-relative)——本地 reviewer 認同。無新 numbered class;新增「收緊型 migration 和解既有列 + fresh-DB CI 遮蔽」lesson(class-5 亞種,已寫入 code-reviewer checklist §6 Migration bullet + 父表三面 bullet 的 UPGRADE/WRITE-RACE 面)、#17 supporting-index 再犯(review 層攔下)。輪次:…→4→3→0→1→**1**。

**#46(BA2c-1 registry-aware build 建立 + pipeline orchestrator 控制流,5 輪 Codex、7 finding(2 P1+5 P2;6 修、1 P2 reply-and-resolve deferral),findings 每輪 1/2/2/1/1;0 本地 blocker 但本地 reviewer 全程 re-stamp + 抓 2 件 session-only 假綠/漏標;BA2 第三切片、全案首個端到端 build 執行器)**:又一個「狀態機×並發」新 surface(build **執行**狀態機,對照 #38 C9 的 build **生命週期**狀態機),曲線回彈到 5 輪——**核心元教訓:多-commit 的 orchestrator 是 TOCTOU/crash-window 工廠**。run_build 把一次 build 攤成約 9 個獨立交易(job 載入、resolve/create、mark running、6 stage、cancel recheck、build 終態、record_run、job 終態),**每個 commit 邊界都是一個 crash 或 race 窗**,Codex 逐輪逼出(每輪 1–2 格,R 編號依 finding 非輪次):(R1 **P1**)create build 與 attach 到 job 兩段 commit=class 10——crash 在中間留孤兒 building build(job 仍 build_id=NULL、RESTRICT 擋不掉隨專案刪)→摺進單一 txn;(R2 P2)兩 worker 同派一 job 都讀 build_id=NULL 都建 build=class 10——job 列 FOR UPDATE 鎖 + 鎖內重讀 build_id,序列化 build **建立**;(R3 P2)cancel 只在每個 stage **之前**檢查,最後一個 stage 期間的 cancel 無後續 checkpoint→丟失、build 誤標 ready=class 9 防護面完整性(cancel checkpoint 漏了「最後一格」)→loop 後補一次終態 recheck;(R4 **P1**)build 終態 flip 與 job finalize 兩段 commit=class 10——crash 在中間留 job 永久 running 指向已終態 build(retry 撞 BuildNotResumableError→卡死、擋專案刪)→build+job 摺進單一 txn;(R5 P2 **判 deferral**)鎖只序列化 build **建立**、不序列化**執行**,兩並發 dispatch 仍會同跑一個 building build 的 stage=class 10,但**正確修法需 lease/heartbeat**(區分 alive-running vs crashed-running;naive「running 就 no-op」會 reintroduce R4 剛修的卡死——dead worker 的 job 永不 retry)→arq 的 job-timeout/retry 領域(BA2d),當輪 reply-and-resolve + 存 TASKS.md BA2d follow-up(對齊 owner「劃邊界不上綱」:一個「加了反而錯」的 P2 正確 deferral,理由要**可辨別**不是 blanket punt);(R6 P2)step-4f 的 cancel recheck 在**獨立連線**、終態 txn 前讀,cancel 落在 recheck 與 finalize 之間=被接受(cancel_requested=True)卻被忽略(job→done)的 limbo=class 10 cancel 面——**跨連線讀信號本質是 race**;修法=cancel 決策**移進終態 txn、job 列 FOR UPDATE 鎖內讀**(鎖=cutoff)+ request_cancel 的 UPDATE 加 `WHERE status IN active`(輸掉 race 的 cancel no-op、不誤標已終態 job)+ record_run 也進同一 txn(`record_run_in_txn`:從 loaned-clean record_run 抽出的 in-txn 變體,record_run 留作 wrapper 供他 caller)——run+build+job 至此**完全原子**;(R7 P2)record_run 從「任一 failed item→run failed」推斷,但 orchestrator 對 §22 門檻**以下**的 failure 標 build ready→ready build 配 failed run,誤導 §18 Health=class 5 檢查者/消費者分岔(judge surface 讀 run.status)→加 `failed: bool | None` override,build run 的 status roll up 到 **build 結果**(門檻中止才 failed),failed items 與其 failed **step** 仍記錄(§18 drill-down/§27.7 retry 讀 step/item——run↔step 不同粒度的分歧是**對的**,collapse step 會破 retry)。**壓縮手段(未落實的教訓,重複第三次)**:R1/R2/R4/R6 四個 TOCTOU/atomicity finding 若在 first push 前做一次「每個 commit 邊界 × crash/並發交錯」審計(#38 生命週期矩陣在**執行**面的對應),本可一輪掃完而非四輪——**多-commit 狀態機開工時先列每個 commit 邊界並問「crash 或並發 op 落在這裡會怎樣」**(#37 請求生命週期、#38 狀態機矩陣、本次執行=同一預防的第三次)。**本地 reviewer 抓 2 件 session-only 事實**:(a)第一版 cancel-racing 測試**假綠**——並發 request_cancel 的 get_job 在其 UPDATE 前就讀到 'done'、Python 分支先擋掉、根本沒碰到 guard(revert-probe 拿掉 guard 仍過)=class 8;拆成**確定性** guard 測試(monkeypatch get_job 餵 stale 'running' 快照、逼出 UPDATE 打在真 'done' 列)+ NOWAIT probe 證明終態 txn 跨 record 持鎖;(b)新整合測試**漏了 `@pytest.mark.integration`**(該檔用 per-test 標記非 module-level pytestmark)→會在 CI fast lane(無服務)紅——本機 fast-cov gate 因服務在跑而靜默放行,靠 collect-only/deselected 數變化抓到。**新防線兩條**:(i)多-commit 狀態機邊界審計(每個 commit 邊界問 crash/並發);(ii)**live-DB 測試在 per-test 標記的檔案裡要顯式加 integration 標記——fast-coverage gate 會靜默跑未標記的 live-DB 測試(本機服務在)但 CI backend job(無服務)會炸**。無新 numbered class;class 10(多-commit 每邊界一窗:建立序列化/終態原子/cancel 鎖內/跨連線信號讀=race)、class 9(cancel checkpoint 最後一格)、class 5(run status roll up 到 build 結果=judge surface 粒度)、class 8(race 測試假綠:probe 沒落在被守窗口/沒觸及 guard)各得亞種。輪次:…→3→0→1→1→**5**。

**#47(BA2c-2a build-config loader:projects.config JSONB → typed §5/§6 config,2 輪 finding-Codex + 1 輪 +1(2 P2 皆判 must-fix、皆修)、本地 reviewer 首審 2 nit(1 修、1 被 Codex 升級);BA2c 第二切片之首、純函式零 I/O、unit-only)**:owner 定案「複用 dataclass 驗證、不建凍結 contract」的三切片(2a loader / 2b stages / 2c real-LLM)首片。loader 只做「型別化 dataclass 不管的那層」——按固定 key 取每個 block、逐葉型別檢查(bool 非 int 對每個數值葉)、再構造既有 typed 物件讓其 __post_init__ 驗業務規則(class 3 規則自洽:門檻序、非空詞彙、relation 端點引用皆單一來源、loader 不複述),失敗一律包成單一 BuildConfigError。**兩個 Codex P2 是同一母題的兩個位置——「畸形輸入靜默取預設而非 fail-loud」(class 1 值域完整性),差別只在畸形發生的層級**:(R1)`raw.get("ontology")` 對「省略」與「顯式 `{"ontology": null}`」都回 None → null block 靜默吃 absent-default=**#43 per-field-nullability 亞種**(省略≠顯式 null 不可 collapse);修法=四個頂層 block loader 改用 `"key" not in raw` 判省略,present 值一律過 `_mapping`(它本就拒 null=not-a-Mapping),巢狀/葉層(proposal_policy/entity_types/rule/sub-key)早已由 _str/_number/_int/_mapping 擋 null,只這四格漏。(R2)巢狀 rule block 不檢查多餘 key → optional key 打錯字(`disambiguator` 之於 `disambiguator_column`)靜默落 None → extract_structured 鑄 name-only entity key、把同名不同 id 的列 collapse 成一個 entity=**class 9 防護面完整性 × 過度阻擋兩面同時**:守面要涵蓋每個「封閉」巢狀 schema(漏了),但**不可**越過 free-form 契約邊界(頂層 projects.config 由 API verbatim round-trip、須續容忍未知頂層 key)。修法=`_reject_unknown(block,allowed,path)` 掃**所有六個封閉巢狀 block**(ontology/resolution/chunking/EntityRule/RelationRule/mapping-value)一次到位(同類掃全、非只改 Codex 點名的 entity rule),arbitrary-key map(structured_mappings、entities)刻意不掃(其 key 是資料非 schema),`"table"` 特例拒絕續在 _reject_unknown 前(訊息更精準)。**元教訓(lenient-vs-strict 有邊界)**:free-form 容忍只在「契約真的存在」處成立(頂層 API round-trip);把它**均勻**套到封閉巢狀 schema=留一整排 typo footgun。判準:**在契約邊界容忍、在每個封閉 schema 嚴格**——而邊界用「這層的 key 是資料還是 schema」界定(map 的 key=資料→容忍;固定欄位 block=schema→嚴格)。**class 5 檢查者分岔:本地 reviewer 首審把 R2 的 free-form typo 面判成「accepted tradeoff」、Codex 升成 data-correctness must-fix(reviewer 事後自承「supersedes the tradeoff I under-called」)**——教訓:reviewer 標「可接受的取捨」時要再問一次「這取捨有沒有真實資料後果」(此處=entity collapse),有後果就不是取捨是 footgun。**壓縮手段(未落實、與 #46 同構第四次)**:兩 finding 同屬 class-1/9「畸形輸入 × 層級」矩陣,first push 前若做一次「每個輸入位置(block-present-null / unknown-key / wrong-type / out-of-domain)× 每層(頂/block/rule/leaf)是否 fail-loud,除非 free-form 契約在該處明文授權容忍」的完整性審計,兩格可一輪掃完而非兩輪——**validating loader 的完整性面=輸入位置 × 層級的笛卡兒積,逐格問 fail-loud vs 靜默預設**。本地正面:nit-1(_str 葉檢查在 _construct lambda 內→型別錯被雙重前綴)修為 hoist 到 lambda 前;複用 dataclass 驗證使 config.py 零業務規則複述;每格修法皆 revert-probe 證載重(null:還原 raw.get→ontology-null 測試失敗;unknown:拿掉 entity-rule _reject_unknown→disambiguator-typo 測試失敗);config.py 全程 100% 覆蓋、26 測試。無新 numbered class;class 1(省略≠null、unknown-key typo=畸形靜默取預設)、class 9(守面涵蓋每個封閉巢狀 schema × 頂層 free-form 不過度阻擋)、class 5(reviewer 把 data-correctness footgun 誤判成 accepted tradeoff、Codex 升級)、class 3(業務規則單一來源)各得亞種。輪次:…→0→1→1→5→**2**。

**#48(BA2c-2b 真實 §5 stage 佈線:sources dispatch + default_stages + 六 adapter,0 輪 finding-Codex(首審 +1)、1 次本地 re-stamp(clean 標籤註解);BA2c 第二切片、全案首個把 C1–C11 引擎端到端接進 orchestrator——H9 loop-throughput 修法後第一個任務,fix-1「pre-push 對抗式矩陣審計」的首個效果數據點)**:default_stages 把六個 `(conn,project,build_id)->StageResult` adapter 閉包在 BuildConfig(2a)+ process-wide LLM/embedder/store client 上,每個在傳入 conn 上建 `for_building_build` writer/projector(DR-006 building-guard 同交易一致)、從 SoR 重讀、把 stage report map 成 StageResult;sources.py 按 kind 分派 connector(text→read_text_documents、structured→read_csv_rows,unsupported kind/非 file:// uri/缺 table|pk_column 皆 fail-loud);graph 對「ontology=None 但有 text-mime doc」丟 OntologyRequiredError。**0 輪的歸因(fix-1 當場擋掉 Codex 會戳的格)**:push 前對佈線類跑到底的 §7 矩陣——(a)**handoff 完整性(C2 教訓)**:ingest→clean 走 **SoR**(clean `fetch_all(documents)` 重讀),非 ingest 回傳 tuple → 結構性避開 C2 dropped-handoff bug(已 commit 的 doc 在 Postgres,resume 時 clean 仍見);(b)**generator 單次消費**:改 lazy generator 後去讀 `ingest_documents` 確認只 `for` 一次(否則就是 bug);(c)**值域 fail-loud**:sources dispatch 每格;(d)PR body **預先寫上兩個 reviewer nit 的理由**(clean 標「cleaned」非「skipped」因 clean_document 兩路徑都回 chunks、重推會複述其收斂檢查=class 3;StageResult.detail 是 forward-fill、orchestrator 尚未 fold)。審計本身還在 push 前逼出 2 個真改進(materialized list→lazy generator、拿掉 type-ignore)——留到 Codex 就是額外的輪。**誠實保留(勿過度歸因 H9)**:(1)**單點且任務型別混淆**——本任務是**佈線模組**(薄 adapter 疊在已測 C2–C7 上),Codex-finding 面本就低;BA2c-2a 是 parser/validator(高-finding 類)。2→0 有一部分是任務性質。**H9 fix-1 的真正檢驗是下一個 parser/validator/multi-commit 型任務**(BA2d arq worker + lease dedup=multi-commit/並發=class-10 高-finding 面,刻意當試金石)。(2)**fix-2(re-stamp 批次)、fix-3(gate-wait overlap)本輪未觸發**——0 輪→無 Codex-driven re-stamp 要批;序列跑單一 PR、未 overlap 等待。仍待驗。**測試**:10 hermetic sources unit + 9 component(spy stage deps 釘 config→arg 佈線 + report→StageResult mapping、fast-lane 覆蓋)+ 2 integration(live PG+Qdrant+Neo4j、fake LLM/embedder:結構化 corpus 六 stage 到 ready、text-無-ontology 在 graph 失敗);sources.py/stages.py 皆 100%、總 86.47%。**component test 是必要不是加碼**——stages.py adapter 只被 integration 打(coverage lane 排除),不補 component 則總覆蓋掉到 85.04%(擦線)且 config-arg 佈線無 fast-lane 驗證。無新 numbered class(佈線正確性靠既有 class:handoff 完整性 via SoR、值域 fail-loud、config-arg 佈線);本條真正價值=**loop 效能數據點:H9 後首任務 0 輪,fix-1 正向訊號但被任務型別 confound,fix-2/3 未驗——結論待 BA2d**。輪次:…→1→1→5→2→**0**。

**#49(BA2c-2c 兩-lane real-LLM pipeline 測試:text arc 端到端過 default_stages,0 輪 finding-Codex(首審 +1)、1 本地 nit(review_ledger cleanup);BA2c-2 最後切片、test-only 無 source)**:補上唯一沒被端到端測到的洞——TEXT 抽取路徑(C3b/C3c)過 default_stages+run_build(2b 用 ontology=None 跳過 text 抽取;test_graph_documents 直接測 extract_documents、非過 orchestrator)。兩 lane 共用一個 tiny corpus(「Alice works at Acme…」,ontology Person/Company/WORKS_AT):**hermetic**(CI 跑、無 key)用 **context-aware fake LLM**——graph 與 summarize 共用同一 chat model 但 system prompt 不同,單一 canned answer 不能兩用,故按 prompt 開頭字串分派(「summarize one community」→summary JSON,否則 extraction JSON);canned relation quote 是 _TEXT 的 verbatim 子串(過 §27.4 evidence CHECK)。**real-LLM** lane(integration + skipif 無 OPENAI_API_KEY)跑真 chat_model/embedding_model、斷言放寬(build ready、≥1 entity、≥1 embedded point,因真模型輸出非決定性)。**skip-only 契約驗證**:CI 無 key(ci.yml 無 secret)→ real lane skip、hermetic lane 無-key 跑(整合 job 綠=real lane 真的 skip 了,否則 chat_model() 會 raise);本地 .env 有 key → real lane **實跑**(2 passed 含真 API call,證非靜默 always-skip=owner「local enforcement」)。本地 nit(reviewer 抓、pre-empt 一輪 Codex):_cleanup 漏 review_ledger——resolve auto-merge 會寫、且它 non-build-scoped(DR-003)跨 run 全域殘留;修=加進 per-project delete loop(它有 project 欄、無 FK)。**H9 效能**:又一個 0 輪,但 **test-only=低 finding 面**,與 #48(wiring)同屬低-surface,仍**不足以驗 H9**;真試金石仍是 BA2d(arq/lease=multi-commit/並發=class-10 高面)。無新 class。輪次:…→1→5→2→0→**0**。

**#50(BA2d-1 執行租約 execution lease:DB heartbeat-lease 做 build-execution dedup,0 輪 finding-Codex(首審 +1)、1 輪本地審查 3 nit;BA2d 首切片、H9 後第一個高-finding-面(class-10 並發)任務——H9 fix-1 的決定性數據點)**:收掉 Codex 在 BA2c-1 開的洞——run_build 的 FOR UPDATE 只序列化 build **建立**、resolution commit 就釋放,兩個 dispatch 於是並發跑完六 stage 打同一個 building build(idempotency 下安全,但雙倍 LLM 成本+derived store 寫入互撞)。owner 定案走 **DB heartbeat-lease**(非 arq-native timeout):worker 用原子 conditional UPDATE 搶租約(`WHERE lease_owner IS NULL OR lease_expires_at < now()`——**guard 在寫句裡、非事前 read**=class-10 TOCTOU 教訓,並發搶 free/expired 租約 EvalPlanQual 保證**恰一個贏**,live 用 asyncio.gather 實測)、run_build 期間 heartbeat 續命、退出必 release(含失敗路徑);crash 的 holder 不 release 但租約在 **DB 時鐘**(單一時鐘源,無跨-worker skew 偷租約)過期→下個 dispatch reclaim。租約是**疊在 idempotent-safety floor 上的 liveness 層**:最差退化成並發-但-安全,絕不腐蝕。**H9 決定性驗證(本條核心)**:這是 H9 後**第一個高面任務**(多檔:migration+primitives+wrapper+重測,並發正確性),BA2c-2b/2c 的低-surface confound 在此**退場**。fix-1(pre-push 對抗式矩陣審計)在 push 前抓到一個**真正的正確性 bug**——heartbeat 的 renew 例外會經 `finally` 裡的 `await beat` 冒出、**遮蔽掉成功 build 的 BuildOutcome**(best-effort 背景任務汙染主結果);修=heartbeat 內 `except Exception: continue`。這 bug 留到 Codex 就是額外的輪、甚至潛伏成 prod bug。本地 gate-2 reviewer 另開 3 hygiene nit(jobs_lease_owner_nonempty CHECK=class-1 識別子禁空字串、echo H6;None-on-acquire-fail 語意註解;offline-SQL drift-pin),**1 輪本地修完+1 次 re-stamp**。Codex:**0 輪、首審 +1**。→ **H9 fix-1 在硬案首次拿到正面結果**:pre-push 把「會是 Codex 輪/潛伏 bug」的並發缺陷當場轉成本地 catch,正是 fix-1 的設計意圖。**誠實保留**:(1)3 nit 有一部分是 gate-2 本地 reviewer 的功勞(非純 fix-1 自審)——但 heartbeat bug 是 fix-1 自審的乾淨勝場;(2)fix-2(re-stamp 批次)本輪只 1 本地輪、無多輪批次可驗;(3)fix-3(gate-wait overlap)序列單 PR、未 overlap;(4)仍是**單一**高面數據點(n=1),但已把 #48/#49「confound 未解」推進成「硬案上 fix-1 正向」。**證的並發性質**:原子 guard-in-write acquire(一贏)、owner-guard renew/release(丟租約的 worker 不能續/偷)、crash-reclaim via expiry、jobs_lease_paired +jobs_lease_owner_nonempty CHECK。測試 6 hermetic component+7 integration(**concurrent acquire 恰一贏**、exclusive-until-release、owner-guard、**empty-owner 被拒**、expired reclaim、**concurrent dispatch 恰跑一次 build**、crashed 租約不擋新 dispatch);coverage 86.47%、lease.py 100%(jobs.py primitives 同 lock_job 屬 integration-only)。**新 lesson-class 候選(暫不編號,循 #41/#42「候選→再犯才正式化」紀律,如 #11 於 #37 才成形)**:*背景 best-effort 任務(heartbeat/cleanup/telemetry)的例外經 finally 的 await 遮蔽/汙染主結果,或殺死本應續命的迴圈*——防線:fire-and-forget 任務在 finally 被 await 時例外要被包住(swallow-and-continue 或 broad suppress),只有 cancellation(BaseException)能終止迴圈;審查問「這背景任務的例外會不會蓋掉主結果?」。BA2d-2(arq)/BA2e(SSE)再犯即正式編號並寫入 code-reviewer §7。輪次:…→5→2→0→0→**0**。

**#51(BA2d-2 arq worker + Redis wiring,4 輪 Codex(5 finding:1 P1、4 P2)皆 must-fix、0 本地 blocker;BA2d 第二切片、H9 後首個「陌生框架生命週期」任務)**:把 build 執行接上 arq queue。**核心線索——本任務引入了「一個 job 可被跑不只一次」(re-dispatch:arq retry / crash 後 in-progress key 過期重派 / 未來 BA2d-3 reaper),Codex 於是把 re-dispatch 的每一條正確性需求逐輪逼出**,全落在兩個母題:(A)**框架逾時語意 vs 應用自有 SoR**、(B)**re-dispatch 生命週期不變量**。逐 finding(R 依 finding、非輪次;R1/R2 同屬 round-1)：R1(P2)job_timeout=3600 太高→crash 復原慢:arq 的 in-progress key = job_timeout+10s 才釋放重派=**class 4 工具語意**(不讀 arq 內部就不知這條)。R2(P2)preflight 失敗(project 消失/config malformed 在 run_build 標 status 之前)→ durable jobs 列永遠 `queued`(擋 delete、誤導 GET /jobs)=**class 9 防護面完整性**(每條失敗出口都要終結化 durable 記錄,含 main-path 之前短路的)→ try/except 標 failed + return"failed"(不 retry,deterministic)。**R3(P1,round-2,本 PR 最深):arq 用 `asyncio.wait_for` 逾時會在 run_build 終結化 SoR 列之前 mid-stage 取消 build,且 `TimeoutError` 不在 retry 分支(worker.py L147 只收 CancelledError/RetryJob)→ 逾時 build 被取消且不重試、jobs 列卡 `running` 永久**——先讀 arq.worker 源碼確認語意(class 4 更深一層),再認清根因=把 crash 復原耦合到框架逾時本身:降 timeout(R1 修法)反而讓 mid-flight 取消更常發生(R3),tuning 無解的三難。**升級到 AskUserQuestion 讓 owner 定案**(不猜架構):job_timeout 改**寬鬆 24h backstop**(arq 永不取消 live build→SoR 必終結化)、快速 crash 復原**解耦**成新切片 **BA2d-3 lease reaper**(週期性重派 lease 過期的 building build)。R4(P2,round-3)+R5(P2,round-4)**config drift on resume**:run_build_task 每次 dispatch 重讀**可變** proj.config→中途 PATCH /projects 讓 resume 的 build 用漂移的 chunking/ontology 參數(毀 convergent idempotency/混輸出)=**class 11 請求級不變量掃全生命週期**(「使用者提交的 config」這個不變量要涵蓋 queue→dispatch→retry→resume 全程)。R4 修法=first-dispatch 快照(`capture_config_snapshot` 原子 `COALESCE` UPDATE,guard-in-write=class-10,並發首派收斂到單一 config);**R5 Codex 精修**——first-dispatch 仍留**queue-delay 窗**(create_job→首派間的 PATCH)→ 快照點上移到 **job 建立**(create_job 用 scalar subquery 在 INSERT 釘 config),worker 的 capture 降為**重用+防禦性 fallback**。**H9 誠實檢討(本條核心)**:4 輪是相對 BA2d-1(0 輪)的**回退**,但成因有指導性——R1/R3 是 **class-4 工具語意缺口**(arq 的 wait_for/retry/in-progress-key 確切行為),pre-push 對抗式審計(fix-1)審的是**我的碼**、審不出「我沒讀框架源碼所以不知其確切語意」;我只在 R3 **反應式**才去讀 arq.worker。**教訓(H9 fix-1 的邊界+補強):建構在陌生框架生命週期(worker/timeout/retry/cancel)上的任務,pre-push 階段要先把框架的確切執行+重試+逾時+取消語意讀進來(讀源碼/測小案),當成「框架出口清單」(呼應 #41 error-envelope)——這會在實作期就攔掉 R1/R3,而非逐輪**。**新 lesson-class 候選(暫不編號,循候選→再犯正式化)**:*框架內建的存活/逾時機制 vs 應用自有的 SoR*——框架的逾時取消會打斷 SoR 的終結化、且其重試語意未必涵蓋逾時;把 crash 復原耦合到框架逾時會生三難;防線=**自有 liveness(DB lease+reaper),框架逾時只當寬鬆 backstop**,且審查問「這個框架機制觸發時,我的 SoR 列會被留在什麼狀態?」。BA2d-3(reaper)/任何框架整合任務再犯即編號。**正面**:收斂judgment 全對(5 finding 全 must-fix、無過度上綱)、架構分叉升級 owner(非猜)、每輪同 class 一次掃完(config-drift 那輪連並發測試+create_job 上移一起處理);pre-push 主動加 concurrent-capture 測試(class-10 紀律);`config_snapshot` plain JSONB **無 none_as_null**(reviewer 認可的雙態設計)——但測試 helper 寫 `None` 竟寫成 JSONB `'null'`(COALESCE 視為 present)、被自己的紅測抓到=**echo class-8 邊界/表示誤差**(Python None→JSONB literal ≠ SQL NULL),改 `sa.null()`。測試:9 hermetic component+11 lease/config integration(含 create_job-pins-at-creation、defensive fallback、concurrent-capture 收斂);build_worker.py 100%、total 86.61%。**里程碑**:build 執行正式跑在 queue 上;下一站 BA2d-3(reaper)或 BA2e(HTTP triggers)。輪次:…→2→0→0→**4**(高面框架任務,class-4 語意缺口主導)。

**#52(BA2d-3 lease reaper,5 輪 Codex——5 P2 皆 must-fix、0 本地 blocker;BA2d 收官;#51 候選 class 在本 PR 再犯 → 正式編號 class 12)**:完成 DB heartbeat-lease 成為唯一 build-liveness authority——`find_reapable_jobs`(held+expired lease+非終結)+ `reap_stuck_builds` cron(2×/min、`unique=True`)重派 crashed build。**核心線索:Codex 把「recovery channel 自身的完整生命週期」一輪一層逼出**,五輪全是 class-12 的面:R1 starvation-recovery race——原 worker(餓而未死)在 reaper 選中後、replacement 起跑前完成並釋放租約 → replacement 取得空租約進 run_build 撞 `BuildNotResumableError` 變製造出來的失敗 → 修=當良性 no-op(run_build 的 FOR-UPDATE 狀態檢查即原子 recheck);R2 **pre-acquire crash 窗**——preflight+stage 建構在取租約之前,死在那裡=queued+無租約,reaper 隱形、退回 24h → 修=抽出 `job_lease` async CM,**租約包住整個 dispatch**(進 task 第一件事),reaper 謂詞不動,加 ordering pin 測試;R3 **重派堆積**——replacement 排在飽和佇列後面時,stale row 每 30s tick 都 match,fresh id 每次鑄新 job=無上限重複 → 修=**deterministic per-generation id**(`reap:<job>:<expiry>`,stale expiry=世代標記,只有 acquire/renew 會動它;DB 時鐘單調→新 crash 必得新 id);R4 **框架 key 保留扣住 recovery id**——replacement 在 acquire 前就 fail(expiry 沒動→同 id),arq 對已完成 job 保留 result key `keep_result` 秒(預設 3600)=同 id 被拒 1h → 修=`keep_result=0`(SoR 教義:無人消費 arq result;由源碼驗證 result 只在 `keep_result_forever or timeout>0` 時儲存);R5 reaper 掃描無索引(唯一 index=jobs_by_project)→ 修=partial index `jobs_reapable`(WHERE 逐字鏡射謂詞,覆蓋集近空;reviewer 在 live PG 驗 planner 確實走 index)。**元教訓(本條核心):引入 recovery/retry channel 時,它自己就是一個要 §6/§7 矩陣掃完的生命週期**——誰重派、與原 dispatch 怎麼 race、重派如何 dedup、id 何時被框架保留、recovery 自身失敗怎麼辦、掃描成本——Codex 一輪一格逼出的正是這張矩陣;開工前把矩陣列完可把 5 輪壓到 0-1 輪(已納入 class 12 寫進 code-reviewer §7)。**正面**:五輪判讀全 must-fix 無過度上綱;每輪同類掃完;R2 修法選「租約上移」而非「放寬 reaper 謂詞」(no-lease+queued 與健康 backlog 不可分,放寬=重複 enqueue 洪水——結構性修因,呼應 class 9 的「別過度阻擋」對偶);本地 reviewer 攔到 queued-crash 測試格(pre-`running` crash 可 reap 的判別 pin)與兩處 stale docstring;**H8 quota 機制實戰**——PR 中途 Codex 額度用罄,`probe-codex.sh` 背景 hourly poke,owner 回報 reset 後 probe 的 poke 即觸發重審拿 +1。輪次:…→4→**5**(全部 class 12,單一母題的完整性鏈)。

**#53(BA2e-1 ingest/build triggers + job endpoints(GET/cancel)+ reaper queued-sweep,5 輪 Codex——6 P2 皆 must-fix、1 輪本地 FAIL(1 blocker)+1 nit 升級採納;BA2e 首切片;開工用 graphrag-ba2e-prep(已刪) 的預研+owner 三定案:兩切片/reaper 掃 never-leased queued/source_ids+reason loud-reject)**:R1(class 6/1)jobs.error 存 3 欄而凍結 Error required 含 request_id → GET /jobs/{id} pass-through 契約不合法;**根因=tables.py 註解把凍結形狀複述錯**(「§15 Error shape {code,message,details}」)而 BA2e 信了複述——**凍結 schema 的欄位清單絕不複述,引用 schema 本身**;修=兩個 writer(orchestrator 終態+worker _fail_job)鑄 request_id(worker 側無 HTTP request,id 命名 failure record)。R2(class 11 亞種)cancel 的 get_job-404 precheck 跑在 run_idempotent 之前 → 終態 job 隨專案 CASCADE 消失後,活 key 的 retry 拿到 404 而非 stored 202、異 hash 拿 404 而非 IDEMPOTENCY_CONFLICT——**replay/conflict 決策先於任何 scope-row precheck;scope row 可以合法消失而 key 還活著**;修=非預留 peek(replay_stored)前置。R3(**新候選 class,暫不編號**:*eager dependency acquisition 把非依賴路徑耦合到資源可用性*)——`Depends(arq_redis)` 在路由入口就開 pool(框架 DI=隱形 eager 點),Redis 掛掉連 replay/409/404/400 都 500;修=zero-I/O lazy provider,pool 只在 enqueue 點開,touch-counting fake 釘「用的路徑恰 1 次、其他 0 次」;再犯即編號。R4a(**#45 再犯,新觸發形態**)round-1 的 fix 改了「已存欄位形狀」卻沒問「已寫入的列怎麼辦?」→ 舊列繼續發不合法 error → 0014 backfill-before-CHECK(+jobs_error_frozen_shape CHECK 把形狀變 storage invariant);**「收緊和解既有列」不只適用於 migration 任務——任何改 stored shape 的 mid-PR fix 都欠這一掃**。R4b(**#52 R5 再犯,隔一個 PR**)queued-sweep 無索引而 sibling(jobs_reapable)有——**sibling 機制的 parity 掃描要含支撐結構(index/測試/文檔),不只語意**;修=jobs_unenqueued partial index。R5(**class 1 省略≠null 再犯,#43/#47 之後第三次、升到 body 層**)`Model | None` 把整包 JSON null 綁成 None=與 absent 不可分 → 契約不合法請求靜默開工;**parser 矩陣的位置軸要含 whole-body:{absent, empty-object, JSON-null, wrong-top-type}**。**本地 FAIL 1 輪(#45 防線在 review 層生效的正面數據)**:0014 的 backfill 在 CI 的全新空庫上永遠跑 0 列——reviewer 依 §6 Migration bullet 要求 populated-DB 測試(downgrade→插 legacy 列→upgrade 必成功且已和解;鏡射 0010 precedent),同輪 nit(?& 對 4-string 陣列也 true→jsonb_typeof='object' 硬化)一併採納;這一輪本地 FAIL 省掉第 6 輪 Codex。**元教訓兩則**:(a)R1→R4a 是**因果鏈**(shape fix→舊列→backfill→index)——fix 當下做完自己的 lifecycle sweep,四輪縮一輪;(b)5 輪各屬不同 class(形狀/順序/耦合/migration/parser 矩陣),per-class sweep 無法跨類壓縮——能壓的是「fix 的 lifecycle sweep」與「先讀 schema 不信複述」這兩個源頭紀律。防線已寫入 code-reviewer §7 新增三 bullet+parser 矩陣 whole-body 句;R3 候選依 #50/#51 紀律留在本檔、未寫入 checklist,再犯才編號進 §7。流程:CI-first 全程;watcher 每輪正常;enqueue-in-band-before-commit+reaper queued-sweep 把 class-12 sibling 窗(TASKS.md BA2e 行的設計註記)結構性關掉。輪次:…→0→0→4→5→**5**(6 finding,含 1 個雙 finding 輪)。

**#54(BA2e-2 SSE job events,0 輪 Codex 首審 +1(quota 重置後);本地 reviewer 1 PASS+1 nit 立即採納(mid-stream poll 例外=蓄意 propagate-and-sever,補註解)+1 次 re-stamp;BA2e 家族收官)**:0 輪的歸因鏈完整可辨——(a)**prep memory 前置**(BA2e prep 在 #52 quota 等待期寫好,開工直讀不重掃合約);(b)**#53 retro 掃項逐條前置套用**:凍結 JobEvent 直接讀 openapi.yaml(不信複述)、stream 生命週期(connect/首幀/change-only/terminal/vanish/disconnect)開工即列格(class-11 儀式)、R3 候選 class(eager acquisition)直接**塑形了設計**——poller seam 生而 zero-I/O(候選 class 用在設計期=預防,非再犯,仍不編號);(c)**自審攔下一個 class-12 亞種級 bug**(session-only,無 trace:修正折入 push 前單一 commit):首版 SSE endpoint 用 `Conn` 做 404 precheck——**FastAPI yield-dependency 存活到 RESPONSE 完成,對 StreamingResponse=request transaction 掛著 idle-in-transaction 一整條 stream 的壽命**;precheck 改走同一 poll seam。此為 class-12(框架生命週期語意)在 HTTP 響應側的新面:**凡 streaming 響應,盤點每個 yield-dep 的實際存活期**。設計三則備查:cancelled→job.failed(凍結 event 集無 job.cancelled;payload status 帶精確值);row 消失=靜默終止不捏造 terminal(SoR 沒說過的話不發);ts=DB 時鐘與 row 同一 statement(get_job_at,單時鐘源)。測試分層教訓:**scripted-poller component 層測語意格(vanish/cancelled/change-only),committed-rows+真 lifespan 整合層專證「並發 writer 的 committed 進度可見」——savepoint harness 結構性測不了這件事**(其未提交世界對 stream 自己的連線不可見);ASGITransport 不跑 lifespan→`app.router.lifespan_context(app)` 顯式進入。無新 class;class 12 得 streaming-response yield-dep 亞種(自審攔下)。輪次:…→4→5(#52)→5(#53)→**0**。BA2 全家族(a–e)完結:jobs 表→FK→orchestrator→config→stages→real-LLM→lease→arq→reaper→triggers/endpoints→SSE。

**#55(BA3a inspection documents/chunks,1 輪 Codex——1 P2 must-fix;本地 reviewer 1 PASS+1 nit 採納(order_by 巢狀攻擊補測)+2 次 re-stamp;BA3 首切片、owner 三定案:三切片/reports 剔除(合約無此端點)/照 Track 2 進行)**:開工直接用 graphrag-ba3-prep(已刪)(quota 等待期預研)——active-binding seam(project 404 先於 409 NO_ACTIVE_BUILD)、meta.build_id 首次佈線、`fetch_page`(ORDER BY 通道過同一 raw-SQL 拒絕+巢狀 pin)、DR-006 隱形性(跨 build+跨 project)整合實證,全部首輪過。唯一 finding=**per-field nullability class 的第四個位置(#43 config-null→#47 block-null→#53 whole-body-null→#55 EMISSION-side)**:凍結 Chunk.status 是 optional NON-nullable string 而欄位 nullable(cleaning path 寫 chunk 根本不帶 status)→ `"status": null` 讓成功響應 schema-invalid;修=NULL 欄位以「省略 key」表達(optional non-nullable 的唯一合法編碼),same-class sweep 掃出 document_dto 同形(同 commit 修)+ 全欄位盤點(其餘欄位 DB NOT NULL 或 contract-nullable)。**元教訓:#53 retro 加的「emission surface audit」bullet 只審 required set——nullability 矩陣是雙側的(request parsing × response emission)× 每個欄位**,§7 該 bullet 已補強。輪次:…→5(#53)→0(#54)→**1**。BA3a 收官;BA3b(entities/relations)沿用同 seam。

**#56(BA3b inspection entities/relations,0 輪 Codex 首審 +1;本地 reviewer 1 PASS+1 nit 採納+1 re-stamp)**:0 輪歸因——BA3a 模式逐格複製(reviewer 自查「documents/entities/relations 三個 list cell-for-cell 一致,無漏格」)+ **#55 的 emission-nullability 規則首次在設計期套用**(created_by/relation_signature omit-when-null、attributes→{}、內部欄位 embedding_point_id/evidence_hash 不發射——寫 DTO 前先逐欄過凍結 schema,retro 教訓→設計儀式的又一例,同 #54 的 eager-acquisition 前置)。唯一本地 nit=**評論事實錯誤**(「no created_at on evidence」——該欄存在且 NOT NULL;id-sort 的真理由=created_at 是 statement timestamp 非 total order)——#53 R1「複述/斷言錯誤=漂移源」的迷你版,這次被本地 reviewer 咬住(防線生效);修正即真理由入註。evidence 子讀取三性質備查:BUILD_ONLY_SCOPED 注入=composite FK 之上的 defense-in-depth;detail-only(無 N+1);全量讀取無 silent cap(§27.4 dedup 有界)。輪次:…→0(#54)→1(#55)→**0**。剩 BA3c(subgraph,Neo4j seam)。

**#57(BA3c inspection graph/subgraph,1 輪 Codex——1 P2 must-fix;本地 reviewer 2 nit 採納+2 re-stamp;BA3 家族收官;owner 兩定案:query_policy 來源=registry config 嚴格模式(不發明 §21 預設)、C8b 插隊)**:核心=id-seeded `subgraph_context` 疊在 C6c 硬化路徑上(`_neighbor_entities` 加 seeds 參數;§16 citability 雙側對稱——node 要 mention、edge 要 evidence;投影只出拓撲,值全讀 SoR)。R1(class 2/3)=policy 檢查跑在 binding 之前 → 無 active build 的專案拿 400 而非本 surface 一致的 409——**「assess and pin」的陷阱:本地 reviewer 把錯的優先序「釘成文檔行為」,Codex 升級成 must-fix(#47 undercall 亞種)——把行為釘進測試≠行為正確,錯誤優先序要跨同 surface 全端點掃一致**。本地 nit 1(class 12 亞種,審中攔下)=sync-def FastAPI dependency 在 threadpool 跑 → 無鎖 check-then-set 跨執行緒可雙建 driver;修=async def(event loop 上無 await 的 check-then-set 才原子)——**FastAPI dep 的執行位置(loop vs threadpool)也是框架生命週期語意**。nit 2=§21 narrows-never-widens 無測試 → capture-stub 釘 [5,100]。flake 觀察:全 tier 兩次 one-off(registry pagination / builds lifecycle,各自隔離綠、終輪 200/200)=跨測試 dev-DB 污染——**isolation debt 候選,再犯立 H-task**(CI 全新庫不受影響)。owner 中場輸入:初期落地=博物館導覽 agent、外部 no-code 平台經 **MCP over HTTP** 接本平台([[graphrag-goal-museum-guide]])→ C8b(MCP HTTP transport)插隊 BA4 之前(本 retro push 落地 TASKS.md)。輪次:…→1(#55)→0(#56)→**1**。BA3 全家族:documents/chunks→entities/relations→subgraph,binding seam+meta.build_id+policy 三件基礎設施就位。

**#58(C8b MCP HTTP transport,1 輪 Codex——1 個 P1 must-fix;本地 reviewer 2 nit 輪採納(SDK 棄用別名/安全註記精確化)+3 re-stamp;owner 場景任務:博物館導覽平台的整合縫)**:R1(**P1,class 12 再犯、新面「mode-enablement re-audit」**)——streamable HTTP 讓一個 FastMCP 實例多工多個 protocol session,SDK 對**每個 session 各進一次 lifespan**(Server.run 源碼驗證)→ build_server 的 module-level `runtime["current"]` 槽被每個新 session 覆寫、任一 session 關閉後倖存 session 的工具讀到已關閉的 store clients。修=SDK 自己的設計通道:lifespan **yield** _Runtime、_rt() 走 `request_context.lifespan_context`(每個呼叫解析到自己 session 的 runtime;stdio 單 session 行為不變);判別性 regression=兩個交疊 HTTP session 各 probe id(lifespan_context)(舊碼對每個 session yield None → id 相同 → 必炸)+ 關 B 只 dispose B 的 engine。**元教訓(class 12 亞種):啟用新的框架 mode/transport 時,框架出口清單的審計要對「既有程式碼中 mode-dependent 的假設」重開一輪**——我讀了 Server.run 來定 run_server 語意,卻沒對既有 runtime 槽問「lifespan 第二次進來時這個狀態怎麼辦」;stdio 時代的單 session 假設在 HTTP 多工下才現形。連帶:integration call-through 測試從「手動進 lifespan+直呼工具」升級成 SDK in-memory protocol session(session-scoped 設計所需,且更貼協定)。本地 reviewer 貢獻:對 SDK 源碼驗證 DNS-rebinding 行為(localhost 綁定才自動啟用;綁非 localhost=完全無 transport 層防護 → §23 auth 才是真門)寫進 config/測試註記;deprecated streamablehttp_client→streamable_http_client(新簽章 http_client 注入)。輪次:…→0(#56)→1(#57)→**1**(P1)。§9 兩 transport 就位;外部平台可 `--transport http` 接 `http://127.0.0.1:8300/mcp`。

**#59(BA5 merge-candidate review,2 輪 Codex——各 1 P2 皆 must-fix;本地 reviewer 1 nit 採納+3 re-stamp;BA4 同 PR 落地 v2 延後(owner:合約無清洗端點、FE2 本身 v2);Console v1 審核後端就位)**:核心=`decide_merge_candidate` 單交易(FOR UPDATE 下驗 §17 轉移=class 10、DR-003 ledger 進位、審計軌跡);本地 reviewer 對關鍵面做了 consumption 驗證(resolution.py 真的消費 defer——#28 R4a 的保證端到端成立:deferred 不 auto-merge 且 re-list)。R1(class 3/5「定義重用」亞種)=list 沒濾終態 → 與 health.py 的 §19 pending_review 定義(pending+deferred)分岔,佇列與自己的 gauge 不一致——**新 surface 服務既有概念時,先 grep 該概念的既定謂詞/定義再寫查詢**(佇列的定義早已存在,我重新發明了一個更寬的);修=同一 predicate 進 list、決定後 re-list 為空的判別 pin。R2(class 4×8「時鐘粒度」亞種)=兩個 decided_at 都用 now()——PG 的 now() 是 **transaction-stable**,同一交易內批次 defer→approve 兩筆 ledger TIE,§27.3 latest-wins 平手任意解——**我自己的 docstring 還把這個 tie 當 feature 讚美**(「同一瞬間」的優雅變成 bug);修=每個 decision 捕一次 clock_timestamp()(仍單一 DB 時鐘、ledger+candidate 仍同瞬間、跨 decision 嚴格遞增),判別 pin=單交易兩 entry 嚴格排序+effective_decision 端到端解到 approve。**元教訓:timestamp 餵 ORDERING/precedence 規則時,時鐘的「穩定性等級」(txn/statement/call)是正確性契約的一部分——「單一時鐘源」不夠,粒度要配 tie-break 的需求**(#38 R8 混時鐘教訓的姊妹:那次是兩源,這次是一源但錯粒度)。輪次:…→1(#57)→1(#58)→**2**。Console v1 後端剩 BA6 playground、BA7 health、BA8 builds。

**#60(BA6a query playground semantic/sql/global,4 輪 Codex——R1 1 P2、R2 1 P1、R3 1 P2、R4 2 P2,全 must-fix;本地 reviewer 1 nit 採納+4 re-stamp;共享 seam=`run_bounded_query` 公開化,REST 與 MCP 一套機器兩個門面;quota 中斷一次(probe 腳本自動收斂),merge 被 auto-mode 分類器擋兩次→owner 明示授權後完成)**:R1(class 1)sql 端點收 top_k 卻無視——accepted-and-ignored 參數即契約謊言;修=narrows-never-widens。R2(**P1,class 12 新面「yield-dep × pool 容量」**)policy 讀取的 `Conn` yield-dep 活到 response 完成(#54 教訓的池形姊妹),而 seam 內 binding 又從**同一個 pool** 取第二條連線 → 滿載時全體 worker 各持一條等第二條=convoy,健康請求把 §21 預算燒在排隊上;修=端點完全不收 Conn,preflight 用短命 acquire-use-release,判別 pin=checkout-counting fake engine 斷言 seam 執行瞬間 held==0。R3(**class 2 逐字再犯:#57 R1 原樣重現在新 surface**)policy/model 閘先於 active-build 查詢 → bootstrap 專案聽到 400/503 而非凍結 409——#57 修在 inspect 的 helper 上,沒修在「概念」上,一個 family 之後新 surface 重新推導就重新引入;修=`_load_policy` 結構性持有優先序(404→409→400→503),BA6b+ 自動繼承。**元教訓:retro 教訓若綁在 surface 上,對「之後才誕生的 surface」不轉移——耐久修法=讓一個 shared helper 擁有該不變量**。R4a(class 11 新面「seam 之前的段落」)preflight 的 Postgres 失敗掉進 generic 500——§22 typed degradation 是請求級不變量,seam 內enforce≠覆蓋 seam 前段落;修=preflight 自己 map STORE_UNAVAILABLE(inspect Neo4j 先例)。R4b(**class 1,且為 R1 修法自己引入的 fix-induced sibling**)R1 的 `min(top_k, sql_rows)` 沒讀凍結 schema 對 `max_top_k` 的欄位描述(「QueryRequest.top_k 的上界」)→ top_k=999 跑了 100 列;修=先過 `policy.top_k()` 再會合 row ceiling([1,20,100] pin)。**本地 reviewer 自錄 self-correction:它 R1 輪判 min(top_k, sql_rows) 可接受——凍結 schema 的欄位描述要在判斷 cap 之前讀,#53「不信複述」的姊妹(這次是「沒去讀」而非「信了錯的」)**。防線已入 code-reviewer §7:precedence-屬於概念、yield-dep×pool(class 12 (f))、seam 前段落(class 11 補句)、fix 的 cap 對凍結欄位描述驗證(fix-sweep (3))。流程:quota 等待期寫好 graphrag-ba6b-prep dossier(options 詞彙/remaining_ms/cap chain/facade parity 設計期審計;BA6b merge 後已刪);merge 分類器連擋兩次(hook 之外的第二道)→ 按 memory 的 merge-授權教訓轉述證據並 stop-and-ask,owner「please merge and continue」後完成。輪次:…→1(#58)→2(#59)→**4**(5 finding)。Console v1 後端剩 BA6b、BA7、BA8。

**#61(BA6b graph/hybrid query endpoints,1 輪 Codex——1 P2 must-fix;本地 reviewer:1 PASS+parity nit 採納為修+1 FAIL(判別性)→PASS,共 3 stamp;BA6 家族收官,Console v1 查詢面完整)**:BA6a 4 輪 → BA6b **1 輪**——歸因:prep dossier(graphrag-ba6b-prep,quota 等待期寫好、merge 後已刪)把 #60 的教訓在設計期前置(options=typed vocabulary 在 shape 層、guardrail 留在 core 求 facade parity、hybrid 吃 binding 剩餘預算 sentinel-pinned、cap 先過凍結欄位描述),且 shared-helper 結構性繼承讓 #60 的 precedence/pool/typed-503 修法零成本轉移(「precedence 屬於概念」教訓首次驗證生效)。唯一 Codex finding=**class 1 省略≠null 第五面(request 側 optional OBJECT 欄位)**:`options: GraphOptions | None` 把顯式 JSON null 折進省略 → 靜默跳過 graph mode,而凍結契約的 options 是 optional 非 nullable object——#43 config→#47 block→#53 whole-body→#55 emission 之後又一位置;§7 nullability 矩陣 bullet 明寫「× every field 雙側」,漏在**應用**不在目錄:**新 request model 的每個 Optional 欄位都要在設計期跑「null 在契約裡合法嗎?」一列(prep dossier 該含這一列)**;同輪 sweep 驗出 top_k 契約 nullable 合法、filters/graph-options 已拒 null、other_entity=MCP tool parity 合法。**本地 reviewer 兩次高價值介入**:(a)parity nit(GraphOptions min_length=1 讓空字串在 REST 400、在 MCP in-envelope——兩門面唯一分岔點)主動採納為修,預判掉一輪 Codex facade-parity;(b)**FAIL 一輪:我的回歸 guard 用 entity="  "(空白)——舊 min_length=1 也放行,revert-probe 不會 fail=false-green**;修=改用「舊碼恰好會拒的值」""(空字串)+補 template="" 案例——**判別性紀律的 fixture 面:guard 測資必須取「修法前後行為不同」的精確值;reviewer 以 revert-probe 執行驗證(執行級審查再得分)**。輪次:…→2(#59)→4(#60)→**1**。Console v1 後端剩 BA7 health、BA8 builds。

**#62(BA7 health/metrics/eval endpoints,4 輪 Codex——R1 2 P2、R2/R3/R4 各 1 P2,全 must-fix;本地 reviewer 2 nit 其一遭 Codex 升級+4 re-stamp;Console v1 後端剩 BA8)**:R1a(**class 13 正式化**,見上)Vectors/Graph route deps 在 404 gate 前解析;修=providers 進 core(`health_report(conn, project, *, vector_provider, graph_provider)`,只在 drift-probe 分支 invoke、session probe-scoped),判別 pin=raising provider+missing project→404 + bootstrap/failed-newest 的 must-not-acquire 結構 pin。R1b(class 1)bool⊂int:`{"failed": false}` 讀成 passed=true——**本地 reviewer 已註記為 non-blocking、Codex 升級成 P2(#47/#57 undercall 亞種:值域 bug 沒有 non-blocking)**;修=共用 `_score()`(bool 先擋)餵**同檔兩個 §20 readers**(endpoint+light,class-5 防分岔);lifecycle._eval_gate 的第三面(_score_of+failed_cases)有意延後、已記為 follow-up。R2(class 6)§19 點名的 source count 不在 producer——**spec 點名「清單」時要逐項 diff 清單 vs 產出 keys**(設計期讀了 §19 的語意、沒把指標清單當 checklist 跑);sweep 再抓到第二個缺席 sibling `last_success_build`。R3(class 5,#53 R1「不信複述」姊妹=「引用了但沒讀完」)`passed` 文檔宣稱「gate 的謂詞」卻只轉錄 failed>0 一半——gate 還有 unscored fail-closed 半邊;`{"failed": 0}` 無 score 讀成 passing 而 activation 會拒;修=完整謂詞(malformed→null;failed>0→false;unscored→null;else true);**reviewer 第二次自錄同型失誤(接受了半轉錄的謂詞沒有對照 gate 全文)**。R4(**#59 R1 再犯,換軸**:status 軸 #59 統一過,這次是 SCOPE 軸)review counts 全專案計數 vs Console queue 只服務 active build → archived/failed build 上的孤兒列點亮 Needs review 卻無可清項;修=build-scoped 三計數鎖 active.id(無 active→0)、proposals 無 build_id 維持全專案;**定義重用要掃「每一條軸」:謂詞、scope、ordering——一軸統一≠定義統一**。**元教訓(流程,最重要)**:BA6b(有 prep dossier)=1 輪 vs BA7(跳過 dossier 直接開工)=**4 輪、5 個 finding 全是目錄已有 class 打在未盤點的面上**——dossier 不是 quota 等待的副產品,是設計期 per-surface inventory 本體;**每個 API/surface 任務 first push 前都寫一份快速 audit(§ 指標清單逐項、引用謂詞讀全文、每個 Optional 欄位的 null 合法性、每個 dep 的解析期做工、既有定義的每條軸)**。輪次:…→4(#60)→1(#61)→**4**(5 finding)。follow-up 備查:lifecycle._eval_gate bool-hardening(兩面)。

**#63(BA8 builds list/get/activate/rollback,0 輪 Codex 首審 +1(quota 重置後);本地 reviewer 1 FAIL(blocker)+1 nit 採納+2 stamp;Console v1 後端 BA0–BA8 全部完結)**:prep audit(graphrag-ba8-prep,寫 code 前完成、merge 後已刪)在設計期攔下三個陷阱,每個都省一輪 Codex:(1)**targeted rollback**——合約點名 build("the given build is active again")而 core 的無參 rollback() 自選前一版;接 core.rollback() 就是把 path param accepted-and-ignored(#60 R1 的 mutation 版);(2)**交易所有權 × §27 原子性(class 12)**——core activate 自管 conn.rollback()+begin(),與 Conn yield-dep 和冪等 reservation 都衝突;解法=新公開 seam `activate_in_caller_txn`(preflight→project lock→_promote_in_tx 全在 caller 交易內;core 自己的 activate 包同一 seam——run_bounded_query 先例再用),reservation+lock+promotion 在 request 交易內原子 commit/rollback:replay 能在自己造成的狀態變更後照常重放(e2e 實證)、失敗 RAISE 不毒化 key、單一 pool 連線;(3)**gate bypass**——§20 eval gate 只對 archived(history-restore、已驗證過)目標豁免,ready 目標走 /rollback 仍過 gate,否則 /rollback 就是 §20 後門;preflight 加 apply_eval_gate 旋鈕(deferred 文字沿用 core rollback 的)。**本地 reviewer 1 blocker=class 11「seam 前段落」face 再現**(mandatory drift probe 的 Neo4j/Qdrant outage 掉 generic 500)——**#60 retro 寫進 §7 的那句話這次被 reviewer 執行出來=防線閉環的實證**(目錄→checklist→本地攔截,0 輪到 Codex);修=_STORE_OUTAGES→typed 503(fail-closed,雙端點判別 pin)。nit 採納=BuildInfo.project 去掉 "" default(必填),positional 消費者一掃(recorder fake row 加寬)。**經濟學三點連線:BA6b(dossier)=1 輪 → BA7(跳過)=4 輪 → BA8(audit)=0 輪——prep audit 從「建議」升格為 FE 階段起的固定第一步**。流程:quota 中斷一次(probe 自動收斂);merge 沿用 owner 標準授權。輪次:…→1(#61)→4(#62)→**0**。BA 家族收官;下一階段=H10(browser-QA hook)→ FE(owner 定案:FE 任務 e2e 後加 Claude in Chrome 真瀏覽器操作測試,LOOP.md step 4 FE-only 段,見 graphrag-fe-browser-qa)。

**#64(H10 browser-QA receipt hook,~24 輪 Codex + owner 2026-07-12 砍回範圍 + 砍回後 3 輪 → 最終 DROP、PR closed unmerged;C1b 之後最貴、且首次由 owner 喊停並廢案)**:任務本體 = FE 任務加 Claude in Chrome browser pass、用 tree-bound browser-QA receipt 機械強制(~250 行早完成)。但 review 一路打在**既有 push-gate 的命令文字分析**上,每輪一個 bypass(head flag → attached shorthand → quoted/ANSI-C → brace/process substitution → aliases → alias chains → flag-prefixed chains → config-env → option abbreviations → tag spellings → …),每個都對、修法都成立,但表面無界(見 class 14)。**owner 介入**:`AskUserQuestion`→「砍回 H10 範圍」,grammar 修補全 revert(hook 669→175)、結構性修法立 H12(pre-push hook),公開留言誠實揭露 revert。**砍回後那道不可下推的 guard(`gh pr create --head` ban,H12 永遠看不到它因它跳過 push)自己又走 3 輪**,全是「這道 guard 賴以生效的兩機制(engagement 偵測＋ban 比對)也各是命令文字表面」的實例:(1) cluster `-fH`;(2) quoted/ANSI-C(`--he"ad"`、`$'--head'`)→ 刪 `"'\$` normalize;(3) **P1 attached shorthand `-RCLYEH/graphRAG`**——engagement 的 flag-token 類別 `[A-Za-z-]+` 停在 `/`,在 receipt/ban **之前**就 exit → 繞過。修法＝flag-token 建模成 `[^space]+`(bash 交給程式的 token),一次收整類。**最終 owner 決策:整個 H10(＋H12)DROP**,browser-QA 規則改回 honest-agent 紀律(證據附 PR、owner/Codex 抽查;LOOP step 4 + [[graphrag-fe-browser-qa]] 已同步)。教訓對偶:**green pin 在「引擎飽和」(receipt 齊全 repo 回 0)下驗＝盲於 engagement,要用判別性 pin(engaged 才 DENY 的取值)**。**class 14 為本 PR 立**(四對偶:無下游接縫的殘留、建模而非列拼法、deny 配 accept pin、知道何時把機制整個丟掉)。額度教訓:站錯層時再多正確 finding 也不收斂——先問「非得用會引出無界表面的機制強制嗎?降級成紀律會怎樣?」。輪次:…→0(#63)→**廢案(#64)**。下一階段=FE0(app shell + OpenAPI codegen + project switcher)。

**#65(FE0 app shell + OpenAPI codegen client + project switcher,3 輪 Codex——各 1 P2 皆 must-fix;本地 reviewer 1 nit 採納為修 + 2 stamp;FE 家族開場、首個前端任務、首個消費凍結 OpenAPI 契約產碼的門面)**:三個 finding 兩條線。(A)**contract 字串→URL 段的 class-14 再犯(FE 版)**:project key 契約=任意非空字串(store 測試用 slash/unicode/dot),塞進 `/p/:project` 路由段。R1 只修 URL-reserved 字元(encodeURIComponent),R3 才露 dot-segment(`.`/`..` 被瀏覽器在 React Router 看到前正規化掉 → stranding NotFound)——**逐 hazard 修就是 class-14 的 whack-a-mole**(class 9「枚舉整個面」母題的對偶 class 14「面無界時枚舉是錯工具」的 encoding-into-constrained-sink 亞種:reserved char → dot segment → 下一個)。結構性答案＝**opaque total encoding**:base64url(字母表 `[A-Za-z0-9_-]`,無 URL-reserved、無 `.`、不切段)讓任意非空 key round-trip、殘餘表面歸零,三處建構點(RootRedirect / ProjectSwitcher / test helper)收斂到單一 `encodeProjectSegment`/`decodeProjectSegment`。**耐久教訓:把「無界輸入域的值」(契約=任意字串)映進「受限 sink」(URL 段 / 檔名 / SQL identifier / shell arg)時,第一輪就用 opaque 全編碼,別逐一 escape hazard**——診斷信號同 class 14(同一轉換連 2+ 輪、每輪一個新特殊字元、你在預測**另一個解譯器**〔瀏覽器 URL 正規化〕怎麼讀輸入)。(B)**分頁靜默截斷**(R2):switcher 停在 `/projects` 第一頁 → 使用者落在第 2 頁的 project URL 時 select 空白、掉資料;修=`next_cursor` 迴圈讀到底。歸 class 11「請求級不變量掃全生命週期」讀側小親戚(「列一頁」≠「列全部」)/silent-truncation:凡消費分頁 API,消費 cursor 到底,別只取 page 1。(C)**本地 reviewer nit 採納為修(preempt)**:`decodeProjectSegment` 的 `TextDecoder` 預設非 fatal → base64-valid-but-invalid-UTF-8 解成 U+FFFD 垃圾 key 而非 undefined,註解過度承諾;修=`{ fatal: true }` + 判別 pin(`"_w"`=byte 0xFF、合法 base64url、非法 UTF-8 → undefined)——**#33 R4b「no change required 是 reviewer 客氣、指向正確性/精度的 nit 立即修」再度執行**,且屬 fail-loud(畸形段落正規化成單一 unknown 訊號、不進垃圾值)。**流程三則**:(1) **FE 首個任務=新 surface class(React/router/typed-fetch 門面),但 encoding 這條設計期可預見**(「project key 任意字串 → 進 URL」該當場觸發 opaque 編碼決策);FE0 跳過 prep dossier、3 輪才收斂——**#63 的 dossier 經濟學延伸到 FE:FE 任務 dossier 要含「每個 contract 值進 URL/DOM/儲存 sink 的編碼決策」一列**。(2) **FE browser-QA honest-agent step 首次執行**:e2e 綠後跑真瀏覽器 pass;Claude-in-Chrome 的 Chrome 連不到 localhost(extension 顯示 error page、curl 200)→ 依 browser-automation 指引 ~4 次後停手,fallback 到 Playwright Chromium 打 live dev server+API(3 截圖、零 console error),**PR body 透明揭露用了 fallback 而非 Claude-in-Chrome**(honest-agent gate 精神=證據＋誠實揭露限制,見 [[graphrag-fe-browser-qa]])。(3) **FE 工具鏈語意備忘(class 4 前端亞種)**:openapi-typescript peer(ts@6 vs peer ^5)用 package.json `overrides` 解(非 `--legacy-peer-deps`,npm ci 驗證);openapi-fetch 在 createClient 綁 `globalThis.fetch` → 測試 mock 在 `api.GET`(vi.spyOn)非 global fetch;vitest `globals:false` → RTL auto-cleanup 不自動註冊,setupTests.ts 要顯式 `cleanup()`。輪次:…→0(#63)→廢案(#64)→**3**(#65)。FE 家族開場;下一站 FE7(Project Health home)。無新 class;class 14(contract-value→constrained-sink 的 opaque-encoding 亞種)、class 11(分頁讀側)、class 9(fatal-decode 的 fail-loud + deny 配 accept/reject 判別 pin)各得亞種/實例。

**#66(FE7 Project Health home,2 輪 Codex——各 1 P2 皆 must-fix;本地 reviewer 2 輪 PASS 無 blocker + 3 stamp;第二個前端任務、首個消費 API 的頁面)**:兩個 finding 是**同一個 value「project key」流進「第二個 sink」的 class-14 再現**——#65 把 key 塞進 **route sink**(React Router)用 base64url 結構性解決,但 **API-path sink**(`GET /projects/{project}/health`)是**不同 sink、不同 decode 擁有者**:route 由我方 encode+decode 故 opaque base64url 可行;API-path 的 decode 屬**伺服器**(它要**字面 key**),opaque 編碼不可用 → 只能**拒絕**不可定址的 key。(R1)`.`/`..` key:`encodeURIComponent` 不動 dot,URL parser 在路由前把 dot-segment 正規化掉(實測 `.`→`/projects/health`、`..`→`/health`;`%2e`/`%2E` 亦然,無編碼可存活)。(R2)含 `/` 的 key(`a/b`):`encodeURIComponent`→`%2F`,ASGI 伺服器在路由前 decode 回 `/`,單段 `{project}`(非 `{project:path}`)接不到(實測 `GET /projects/a%2Fb/health`→**404**)。round 2 **從 transport 機制推導出完整不可定址集**(不追拼法):`isPathAddressable = key!=="." && key!==".." && !key.includes("/")`——這是**全集**(其餘字元 percent-encode 成非-dot `%XX`、decode 回自身、`[^/]+` 接得到,故 query/hash 字元、空白、unicode、`%` 全 round-trip),配**正向 acceptance pins**(`a?b`/`a b`/`%2e`/unicode 皆 accept,證不過度阻擋——class 9 對偶)並向 Codex 明述窮盡性 → 一輪收、無 round 3。修法=`useHealth` 對這些 key 保持 disabled(不發注定 404/錯 endpoint 的請求),頁面誠實報「不可定址」而非 generic error。**過程教訓(2 輪的真因,最重要)**:FE7 的 prep audit **有**寫 class-14 encoding 那一列,但我用**推理**填錯——寫「API path param 由 openapi-fetch encode,無新 hazard」,卻沒**實測**(a)`encodeURIComponent` 不編 dot、(b)`%2F` 會 404;round 1 才 probe URL、round 2 才 probe 404,兩次都是「本該 first push 前就 probe」的補課。**dossier 的 transport/encoding 宣稱必須 execution-verify(探真正發出的 URL/bytes),不能推**——這是 #62/#63「dossier 省輪」撞上「執行級驗證/舉證義務」:寫了 dossier 不夠,dossier 裡「client library 幫我處理了」這類 encoding 宣稱最該親手 probe。**class-14 精煉(多-sink×decode-擁有權)**:一個 value 常流進**多個 sink**;在 sink A 的結構性修法**不自動涵蓋** sink B(#65 修 route ≠ 修 API-path)——(甲)先**枚舉 value 流進的每一個 sink**(class 9「枚舉整個面」母題套在 sink 集上);(乙)每個 sink 的結構性修法**依 decode 擁有權而異**:我方擁有 decode → opaque encode(base64url);對方(伺服器)擁有 decode 且要字面值 → 只能 refuse;(丙)每條 sink 的 encoding 宣稱都要 execution-verified。**根因(flagged 給 owner)**:契約以**單一 URL path segment** `{project}` 定址專案,但 key domain=「任意非空字串」⊋ path-addressable 集——`.`/`..`/含`/` 的 key 可建立、可在 route 開啟、卻無法經 REST path 定址,這是**跨整個 `/projects/{project}/*` 面**的 latent gap(jobs/review/playground/builds 皆然);真正結構性修法=**DR-002 契約決策**(建立時把 key 限制成 URL-safe 字元集,或改用非-path 承載 key),超出 FE 任務範圍,reply 已標 follow-up。輪次:…→廢案(#64)→3(#65)→**2**(#66)。FE 家族:FE0→FE7 落地;下一站 FE8(Pipeline/jobs 儀表板)。無新 class;class 14(多-sink×decode-擁有權)、class 9(sink 集枚舉 + acceptance pin 防過度阻擋)得亞種;process=「dossier 的 encoding 宣稱須 execution-verify,別信『library 幫我處理了』」。

**#67(FE8 Pipeline/jobs 儀表板——builds runs table + live job SSE progress,2 輪 Codex——各 1 P2 皆 must-fix;本地 reviewer 主體 1 FAIL(blocker,useJobStream teardown 零斷言)後 PASS + 本輪 abort-race 修法 1 PASS;第三個前端任務、首個「live 訂閱通道 × REST 快照」門面)**:兩個 finding 都打在**同一個新 surface「SSE live 通道消費者」(useJobStream/JobWatcher)**、都是目錄已有 class 的 lifecycle 亞種——(R1,U8P)**live delta 通道是全資源的有損投影**:`JobEvent` 只載快變欄位(status/step/progress/message),終端才有的欄位(build_id/error/finished_at)**永不經串流送達**;watch 在 job 完成前開始 → 串流關閉後畫面停在 watch 時的 GET、缺失敗詳情 → **class 5「通道完整性」亞種**(通道存在≠載了消費者要的欄位)；修法=`stream.status==="closed"` 時 refetch 快照補終端欄位,判別 pin=running 快照(build_id/error null)→ `job.failed` 關閉串流 → refetch 後斷言 `error.message="boom"`/`build_id="b-final"` 現身;附帶把 load-error 分支 gate 在 `isError && !job`,免得失敗的 post-close refetch 蓋掉已載入的 job。(R2,X11)**async teardown 的中止旗標必須守住被拆生產者的每一個發射點,不只終端 settlement**:effect cleanup `controller.abort()` 後,一個**已 resolve 的** `reader.read()` chunk 的 continuation 仍會跑、在**新 job 的串流已啟動後**呼叫舊 job 的 `onFrame`→`setEvent`;因 hook 實例與 setEvent 跨 jobId 變更留存,stale frame 覆蓋 job-2 的 reset 狀態(新 job 顯示舊 job 的進度直到下個真 frame)——`.then`/`.catch` settlement 已守 `signal.aborted`、**per-frame `onFrame` 沒守** → **class 10 TOCTOU/lifecycle 亞種**(拆解旗標要 gate 每個 sink-write、不只終端一個;亦是「第二寫入路徑」——守了 settlement 那條 ≠ 守了 onFrame 這條);修法=`onFrame` 開頭 `if (controller.signal.aborted) return`(與 settlement 同一旗標),讓 hook 兌現自己註解已宣稱的「cleanup 後無 state 落地」不變量;單一 hook 級 guard 同時關掉兩種措辭(event reset null + late onFrame 短路 → JobView 收不到 stale non-null event 去 overlay,毋須 JobView 端 job_id 檢查);選 hook 級而非 transport-loop guard 讓 `jobStream.ts` 保持 React-agnostic;判別測試「switch job 後丟棄舊串流的 late frame」捕捉 stream-1 的 onFrame、切 job-2(event→null)、投遞 late job-1 frame、斷言 event 留 null,**revert-probe 驗過拿掉 guard 即 FAIL**。**元教訓(最重要)**:兩個 finding 同 surface、同「lifecycle 完整性」母題的兩面——**live 訂閱通道 × 快照**是可預見的 completeness checklist(delta 通道漏了哪些欄位?通道關閉時怎麼 reconcile?訂閱在 flight 中被拆時哪些寫入路徑還活著?)——FE8 又跳過 prep dossier(FE0/FE7 同)得 2 輪;**dossier 該含「每個 live/delta 通道對 full 資源的欄位差 + teardown 競態面」一列**(#63 economics 在 FE 的第三次印證)。**流程二則**:(1)**revert-probe 用 `git checkout <file>` 會連未提交的 sibling 修法一起還原到 HEAD**——本次 sed 拿掉 guard 驗 FAIL 後 `git checkout` 把整個未提交的 fix(含 guard)一起清掉;靠 system reminder + 重新 grep 抓回、重新套用;**教訓:要 revert-probe 未提交的改動別 `git checkout`(它回 HEAD、丟掉整段未提交),改成只還原被 probe 的那一行、或先複製檔案/`git stash`**。(2)**接手 stale summary 時 repo 實況(branch/PR/CI/Codex 狀態)是 ground truth**——本 session 從摘要接手時分支/PR 皆已推進(摘要說「current work=完成 #67 refetch reply」,實況是 refetch 已修已推、Codex 又新增 abort-race 一輪),先 live 查證(git branch、gh pr view、reactions、reviewThreads)再動作,不照摘要盲跑。輪次:…→3(#65)→2(#66)→**2**(#67)。FE 家族:FE0→FE7→FE8 落地;下一站 FE5(實體解析審核 UI,消費 BA5 merge-candidate 端點)。無新 class;class 5(live 通道欄位完整性)、class 10(teardown 旗標守每個發射點/第二寫入路徑)各得 lifecycle 亞種;process=「live-channel×snapshot 的 completeness checklist 入 dossier;revert-probe 別用 git checkout」。

**#68(FE5 實體解析審核 UI——merge-candidate 佇列 + approve/reject/defer,2 輪 Codex——各 1 P2 皆 must-fix;本地 reviewer 1 nit(TERMINAL 集只測半)採納 + 3 個 delta 用 SendMessage 回同一 agent re-review/re-stamp;第三個前端頁面、首個「寫入 + build-scoped 分頁讀」門面)**:**FE5 有寫 prep dossier,其涵蓋的 6 個 hazard(§17 gating、encoding sink、fail-loud、active-build 空佇列、status-vs-decision、cursor 讀到底)全部 0 finding**——但 2 個 Codex P2 都落在 **dossier 沒列的「可靠性/生命週期」維度**,都是 class 11「請求級不變量掃全生命週期」的亞種:(R1)decision mutation 沒送 `Idempotency-Key` → 回應在 server 已 commit 後遺失、curator 重試=新 POST 打在已決議的 candidate → illegal-transition 400 → **把成功的審核誤報為失敗**;修法=deterministic key `${candidateId}:${verb}`(§17 讓每個(candidate,verb)一次性,故自然鍵跨重試穩定、零額外 state,endpoint replay 已存的 200;**random-per-click 不修此 bug**,因每次手動再點=新 mutate=新鍵)——正確性依賴 BA5「replay 先於 scope/candidate precheck」(class 11,BA5 task line 已宣稱;reviewer 點出這條跨層依賴,FE 端只需送鍵)。(R2)build-scoped 分頁讀:endpoint 每請求 re-resolve active build,分頁途中若 activate 新 build → 迴圈把 page1(舊 build)接上 page2(新 build)=混版佇列,stale 列 decide 時 404;修法=pin 首頁 `meta.build_id`、後頁不符即 fail-loud(retry 拉乾淨單 build 快照;`undefined`=未見過 sentinel,build_id 本身 string|null,不會混淆)。**元教訓(最重要)**:dossier 在**它列到的維度**上把輪次壓到 0(6 hazard 全清),2 輪全來自**未列舉的維度**——**FE dossier template 要加一列「可靠性 affordance」:每個 WRITE→retry-idempotency(契約的 Idempotency-Key header)、每個 build-scoped PAGINATED read→跨頁 scope 一致性(meta.build_id 版本標記)、每個 cursor→讀到底**。診斷句:**凡契約在請求/回應上暴露 reliability affordance(idempotency header、meta 裡的 scope/version 標記、cursor),就是擺明要你在該生命週期消費它;沒消費=lifecycle-invariant 缺口**——這是 #65「pagination 讀側(list 一頁≠全部)」的深化(不只「頁數」,還有「跨頁的 scope 版本」與「寫入的 exactly-once」)。本地 reviewer nit=TERMINAL 集 {approved,rejected} 只測 approved(#17/#27「全稱測試枚舉全部實例」再犯,reviewer 執行級抓)→ 改 `it.each(["approved","rejected"])`,掉任一成員即 FAIL(判別性)。**follow-up(flagged 給 owner,非 FE5 範圍)**:merged FE8 的 `useCancelJob` 同屬「寫入無 idempotency key」class——留在未動的既有碼,surgical 不折進 FE5;若要 console 寫入面一致 retry-safe,值得開後續小任務(reviewer 主動掃出的 same-class sibling,但正確地判為 out-of-scope)。**流程三則**:(1)3 個 delta(nit 修、idempotency、build-swap)都用 SendMessage 回**同一 reviewer agent** re-review + re-stamp receipt——context 保留、每次快、判別性 revert-probe 由 reviewer 執行,比每次開新 reviewer 省;(2)eyes-no-poke 全程守(從不 poke,每次 push 自動觸發 Codex re-review);(3)browser-QA(Claude-in-Chrome 連不到 localhost → Playwright Chromium fallback,零 console error,§17 gating〔pending 全開/deferred 禁 Defer/approved 全禁〕在真瀏覽器目視驗證+截圖)證據入 PR body,誠實揭露用 fallback。**接手 stale summary 的教訓再驗**:本 session 開場摘要說「current work=貼 #67 refetch 回覆」,實況是該修早已推、Codex 又新增一輪 abort-race——先 live 查證(gh pr view/reactions/reviewThreads/git branch)再動作。輪次:…→3(#65)→2(#66)→2(#67)→**2**(#68)。FE 家族:FE0→FE7→FE8→FE5 落地;下一站 FE6(查詢遊樂場,v1 前端收官)。無新 class;class 11(write-retry idempotency、multi-page scope 一致性)得兩 lifecycle 亞種;process=「FE dossier 加 reliability-affordance 列;契約的 reliability affordance 擺明要在生命週期消費」。

**#69(FE6 查詢遊樂場——五模式 + 引用 + §22 warnings + routing trace,1 輪 Codex——1 P2 must-fix;本地 reviewer 1 PASS + 3 個 delta〔2 nit + Codex 修〕re-review/re-stamp;第四個前端頁面,v1 Console 前端收官)**:**最複雜的 FE 任務(契約⇄runtime 分歧:codegen 用單一寬鬆 QueryRequest,runtime 五個 per-mode `extra="forbid"` 模型按欄位 PRESENCE 拒絕),卻只 1 輪**——歸因:dossier + **execution-verify(先讀 `api/schemas.py` 四個 Pydantic 模型再寫 body,#66 教訓)**把最硬的 request 面(`queryBody` 條件式含鍵、禁止欄位一律 OMIT 不送 null;graph 丟 top_k、semantic/sql/global 不送 options、hybrid options 省略非 null)一次做對、0 finding。唯一 Codex P2 落在 **response 顯示保真度**這條 dossier 沒點名的維度:source_ref.id 我用 `slice(0,8)` 截斷,對 uuid 安全,但 SQL/row 引用的 id 是 core `row_source_ref(table,pk)` 的**無損 `table:pk` 複合字串**(如 `9:customers:12345`),截成 `9:custom` 丟掉 pk、令同表兩列引用無法區分 → **破壞 §16 引用可溯性**;修法=顯示完整 id(uuid 與 table:pk 一致覆蓋,毋須特判 metadata),判別測試 row ref `9:customers:12345` 須完整現身(slice 會渲染 `9:custom` 而 FAIL)。**教訓(class 1 identifier-domain 的顯示側亞種)**:**截斷一個識別子只在其 domain 是 opaque 定寬(uuid)時安全;對變寬/複合 id(table:pk)截斷即有損 —— 顯示 id 前先確認其 domain,CITATION 尤然(可溯性是不變量,寧顯全長)**。與 FE5「project key domain=任意字串 ⊋ path-addressable」、FE6 自身「source_ref.id domain=uuid ∪ table:pk」同一母題:**契約的 id/值 domain 常比我渲染/處理的假設更寬**。**元教訓**:dossier + execution-verify 把投資的維度(request 契約分歧)壓到 0 finding,唯一輪次來自未點名的顯示保真維度——**dossier 的 class-14「值進 sink」列要擴一格:不只 source_uri→href sink(注入),還有 id→顯示截斷(有損/碰撞),特別是 citation**。**兩個本地 nit(reviewer 先抓、push 前修)**:(1)hops 空字串 `Number("")===0` 會送 hops:0(不一致 top_k 先守空)→ 先守 `hopsText.trim()!==""`;(2)`queryBody` graph 分支的 required-options 不變量活在 form(canRun 閘 entity)非函式→補「graph 模式 entity 空時 Run 禁用」判別測試。`Placeholder.tsx` 現無 importer(review/playground 皆真頁),留作 v2 路由(FE1–4)休眠 scaffolding(Rule 3 不刪既有碼;reviewer 記為決策非疏漏)。browser-QA(Playwright Chromium fallback,零 console error;結果+引用+LOW_CONFIDENCE warning+routing trace+source_uri-as-text+graph 模式欄位切換皆真瀏覽器目視驗證+3 截圖)入 PR body。**QA 工具語意備忘(class 4 前端亞種)**:Playwright `getByLabel` 對 `<select>` 會把 option 文字併進 accessible name,故 `getByLabel("mode")` 逾時不中(jsdom/RTL 算成 "mode",故 vitest 測試照過);QA 腳本改用結構定位 `.play__form select`——**同一 UI、兩個測試框架的 accessible-name 計算不同,是工具語意差異不是 bug**。輪次:…→2(#67)→2(#68)→**1**(#69)。**FE 家族收官:FE0→FE7→FE8→FE5→FE6,v1 Console 前端(health/jobs/review/playground)全部落地;FE1–4 為 v2**。無新 class;class 1(identifier-domain 顯示側:截斷對複合 id 有損)、class 4(Playwright getByLabel×select accessible-name)各得亞種;process=「dossier class-14 列擴含 id→顯示截斷;顯示 id 前確認 domain,citation 顯全長」。

**#70(FE1 Import,26 findings=25 修+1 decline、19 輪本地 re-review、Codex quota 中途燒盡一次;v2 FE 首任務)**:單一頁面被 Codex 掃出**三條完整鏈**。(一)**「FE 鏡射後端實際行為」鏈**:契約的示意文件全不可信 — source kinds 實際=text/structured(非契約 doc 的 file/directory/url/database)、ingest 與 build 是同一條 full pipeline(只差 job kind 標籤,copy 不能說 stage-1)、text 要 file:// **目錄**(NotADirectoryError)、structured 要 CSV+table/pk_column、ontology 缺失死在 graph 階段;dossier 讀了 api/schemas.py 卻沒讀 connectors/stages/config——**FE dossier 必須讀到 worker 實際消費層**,否則每個沒讀的檔案是一輪 finding。(二)**display≠read uri 家族**(host-bearing、四斜線、%2F、query/hash、raw/encoded dot segments、C0 controls、edge whitespace、NUL,共 8 輪):最終收斂=驗證 **backend-derived 值**(urlparse(raw).path 的逐字節鏡射)而非 browser-normalized url.pathname——WHATWG 每一步靜默正規化(dot-segment、tab/LF 剝除)都是 mirror 的 divergence point(class 5 稅);run gate 判準=「會讀到 operator 註冊的那個路徑」而非「不會失敗」(silent wrong-data 比 loud failure 更糟——一次翻案)。(三)**fail-closed gate 演化鏈**(class 10 stale-data-during-refetch 變體):data 存在 → +!isFetching(post-add invalidation 窗口)→ +!isError(失敗 refetch 保留舊 data 且 isFetching 落回 false)→ ontology 三態(absent 只 gate text;present-invalid gate 全部)。**流程**:19 輪=額度經濟學反例(對照 #20 的 0 輪);兩次翻案(trigger no-key、%2e%2e 收斂論)+一次成功 decline(full-config mirror,reviewer 獨立驗證 stages 不碰 raw config)→ 立 class 15。後端 follow-up(owner 未決):_local_path 對 display≠read uri 直接 raise(Console gate 蓋不到 CLI/API/MCP;%5C 亞種 client 根本無從判斷 worker OS)+ ontology-configuration UI。(原素材檔已於 2026-07-18 memory 大掃除移除;懸置 follow-ups 見 [[graphrag-open-followups]]。)

**#71(BA9 canonical source-uri at the SoR + cancel idempotency;6 條 Codex P2 + 5 輪本地 FAIL,全部同一 class → 立 class 16)**:#70 留下的後端 follow-up 落地。**Codex 的六輪**依序=relative `file:../x` → `%5C` Windows separator → `%2F` encoded slash → `|` drive alias → 單斜線 `file:/x` → driveless path 在 Windows worker 上是 drive-relative;**前四輪我都只關掉「那個拼法」,下一輪同一個機制換個拼法回來**——直到本地 reviewer 去讀 `nturl2path` 原始碼、發現第一句是 `url.replace(':','|')`(pipe 不是 drive separator 的特例,**它就是 drive separator**),才把「舉例關」換成「讀源碼列出三軸(resolver/decoder/parser)並 by construction 關上」。**轉折點的證據**:改讀源碼之後,本地 reviewer 又抓出 **5 個同族缺陷、Codex 一個都沒看到**(colon 本體=`file:///data/foo:bar` 讀成 `O:bar`、`%3A` encoded drive separator、malformed `%`、hex 合法但非 UTF-8 的 `%FF`、裸 drive `file:///C:`)——枚舉拼法時我們永遠落後 Codex 一輪,讀源碼之後我們跑在它前面。兩個 finding 不是 display≠read 而是 **split enforcement**(讀到的路徑正確,但兩道 gate 接受集不同 → 用 API/CLI 註冊的 source 在 CLI 建得起來、在 Console 永遠建不起來且擋掉全 project 的 build)——修法=**兩套 suite 共讀一份 corpus**(`tests/fixtures/canonical_file_uri.json`),把 parity 從「宣稱」變成「機械強制」;而 `file:///C:`(兩邊都收、`Path("C:")` 卻是 worker 的 cwd)證明**純 differential 對共有缺陷失明**,必須配 display==read oracle。最後一輪最微妙:driveless path 在 Windows worker 上 drive-relative,但那是 **POSIX 的正規形式**(deployment 就是 Linux)→ 不能用 shape rule 修,只能**驗 resolved path 在「跑這段程式的 OS」上是否 absolute**,因而是**唯一一條 Console 鏡射不了的規則**(browser 不可能知道 worker 的 OS)。parity 因此不是「破了」而是「精確化」:POSIX(deployment)上兩邊接受集**完全相同**;Windows 上 SoR 是 Console 的 **strict loud subset**,致命方向(SoR 收/Console 拒)為空。本地 reviewer 五次 FAIL 全部正確,包含**推翻它自己上一輪的建議**(`"C:" in resolved` 對 driveless `\C:\corpus` 也成立=pin 驗不到它自稱防的 regression);且它坦承 finding #7 是**它自己四輪前把 oracle 調弱**(`is_absolute()` → `drive and not root`)藏起來的。細節見 class 16。

**#72(FE3 檢視 Inspect——文件/chunks 兩 tab、opaque cursor「Load more」、row-click detail;4 條 Codex P2(3 輪:R1/R2 各一條、R3 一輪兩條)+ 本地 reviewer 3 FAIL(全 blocker)+ 2 nit 全採納,pre-PR 另 2 FAIL(§10.2 四 tab 超規格→砍成兩 tab=class 6;sort/filter 不變量只斷言單 tab=false-green,class 1 全稱枚舉再犯);v2 FE 第二任務 → 立 class 17)**:整個 PR 是**同一個問題**——「快取的列何時可示人?」——被沿狀態機逐格回答:(R1,Codex)`isError && !data` 把 refetch 失敗誤標成 load-more 失敗 → 修成 `isFetchNextPageError`(源碼驗證:fetchMeta 每 fetch 重設,`isRefetchError` 會漏 initial-load)→ 本地 FAIL:Detail 元件同款未修(list refetch 成功而 detail 404 時,舊 build 的 raw 在錯誤訊息下面照渲染)→(R2,Codex)load-more 收到 409 NO_ACTIVE_BUILD 仍 keep rows=**軸選錯了**(「哪個請求失敗」≠「build 是否還活著」)→ 本地 FAIL:`_bind` 的第二個 exit PROJECT_NOT_FOUND 是同一格的另一個拼法 → 修法=**倒轉成 scope-neutral allowlist**(STORE_UNAVAILABLE/RATE_LIMITED/INTERNAL/QUERY_TIMEOUT+transport;additive-only enum ⇒ blocklist 會讓未來新碼默默落在 keep 分支)→ 本地 2 nit:Set typed 對凍結 enum(拼錯=編譯錯)、code-less body(proxy HTML 502)的 unguarded read 會 TypeError→transport 分支 silent keep → 本地 FAIL:該分支 unpinned(revert 後 13 綠)→(R3,Codex ×2 threads)**in-flight 窗口**:stale-while-revalidate 在 refetch 未回時照渲染舊 build(hung=永遠),list 端 `isFetching && !isFetchingNextPage` gate、Detail 端三態互斥鏈(next-page 豁免:它不重開已上屏列的問題,splice/scope guard 落地時裁決)。**判別性測試設計亮點**:detail in-flight pin 刻意讓 list refetch settle 而僅 detail hang——若兩者皆 hang,list 自己的 gate 會 unmount 整個 panel,測試不經 Detail 的 gate 也綠=非判別。**本地 reviewer 抓到 Codex 沒看到的 2 個 blocker**(PROJECT_NOT_FOUND sibling、unpinned silent branch),延續 #71 的額度經濟學:機制級收斂後我們跑在 Codex 前面。**流程四則**:(1) **土製輪詢重演 PR#5**——Codex 兩條 P2 以 review 形式 01:58 送達,我的 ad-hoc loop 只看 reactions+comments-after-timestamp,誤判「無回應」→ poke(規則正確、餵給規則的資料錯);merge hook 的 unresolved-threads 檢查當了最後一道網。教訓=**等 Codex 一律 `scripts/watch-codex.sh`**(它的註解就寫著這個 failure mode),詳 [[graphrag-ci-before-codex]]。(2) **寫入前 fresh-state check(owner 2026-07-13 協議)**:對 PR 的每個寫入(comment/resolve/poke/merge)同腳本先重查當下狀態、條件仍成立才發送——程式判斷,不 LLM 讀。(3) merge hook「+1 必須晚於 head commit」正確擋下 stale approval ride(Codex 02:08 的「no major issues」評的是前一個 head `d8a3266`;in-flight gates 於 02:45 推上後,fresh review 的 02:55 +1 才放行)。(4) doc-reviewer 抓到 README 描述**尚未 merge** 的 Inspect 頁(docs 的真值時點=落地時而非撰寫時);citation nit:activation 擋在 §14 preflight,§20 只是 threshold config。輪次:…→1(#69)→#70(26 findings)→#71(6+5)→**#72(4 Codex + 本地 3 blocker 2 nit)**。v2 FE:FE1→FE3 落地;下一站=DR-002 清洗回合(openapi 升版)→BA4→FE2,再 FE4。立 **class 17**(快取信任述詞);class 1(全稱斷言枚舉雙 tab)、class 6(§10.2 超規格)、class 10/11(其收斂即 class 17)各得亞種。

**#73(BA4 清洗預覽端點——契約 v1.1/DR-009,凍結後首個 DR-002 回合;5 條 Codex P2(5 輪,每輪一個投影面)+ 本地 reviewer 1 nit(取:config-side bool 守衛+誠實化措辭)+ 5 次 delta re-stamp;CI 紅一次(anyio 標記 × asyncio_mode=auto 雙插件 loop 分裂,本機僥倖綠——class 4)→ 立 class 18)**:端點本體(純函式、DR-006 讀取、參數 fallback 鏈、read-only RPC 不吃 Idempotency-Key)一輪過;五輪全部打在 `CleanPreviewRequest` 這**一個物件的投影疊層**上,依序=lax int bool → 顯式 null(掃寬到四欄)→ combinator 塌成 unknown|unknown → 結構 union ≠ 精確 union(`false` schema → `?: never`)→ **web tsconfig 從未開 strict**(開啟=零錯誤,免費 repo 加固+typetest 絆線)。詳見 class 18;checklist 第 8 條已落地。**環境考古(H11 立案)**:dev 服務累積數百測試殘留(全表計數斷言 × QA 資料、殘留 arq in-progress key 無限楔死 worker 測試、~300 孤兒 Qdrant collections、被 kill 的測試跑漏 teardown 留孤兒 job)——CI 每次全新服務永遠看不到,只有本機 check-full 會踩,這正是傷 loop 的位置。**流程五則**:(1) owner 的 fresh-state-before-write 協議**兩次**攔下盲寫(resolve 寫入前重查發現新 thread → abort);(2) watch-codex.sh 的錨點缺陷:它以 poke 當 ack,「已 triage 但未 poke」的事件讓它開場即 exit-10——需要 --anchor 參數(H13 立案;H12 已是 #64 被 DROP 的 pre-push hook 的專名,不再複用);(3) eyes-no-poke 破戒一次(eyes 在場仍 poke,無害但多餘——fresh-state 腳本要把 eyes 納入判斷);(4) 管道假綠兩次(`cmd | tail` 的退出碼是 tail 的;kill 掉 pytest 後鏈條照跑 e2e 回報 exit 0)——退出碼要在管道前抓;(5) probe-of-probe(見 class 18)。輪次:…→#71(6+5)→#72(4 Codex)→**#73(5 Codex,一物件五面)**。v2:BA4 落地,FE2(清洗 UI,消費 v1.1 preview + 既有 PATCH)接棒,然後 FE4。立 **class 18**;class 4(anyio×pytest-asyncio)、class 12(arq 殘留 key 楔死測試=其跨世代 key 教訓的測試面)各得亞種。

**#74(FE2 清洗 Clean——v1.1 preview RPC + 既有 PATCH 寫 rules;4 條 Codex P2(3 輪:R2 一輪兩條)+ 本地 reviewer 2 nit 採納 + 1 nit 接受(class-5 顯示鏡射)+ 5 次 delta re-stamp;瀏覽器 QA 全項通過)**:dossier 的 load-bearing 發現(PATCH 整欄替換 config,無深合併——讀 core/registry/store.py 而非假設)擋掉了最大的 wipe 事故,但四條 Codex 全部打在同一母題的遞進面上:**「頁面快取的世界觀」滲入寫入路徑或顯示宣稱**。(R1)save spread 頁面快取的 config → 幾分鐘前別人改的 config 被整包復活;修=mutation 內 fresh GET 再 spread,失敗 loud 不 fallback(pin: PATCH 不得被呼叫)。(R2a)「Saved」banner 在編輯後仍站著=假收據;(R2b)**空欄的 effective 值來自 config,而 useProject refetch 會在零 input 事件下改變它**——我的 staleness 是事件計數(markStale on onChange),對此結構性失明。修=重寫成**狀態比較**:preview 快照它跑時的 effective tuple,stale=每 render 重算 snapshot≠current;save banner 記住實際寫入的 pair、只在 effective pair 仍相符時站著——事件計數器/markStale/onSuccess 邏輯全刪,程式更少涵蓋更廣(打字/切來源/config-refetch/in-flight 全落在一個述詞)。**誠實記錄:class 17 的文字就寫著「述詞算在狀態上」,我第一版仍蓋了事件計數器**——教訓存在≠教訓被套用,retro 目錄要在動工前讀而非事後對照。(R3)fresh GET 保住 sibling blocks,但 runSave 在 mutation 前就把空欄「烤」成快取的 fallback 數字 → 別人剛改的 overlap 被靜默回退;修=raw optional knobs 傳進 mutation、對 fresh chunking 解析,且合成 pair **在 mutation 內重新驗證**(fresh fallback 可讓 typed 值非法,PATCH 不驗 chunking、會晚到 build 才爆)——寫入路徑的終局形狀:**一切(sibling blocks、fallback 數值、合法性)在 mutation 內對同一次 fresh read 導出**。殘餘的同 RTT 併發競態=class 10(recheck≠lock),真解=契約 version token(另一輪 DR-002),註解點名。**流程**:in-flight 版本守衛(本地 nit)一輪後被比較式重寫取代——nit 修「案例」,Codex R2 逼出「機制」;若第一輪就問「這個 staleness 述詞的輸入集是否完整?」(effective 值的來源=typed∪config,config 會自己動)就是一輪。輪次:…→#72(4)→#73(5)→**#74(4 Codex/3 輪)**。v2:FE2 落地,剩 **FE4**(graph/subgraph 端點已在,無契約變更),然後海科館資料。無新 class;class 17 得**寫入側亞種**(快取世界觀滲入 write path:spread、fallback 解析、驗證前提全要 fresh)、class 10 得亞種(client 寫入的 resolve-then-validate 也要在同一快照內完成);class 15 的 gate 判準再驗證(pair mirror 的 LATE-failure 論證)。

**#75(FE4 圖譜互動探索——三欄、SVG 放射佈局、無契約變更;v2 收官任務;8 條 Codex P2(7 輪,R2 一輪兩條,1 條 reply-and-resolve駁回)+ 本地 reviewer 5 nit + 2 個主動 sweep 發現 + 1 個 tree-hygiene FAIL → 立 class 19)**:端點消費面(policy-missing 具名化、hops 拒絕、evidence-on-detail、誠實 client filter)一輪過;七輪全部打在**同一個多 query 頁面的 scope/staleness 一致性**上,逐輪擴格:(R1)EntityColumn restate FE3 守衛漏掉 keepsRows 半邊(class 17(c) 的述詞漂移,發生在引用它的 PR 裡)→(R2a)scope-gone 只清左欄=頁面同時說「沒 build」又展示舊圖 → 升頁面級 →(R2b)非 active 實體可點必 404 → disable+標示(還曝露 fixture 用了 enum 外的發明狀態 "resolved");(R3)splice 成功案也要頁面級;(R4)Playwright glob 遮蔽說=**假前提駁回**(`*` 不跨 `/`;三重可查證 rationale:文件語意+綠 CI+#74 的反例);(R5)selection 可指向已不在畫面的節點/邊 → **比較式調和**(這次第一版就用對 FE2 教訓);(R6)subgraph 的 scope 失敗也是頁面級證明(code×status 分類:hops 400 是使用者輸入必須留局部——over-block 對偶);reviewer sweep 再揪 detail 404/409 同為證明且**可先到達**(點擊不 refetch subgraph)→ 主動關;(R7)`visibleSelection` 從 kept-data 導出=class 17 述詞漏了一條邊 → graph 只在 settled∧success 存在。**Pin 判別性被 probe 抓到兩次**(hang 全部會讓 detail 自己的 gate 遮蔽目標;判別形=503-subgraph×success-detail)。**Tree-hygiene FAIL**:root 跑 playwright 留下 test-results/ 被 git add -A 掃進 receipt tree——內容定址 receipt拒絕替垃圾 tree 背書(制度照設計運作);root .gitignore 補上。**Reviewer 兩次公開修正自己**(R1 漏看 unconditional isError;R5 把 R7 的情境誤判 benign)並沉澱 lens:「元件 restate 兄弟已解的守衛時,逐項 diff 述詞而非確認守衛存在」。輪次:…→#73(5)→#74(4)→**#75(7,一頁四 query)**。**v2 全部收官**:FE1 #70→FE3 #72→BA4 #73(契約 v1.1)→FE2 #74→FE4 #75。下一站(owner 指示序):使用教學(手把手+截圖,自產測試資料)→ 海科館資料適配。

歷史脈絡(2026-07-03 更新的快照,僅供追溯,不代表現況):P0/P1 契約與 H1–H3 harness(fail-loud gates、triage 規則、watcher/doc lane/收據/governance)皆已 merge;當時正要開工 P2(build/activation + Alembic)。

相關:[[codex-plus-one-merge-gate]]、[[graphrag-architecture]]

**Class 20 — 寫入生命週期的宿主 × keyed 子元件瞬時卸載(PR #76,12 輪 16 P2)**:
RQ v5 的 mutate 層 callback 綁 hasListeners——**keyed 子元件在父層資料變動的過渡
render 會瞬時 re-key 卸載**,callback 靜默消失。同一陷阱在單一 PR 咬了三次
(R4 nit:onSettled 清旗;R7:onSuccess arm 旗;R12:defer advance + onError disarm
全滅)。修到底=寫入的完整生命週期(arm/disarm/advance/error/pending)住在**不會
卸載的層級**(頁面),卡片只留 UI;過渡期補丁=「mutate 前同步 arm + 事後形狀驗證」
(R7+R8:記錄「預期消失的 id」而非布林,新佇列==舊佇列-該id 才算自家縮短)。
判準:每個 mutate callback 問「父層任何資料變動會讓我的宿主 remount 嗎?」
子教訓:(a) **哨兵狀態機要枚舉完備**——pending/快取重驗(isFetching≠isPending)/
neutral-error/proof-error/success-但-build-不符/disabled-殘留快取錯誤,每格都要有
指定語意,R9-R11 才掃完;殭屍資料(error 狀態仍保留舊 data)導出的下游探針=獨立
一格。(b) **lock test 必須實跑 revert-probe**——R11 測試假綠(404 分支是死碼),
reviewer 實測 revert 才抓到;推理「應該有鑑別力」不算數。(c) 新的有狀態互動面
=先枚舉完整狀態機再推第一版,否則 Codex 會逐輪幫你枚舉(12 輪 vs FE 平均 2 輪)。

**Class 21 — mock 必須對齊真實錯誤信封(PR #77,3 輪 4 P2+2 reviewer blocker)**:
activate 拒絕的實質內容在 `error.details.failures`,`error.message` 只是含 UUID 的
泛句——FE 只丟 message = 把死路渲染給使用者;而我的測試 mock 了「gate 字串在
message 裡」這個**伺服器從不會發的形狀**=假綠,直到 reviewer 對照 integration
test 的真實信封才拆穿。判準:**寫錯誤處理的 mock 前,先讀 server 端或
integration test 的實際 envelope**(code/message/details 各放什麼),mock 照抄
真形狀。副教訓:(a) copyable CLI 提示是三層安全問題——shell quoting(空白/
元字元)、`--` 分隔符(開頭破折號,quoting 救不了:shell 在 argv 前剝引號)、
argparse 語意;(b) 帶反斜線的程式碼產出用 Write 工具寫檔,別過 bash heredoc
(跳脫層層剝皮,`"\$1"` 變 no-op 咬過一次、heredoc 語法炸過三次),寫完驗證
**檔案位元組**而非 shell 轉印;(c) UXA2 靠 class 20 的「先枚舉狀態機」把 12 輪
壓到 3 輪——新有狀態面的前置設計成本完全值回。

**Class 22 — 翻譯即斷言 × 不變量 oracle 閉合路由表(PR #78,3+1 輪 4 P2+reviewer 首輪 FAIL)**:
全站翻譯層 sweep 的固有風險:譯錯標籤不是文案問題,是**語意錯誤**——R1a
`token_count` 譯「字元數」(單位錯 ~4x);R1b `created_by` 譯「來源」,與 §10.2 的
「來源」(evidence source_uri)撞名。判準:**譯每個欄位前讀 producer(server 端)
確認語意與單位;譯詞對照既有 UI 詞彙表查撞名**(同詞異義=class-5 分岔的新軸)。
子教訓:(a) **聚合 gauge≠可行動計數**(R2):health.pending_review 聚合 4 型,
/review 只服務 merge candidates——導流連結/卡片要 gate 在可行動分量
(pending_merge_candidates);同類掃除抓到 Overview 卡片連數字都是聚合值。
(b) **共用 selector 的 presentation 修補要先枚舉全部消費者**(R3):治數字疊行的
blanket nowrap/ellipsis 把同 class 的 drift 行 JSON 診斷剪掉——動共用 CSS 前列出
每個消費者,例外走 scoped class(`health__grid--drift`)。(c) **不變量 oracle 要
閉合整個路由表**:chromeInvariant 首版蓋 6 頁,reviewer FAIL 抓到的 3 個裸 UUID
全在未蓋的頁(Clean/Graph/Import)——有界表面=路由表全枚舉,抽樣=假綠;
且全 UUID regex 抓不到前綴洩漏(`id.slice(0,8)`),那層由 per-component 測試 pin
(分層 oracle,各管一種洩漏)。(d) **會 crash 的檢查不是 gate**:merge 前
fresh-state 腳本 IndexError 死掉,merge 指令卻因換行分隔照跑——precondition
檢查與寫入必須 `&&`/`set -e` 同鏈 fail-closed,這次靠 PreToolUse hook 才守住。
(e) Codex 會對新 head 原樣重貼已修 finding——checkable rationale resolve;
push 後 ~35 分鐘無回應=fresh-state check 後 poke `@codex review`。

**Class 23 — 「寫入端不驗證」的靜默磚化:每個子塊各自晚炸、client 鏡像是唯一飛行前守衛 × 修復即未存狀態 × 同一鏡像三面對稱掃 + 抽屜生命週期兩面(PR #79/UXB1,10 輪 Codex 全 P2;UXA 之後最貴的 FE 任務)**:
主病根=**PATCH `/projects/{project}` 對 config 塊什麼都不驗**,每個子塊在**各自不同的晚時刻**失敗(ontology→下次 build 的 config load、query_policy→下次 query 400、chunking→下次 build 的 config load)——所以 Settings 表單的 client-side 有效性鏡像是**唯一的飛行前守衛**:手寫/畸形塊在 save「成功」而每次 build/query 持續拒絕=**靜默磚(silent brick)**。三個 config 塊(query_policy R1/R2、ontology R4、chunking R8)是**同一 class 的 sibling 面**,沒一次把三塊一起硬化就一塊一輪。**寫入端不驗證的 config,每個子塊都要同一套四項硬化**:(a) **client 鏡像 pin 到真 server validator**——class 16 的雙 gate 共用 corpus(`*_block_validity.json`,pytest 跑真 `load_build_config`、vitest 跑鏡像;複製兩份會 drift、什麼都沒證明);(b) **missing/malformed 塊本身=未存狀態**→ 修復不得要求 dummy edit(gate=`present && !malformed && !dirty`,非 `draft===null`;表單顯示 salvage、直接存即修復);(c) **salvage 不得產出 validator 會拒的值**(R4 空白型別 bug:salvage 留 `[""]`,build 拒而 UI 收——salvage 走與 validator 同一 normalize);(d) **未動欄位從 fresh read 導出**(class 17 fresh-resolution,R5/R6/R7:per-field edits,untouched 從 fresh 塊 resolve——valid 塊保留並發詞彙、malformed 取 salvage、**absent 取空以尊重並發刪除而非復活**)。**表示邊界殘留(誠實標明)**:整數葉的 client 鏡像對「整數值的 JSON float `500.0`」無能為力(JSON.parse 後 JS 無 int/float 之分,鏡像收而 server `_int` 拒)——不可約,只有 server 端 PATCH 驗證能關,corpus 刻意不測並註解標明(class 8 表示誤差在 JSON 邊界)。**抽屜生命週期兩面(R9/R10,class 20 深化)**:(R9)**跨身分 keyed remount**——`/p/A/settings→/p/B/settings` React Router 重用 tree,B 快取命中則**無 loading remount** → section useState 抽屜從 A 存活、可 PATCH 到 B;修=`<SettingsBody key={project}>`(身分變才 remount 清抽屜,同專案 refetch key 不變故 class 20 同專案存活紀律不破)——class 20 是「keyed 瞬時卸載」,這是它的**跨身分面**。(R10)**同頁併發存=丟更新**——兩塊都 dirty,存 A(飛行中)再存 B,各自 spread 自己的 fresh read 整欄 PATCH → 後者蓋掉前者;修=三塊共用 project-scoped mutationKey、`useIsMutating` 讀飛行數 OR 進頁級 `locked`(任一 save pending 凍結全部,hook 必在 early-return 之前=Rules of Hooks)。**判準:鎖關的是哪個 race**——這關**同頁雙提交**(完全);**跨寫者**(別的 tab/CLI 在一次 save 的 fresh read 與 PATCH 之間寫)仍是版本 token 的凍結契約缺口(DR-002,save docstring 已註),兩者別混、別假裝 UI 鎖關掉了跨寫者。**元教訓**:寫入端不驗證的結構化 payload,開工前把「每個子塊 × 四項硬化 + 雙 gate corpus」列成表一次做完,否則每子塊每項各一輪(10 輪即此)。**流程**:reviewer 一輪因 API 錯誤中斷(connection closed mid-response)→ resume 續審非重開;watcher 的 `watch-codex.sh` bootstrap 在 poke 陳舊時對「已處理的舊 head 事件」誤報 exit 10(H13 再現)→ 改用**錨定在 head commit 的 watcher**(輪詢 `commit_id==head` 的 review 或 +1,對陳舊 poke 免疫)一勞永逸。輪次:UXA1 12 → UXA2 3 → UXA3 3+1 → **UXB1 10**。

**Class 24 — 凍結契約的「散文承諾」必須由 schema 結構強制,prose＝裝飾:每條 never/always/must/only 都要一個結構 pin(PR #80/UXC1a,5 輪;DR-002 契約 v1.2 eval+upload+通用 metadata envelope)**:
主病根=我把保證寫進 **description/DR-010 的散文**(「rejected 必附拒因、不靜默丟棄」、per-project exposure allowlist、metadata per-file、context 為 project-defined、每檔對應回選檔)但 **schema 沒把它們結構性凍結**——一個完全符合契約的 server 仍能違反每一條。Codex 遂逐輪把每條「未凍結的承諾」翻出來(這是 class-23「不先枚舉就逐輪被枚舉」的**契約作者面**:寫契約時不跑完備矩陣=審查時被逐格枚舉)。動 `contracts/` 前,對**每條 prose 保證跑「guarantee → 結構 pin」完備矩陣**:
(a) **每條散文保證換一個結構 pin**:「rejected 必附 reason」→ `oneOf(accepted, rejected)` + rejected `required:[reason]` + `minLength:1`(非 optional `reason`,否則空字串/缺席都合法);「不靜默把整批丟掉」→ manifest array `minItems:1`(空陣列=「全部靜默丟棄」是可表示的合法回應=病);互斥變體用 CleanPreview 的 `oneOf`+**`false`-schema**(`accepted` 側 `reason:false`、`rejected` 側 `document_uri:false`/`filename:false`)codegen 才出 `?: never` 精確判別,裸 `required`/`not` 出 `unknown|unknown`(接 §8「Generated client type」)。
(b) **defined-but-unreferenced component＝dead**:`DocumentMetadataEnvelope` 定義了卻沒被任何 request/response `$ref` → 凍結不了「回應帶 server 蓋章的 system/schema_version」這條保證;必須 wire 進 `UploadedFileAccepted.metadata`(required)才 load-bearing。**每新增一個 component 就問:誰 `$ref` 它?沒人＝刪掉或接上,不留裝飾件。**
(c) **設計依賴的關聯鍵在回應中必為 required + non-null**:upload 的 metadata 以「submitted filename」為鍵、response 的 `original_filename` 卻 optional → client 無法把 manifest 每列對回它送的檔案/它填的 metadata,per-file 語意名存實亡。**凡設計靠某鍵把回應對回請求,該鍵 required 且 non-null**(兩個變體都要;request 端同名 metadata 鍵重複 → 400,別讓兩檔搶一份 metadata)。
(d) **靜態型別表達不了的動態策略 → 凍 policy 的形狀 + 文件註明 runtime enforcement,別再造 dead component**:per-project exposure allowlist 無法在 OpenAPI response type 靜態表達「回應只含被允許的欄位」(欄位集隨專案 config 變)→ 走 **query_policy 前例**(凍結 policy 的 schema 為一個 config 概念、實際 enforcement + 其測試放 runtime),而不是硬塞一個靜態 union;但別為此另造一個沒人引用的 `MetadataExposurePolicy` component(那就重蹈 b)。判準:「這條保證能否被一個 conformant 的靜態型別捕捉?」不能 → policy-shape + runtime-enforced + test,並在 DESIGN 明述這是刻意的 runtime 面。
(e) **doc/schema 對齊(class-3 的契約面)**:`context` 的 schema 是 `additionalProperties:false`(closed 核心 `{title, document_type}` + 一個開放 `attributes` 袋)但 DR-010 散文寫「project-defined 欄位」暗示 top-level 開放 → 矛盾。改**散文對齊 schema**(專案自訂欄位一律進 `attributes` 袋,頂層封閉),別讓讀者以為能往頂層加鍵。
(f) **codegen＝消費端契約,但不是每個生成型別都由契約層負責**:`format:binary` 在 openapi-typescript 出 `string[]`(實測 `contentMediaType:application/octet-stream` 也一樣)→ FE 的 `File`/`Blob` 對不上;此為**契約層無解**(需 gen transform 或 wrapper),屬消費端(UXC2)job → reply-and-resolve(runtime `openapi-fetch` 送 `FormData` 不看編譯型別,契約語意正確;無凍結 § 規定 codegen 型別)。判準:生成型別的錯若在**編譯型別 vs runtime 傳輸**之間、且無凍結物規定該型別 → 記為消費端 follow-up,不是契約缺陷。
**元教訓**:這 5 輪全可由開工前在本地 review 跑一次「每條 prose 保證 → 結構 pin」矩陣(§7 的契約軸 = §8 的「Schema text」深化)提前收斂——已把該矩陣寫進 code-reviewer.md §8。**流程**:auto-trigger 整場失效(push 後久無回應),watcher 每輪對 pre-push 舊 review 陳舊 bootstrap(H13 再現 5×)→ 每輪先驗 Codex review 時戳晚於 push、再 poke `@codex review`、再啟 watcher 手搖;**H13(錨定 head commit 的 watcher)仍是該做的機械修**(class 23 已立案,本任務再証其必要)。輪次:UXA1 12 → UXA2 3 → UXA3 3+1 → UXB1 10 → **UXC1a 5**(全 P2/P1,契約 5 面各一輪;Phase C 起點)。

**Class 25 — async job handler 的寫入授權矩陣(class 17 的寫入側對偶)× review 中新增的機制本身是新面(PR #81/UXC1b,29 輪 Codex、45 threads、35 個 triage——史上最貴的已合併 PR,超越 C6b 的 19 輪;eval+upload+受管來源三個新面捆一個 PR)**:
主病根有二。(一)**eval async job handler 的正確性=「每個 DB 寫入點都在同一寫入授權述詞下」**,而守衛是被一輪一格逐步加上的(~10/35 輪全在同一個 handler):finalize 鎖定截止(T5)→ preflight I/O 終結化(T11)→ 取消不寫 builds.eval(T16/17)→ 終態守衛(T19)→ finalize 租約擁有權(T21)→ 取消不釋放 lease(T31 P1)→ mark-running 前復查 lease(T32 P1)→ preflight 失敗經 lease 守衛(T34 P1)→ 指紋與解析共讀(T35)。class 17 的讀側述詞是「這份快取此刻可示人嗎」;寫側對偶=「**此刻我還有寫權嗎**」——述詞(row lock 下:status 仍 active ∧ lease 仍我持有)推導一次,然後**列舉 handler 的每個寫入點**(mark-running/各失敗終結化/finalize/persist)逐點套用,一張矩陣一輪收完。凡 handler 跑在 lease/liveness 機制上,寫入點清單就是 class 12(b)「liveness 包住整個 handler」的寫入面精確化。
(二)**修法自身是新面**:T27 加的漂移守衛(接受時 pin 指紋、dispatch 再驗)在 T35 被抓到守衛內部的 TOCTOU——驗指紋讀一次、解析又重讀一次,兩讀之間的編輯以「已驗過」的身分被計分。**review 中新增的機制(pin/guard/lease-check)必須在 push 前對它自己重跑目錄**(尤其 class 10:「檢查與消費必須共用同一次讀/同一把鎖」),否則後續輪次審的是你的修法而非任務。
其餘 findings 全是既有 class 打在三個新面上:class 1 大宗(required/malformed、null 子物件×2、存在≠型別、NaN/Infinity、溢位數字、duplicate 部件與非檔部件勿靜默丟棄、保留鍵×兩個入口、無法解碼、NUL=store 值域)、class 16(檔名不可信/路徑穿越×3、路徑別名、上傳收/建置讀的雙 gate parity——corpus 名必須 build 可解析)、class 12(孤兒上傳、queued 滯留、哨兵 vs loud path 的層次選位、框架無關冪等)、class 13 再犯(路由入口 eager 取 DB 連線,拒收路徑 500)、class 5(受管來源退化目錄掃描、upsert 只併最舊一列=全稱主張沒枚舉全部)、#17 hash-輸入完整性(冪等雜湊漏 golden-set 指紋、漏實際 body)。本地 reviewer 一次 FAIL=class 9 再現(NUL 守衛只蓋 metadata,漏了餵同一 JSONB sink 的 original_filename)。
**輪次經濟學教訓**:三個彼此獨立的新面(multipart 上傳、async eval 生命週期、受管來源 pipeline)捆在一個 PR,per-surface inventory 的未掃格互乘 → 29 輪。任務跨 ≥2 個真正新面時,**計畫期就提議切片**(BA2 切 2a-2e 的前例)。防線已寫入 code-reviewer checklist §7(寫入授權矩陣+修法即新面兩子句)。

**Class 26 — 隱含預設不跨時間與交接 × 殘留狀態要 gate 身分 × invalidation 探針的 stub 必須建模世界狀態(PR #82/UXC2a,2 輪 Codex 3 P2 + 本地 reviewer 對我的修法連續 2 次 FAIL——首個「修法被本地 reviewer 實證推翻兩輪」的 PR;首輪 +1 前 Codex auto-trigger 再度整場失效,poke 才動)**:
(一)**隱含預設(derived default)只在導出的那一刻有效**;任何**跨時間**(async job:T1 起跑後 terminal refetch 重導出 selected,期間落地的更新 ready 版本偷走結果 scope)或**跨頁交接**(T2:總覽 step ③ 的阻擋版本=active??newestReady,品質頁預設=newestReady 優先——兩頁預設對同一情境給不同版本,籠統 CTA 送去評錯版本)的操作,都必須把它**具體化**:起跑即 pin(setPickedId)、連結即帶 id(?build=)。判準:看到「預設值+會出手的東西(job/link/mutation)」就問「這個預設在操作結束時還會導出同一個值嗎?」
(二)**讀殘留(retained)狀態必須 gate 在身分上**(T3 修法自身的回歸,reviewer 實證抓出):useJobStream 跨 jobId 切換保留前一 job 的事件直到自身 reset effect 執行——同掛載重跑時 jobId=B 的首個 render 仍看得到 A 的終態事件;把它算進 B 的終態判定=提前 settle,B 真正完成被 once-per-job 守衛跳過。修法=事件必須 `job_id === jobId`(hook 自己的 stale-frame abort guard 同款)。class 25「修法即新面」再驗證:這是 triage-3 修法引入的回歸,在 push 前被 gate-2 攔下。
(三)**invalidation/refetch 探針的 stub 必須建模「世界狀態」而非 call count**(reviewer 第二輪 FAIL:我的 revert-probe 假綠):call-count 分頁的 builds stub 會把「B 完成後」的世界端給**提前 settle 的多餘 refetch**——探針對「誰觸發了 refetch」失明,拔掉修法照樣綠。修法=stub 以 aFinished/bFinished 旗標決定世界內容、被測事件手動釋放(gate promise),再**實跑 mutation 證明**(拔 gate→恰好該測試紅)。class 18 probe-of-probe 的擴充:mutation 沒落地=什麼都不證明(首次 probe 的 regex 沒中,assert 抓到但 shell 沒斷鏈照跑了後半——probe script 要 fail-fast 串接);**probe 的暫存/還原用 temp copy,絕不用 `git checkout --`**(這次把未 commit 的 triage-2+3 改動整檔清掉、重建了一遍)。
其餘:T1/T2 同種掃一輪收完(class 17 的 default-drift 對偶面);本地 reviewer 的 Rule-12 nit(鎖住的按鈕不得沉默)=誠實面小種。防線:code-reviewer checklist §7 增「隱含預設×出手操作」與「invalidation 探針 stub=世界模型」兩子句。

**#83(UXC2b 上傳 UI,3 輪 Codex 各 1 P2 皆 must-fix;本地 reviewer 4 次 PASS 無 blocker;無新 class——三個 finding 全是目錄已有 class 打在「同頁 sibling 已有紀律、新 section 沒繼承」上)**:
(T1)**UI 只服務了契約的「無配置子集」**:上傳 UI 對宣告 required context attributes 的專案是保證被拒的死路(端點對缺席 context 也驗 schema)——#21 per-surface inventory 的值域問句「每個 contract-valid 輸入都能被服務嗎?」套在**專案 config 的變體**上:設計新 feature 面時要枚舉 config-driven 變體(無 schema/有 optional/有 required),不是只做預設形。修法=僅必填欄位的逐檔輸入(產生用鏡像,判定留伺服器;留空「省略鍵」讓伺服器用自己的拒因退檔——空字串伺服器合法,client 非空 gate 會過度阻擋)。
(T2)**「設定未知」塌縮成「無必填欄位」**(class 23/UXB1 fail-closed 再犯;該述詞本體出自 FE1/#70 的 class-10 stale-data 變體):requiredAttrs(active?.config) 在冷載入/refetch/讀取失敗時回 []——表單不渲染、送出即開。**同一頁的 RunPipeline 就掛著 gatesLoaded**,新 section 沒去接。#21 的「family transfer ≠ per-surface sweep」推到極致:紀律不在別的模組,在**同檔案下一個 component 的 props 上**。審新 section 先掃同頁 sibling 的 guard/gate props 清單。
(T3)**鍵輪換觸發集必須覆蓋伺服器 hash 的「全部」輸入**(class 1 hash-輸入完整性的鍵側對偶):伺服器把 metadata 內容摺進冪等指紋,UI 卻只在檔案選擇變更時重鑄鍵——修正欄位後重試=舊鍵×新 body=409 死路。**同檔案上方的 Sources 表單就有「任何欄位編輯即重鑄」的 effect**,新表單只做了一半輸入。規則:先列「伺服器 request hash 的輸入清單」,鍵的重鑄觸發集=該清單。
**流程**:Codex auto-trigger 連三個 PR(#82×2、#83)沒對 open/push 自動觸發——watch→timeout→poke 已是常態路徑,別等;bash 工作目錄會在 git 操作間漂移,`npx vitest` 在 repo root 跑會「document is not defined」全紅(vite config 在 web/)——先 cd 再跑,別誤判為壞了。真瀏覽器 QA 首次驅動真實檔案上傳(dropzone 收合成 DataTransfer 的 DragEvent;file_upload MCP 工具在本 client 拒收 host path)。

**#84(UXC2c 檢索改名+全流程 e2e,2 輪 Codex 各 1 P3:1 修 1 以「執行證據」decline;本地 reviewer 2 PASS+1 nit;Track 4 至此全數完成)**:
(T1,修)**class 26 世界模型的自我精煉**:capstone 的 world 旗標原在觸發 POST 翻轉——「job 完成才有 build」這條旅程的承重邊界沒被表達,UI 若不再接 job watch 旅程照樣綠。修法=旗標移到 **job snapshot 被觀測到終態**的路由(觸發 POST 回純 202)。規則:world-state stub 的旗標要掛在「被測試保護的那個觀測邊界」上,不是掛在觸發動作上——「寫入落地」的建模點=消費者能觀測到它的那一刻。
(T2,decline)**decline 要帶「執行過的證據」,不是推理散文**:Codex 主張 `**/jobs/<id>*` 會吃掉 `/events` URL(glob 語意錯誤)。回覆=讀安裝版 playwright-core 的 `globToRegexPattern` 原始碼(單一 `*` → `([^/]*)`,不跨 `/`)+ node 實測編譯後 regex 對兩條 URL 的匹配結果貼進 thread——結構上不可能,註冊順序無關。class 4/7 的執行級驗證套到 review 回覆:面對第三方工具語意的爭議,**先打開它的原始碼再實測**,兩邊的直覺都不算數(本地 reviewer 此前也「驗證過」同一 partition——與 Codex 相反,若只比推理就成了羅生門)。
其餘:改名的刻意邊界(Settings「問答安全」=另一面,owner 措辭未點名→不動+PR 標註;`Settings.tsx:101`「下一次問答」同框待 owner 延伸);capstone 旅程包含設定 ontology 步(文字語料沒有 ontology 建不了——真實旅程的一步,順帶走真 UXB1 表單)。**流程**:python 批次編輯的 anchor 不唯一→路由插錯測試(count=1 replace 咬到第一個匹配;anchor 必須先 assert 唯一);cwd 漂移第三次咬人(watcher 127:scripts/ 在 repo root)——bash 持久 cwd 會被前面的 cd 留下,跑 repo-root 腳本前先 cd 回去。

**#85(SRC1 xlsx connector,4 輪 Codex 各 1 P2 皆 must-fix + 本地 reviewer 首輪 1 blocker;無新 class——全是目錄已有 class 打在「新 connector 面」上,per-surface inventory 又漏了一輪掃)**:
(T1)重複的正規化標頭 first-wins 默選=class 1「拒絕勝於默選一邊」再犯(C3a key==table 同款);修法=只對「被對應的名字」拒歧義+未對應撞名照容忍(class 9 對偶 accept pin)。(T2)尾列停掃 guard 鍵在 any(values)、內容判定卻是 title+body——**同一個「這列是不是內容」有兩個述詞=class 5 分岔**,預編號模板尾列(編號填到底)每列重置 streak、停掃永不觸發;修法=統一成一個內容述詞。(T3)_xlsx_required 以 strip 驗、回傳未 strip=**接受與消費在 whitespace 上分岔**(class 5);sibling _required_meta 經審為不同消費契約(CSV 對未正規化 fieldnames 原樣比對)→ 標註 follow-up 而非機械複製。(T4)同內容異列在 content_hash 去重下塌縮、丟第二列 #row= 溯源=**#17 hash-輸入完整性的文件身分面**:去重鍵必須涵蓋要保住的身分;修法=作者編號入渲染文字(§18 item_ref 不動;structured 家族由 pk 入列 JSON 本就免疫),序數 fallback 刻意不渲染(印杜撰數字=誤引原表)。
**本地 reviewer 的 blocker(最有價值的一課)**:第一版「全形括號」regex 與其測試的全形字元**全是半形重複**——全形/半形視覺同形,經 tool 層靜默降級,**pattern 與守門測試一起降級=假綠對**;reviewer 以 codepoint dump 實證抓出。防線(class 21(b) 位元組驗證紀律的銳化):(a) 載重 pattern 含視覺同形字元(全形/半形、全形空白、相似破折號)時,**以 codepoint dump 驗磁碟上的位元組**,不信眼睛;(b) fixture 以 **ASCII escape 自驗**(`assert "（" in header`——escape 是純 ASCII,任何層都無法 mangle,能判別降級的 literal);(c) pattern×test 成對 mutation 實證。過程中 bash heredoc 對 CJK/escape 的 mangling 反覆咬人(替換「回報成功」但位元組未變/變錯)——**跨 tool 層寫入非 ASCII 後一律回讀驗位元組**;chr(92) 運行期組裝是最後手段。
**流程**:實資料 QA 高值——謊報 ~1M 列的區域探索廳以 0 LLM 花費證明停掃+19 列溯源;缺 ontology 的誠實失敗即「走 text 管線」的證據。4 輪×1 finding 的擠牙膏再現:新 connector 面的 pre-push inventory 應列 {每個識別子:歧義+正規化分岔} × {每個 guard:與消費者同述詞} × {去重鍵:涵蓋全部身分成分}。

**#86(GOV1 型別穩定/跨型別 resolve+DR-011 雙鍵家族,本地 reviewer 1 blocker(FAIL→PASS)+2 輪 Codex 共 3 P2 皆 must-fix;新增 class 27)**:
(本地 blocker)**鍵家族遷移=枚舉「全部」mint/lookup 站點,且守門測試的種子要來自 spec 公式而非實作**:v1→v2 ledger 鍵遷移改了第一個 relation-verdict 查找(主迴圈),漏了 `_apply_merge` 內 post-merge re-mint 的 sibling 查找——它仍以 fpv1 儲存簽章查 v2 ledger,永不命中;而守它的兩個測試把 ledger 種子鑄成「實作查什麼就種什麼」(fpv1)→ **假一致對**(#85 假綠對的鍵遷移版):class 2(改規則掃全部入口)×class 5(測試 oracle 抄實作=分岔不可見)。防線:遷移身分/鍵 scheme 時 grep 全部 `_decision(`/mint 呼叫列站點清單;測試種子用 spec 公式獨立鑄鍵;修好後 revert-probe 證明恰好那兩測試紅。
(T1)**「禁入某 band」必須是結構旗標,不是分數竄改**(class 8 邊界語意×表示誤差再犯):`score = min(score, auto - 1e-9)` 在 review_threshold == auto_merge_threshold(驗證器明文允許相等、strict 整合測試就跑 1.0/1.0)時把 exact match 壓出**兩個** band → 恰好丟掉 DR-011 要浮出的 pair。凡「x ± epsilon 再寫回、之後拿去比 band 邊界」都是這味道;修法=review_only 旗標(auto 分支要求 not flag),分數永不竄改(candidate 存真值=curator 真訊號)。**抄來的 pattern 連 bug 一起抄**:épsilon clamp 是從既有 both-ids 分支複製的,同類一次掃完把兩處一起換掉;附帶恢復 §17 defer-relist 一致性(early-continue 曾吞掉低於 band 的 deferred pair)。
(T2)**拆桶時,桶的「效能功能」也要在同一刀補回**:predicate 過濾 ≠ 免掃描——去型別桶後全對掃 O(n²)照付,只是配對被拒。修法=倒排索引桶(token/prefix→成員),成本貼齊 predicate 收的配對數;等價驗證=合成語料 old-vs-new 輸出(集合+順序)全同+既有 pair-set pin。
(T3=class 27)**放寬可達集,必須重推導下游每道守衛的隱含前提**:namesake skip 的健全性(「兩列皆帶 id 且同名 ⇒ id 值必不同」)不是 skip 自己驗的,是**上游結構替它保證的**(同型別內 same (name,id) 收斂為同一儲存鍵→配對根本不存在);拆型別桶放寬了可達集,同名同 id 跨型別配對(同一來源列的 LLM 型別漂移)變得可達,value-blind skip 把「來源主張同一物」的配對當 namesake 靜默丟棄——既不併也不浮出。**三個各自正確的元件(儲存鍵去重×型別桶×存在性 skip)聯合自洽,抽掉中間一個,第三個就靜默壞掉。**防線:凡 diff 放寬可達集(拆桶/移除 filter/開新 caller),列出下游每道 guard,逐一問「它的前提裡哪些是舊可達集結構性保證的?」;修法=判準下沉到值(比對 trimmed disambiguator 值,不可證同=fail closed 保 skip——舊列 id 只活在 hash 內回鑄不了)。
**流程**:dev DB 衛生——先前直接 SQL 刪 scratch 專案留下孤兒 documents/chunks,把 H11 全表污染面擴大(integration 多紅 2 個);清理=child-first 刪除,教訓=scratch 資料刪除走 API 或連 child 一起。CI-watch→poke→watch-codex 鏈成單一背景命令運作良好;Codex auto-trigger 依然死著(#82 起連五個 PR),CI 綠後即 poke 已是常態。

**#87(GOV4 Filter fail-loud + status facet,0 輪 Codex 首輪 +1;無新 class)**:調查先於實作的正面示範——O4 宣稱「impl silently ignores Filter」半數過期(deepObject 拼法自 BA1b 就 400),真缺口=裸拼法 `filter=x` 繞過 `filter[` 前綴檢查被靜默吞掉;先讀 guard 源碼+git 史再動工,把任務從「加 gate」修正成「收殘拼法+開 allowlist facet」。兩個可複用點:(a) guard 加 allowlist 參數(預設空=舊語意)讓 SS1 的更多 facet(filter[type] 等)成為免 DR-002 的 additive runtime 實作——契約的 deepObject additionalProperties 早已允許任意欄位,拒/收是 runtime 語意;(b) **hermetic pin 的正常生命週期=實作到來時刻意反轉**:舊 pin 凍結的是「尚未實作」的過渡行為(filter[status]→400),facet 落地時該 pin 必須反轉並擴充判別集(有效值 200/詞彙外 400/allowlist 外 400/裸拼法 400)——pin 過渡行為時就預期這一天。facet 詞彙以 §17 STATE_MACHINES 為 SoR 非重述字面集(class 2/5 預防);重複參數=歧義拒絕(C3a 拒絕勝於默選一邊)。

**#88(SS2 引用卡片,3 輪 Codex 共 4 P2 皆 must-fix + 本地 reviewer 首輪 3 nits;無新 class——四個 finding 全是「多形契約值的消費者沒先枚舉全部 producer」一件事的四個切面)**:
(R1)UUID gate 丟掉 manual 證據的 document ref(id=evidence_ref 非 UUID,quote 在 metadata)——寫多形值(SourceRef)的新消費者前,**先讀「全部」producer 的發射面**(graph.py evidence_ref 三形×semantic×sql×global×enrichment),class 16「讀消費者源碼」的對偶:這裡該讀的是 producer;value 的 SHAPE(UUID 形)≠ 它的 SEMANTICS(R2:manual evidence_ref 可恰為 UUID 形——詳情查找輕則 miss 重則掛錯文件標題,quote 才是解析結果,語意先於句法)。(R2)fold 展開=每 ref 掛 resolver、每 resolver 一個 fetch——community hit 引全部成員實體,一鍵數百併發 GET;修法=fetchKind 純函式當「唯一」分派述詞(渲染與上限記帳共用,class 5 預防)+ RESOLVE_FETCH_CAP=30 逐 (kind,id) 佔位+**可見**註記(no silent caps);純解析卡(quote/row/stable)不佔位。(R3)查詢邊界早把 allowlisted envelope 掛在 metadata.document 上,client 卻只渲染 hash+序數——**先用回應已載的欄位,再考慮加 fetch**(API 已給的答案被丟掉=R1 的鏡像:那邊是沒讀 producer,這邊是沒讀「已送到手上的資料」)。
**流程教訓(本 session 三度咬人)**:`gating-cmd | tail` 把 exit code 換成 tail 的 0——doc-lane ff chain 因此在 CI 未綠時搶跑 push main 被 branch protection 擋、PR chain 讓 governance 紅了三輪不可見(checkoff lint 拒絕「重寫已勾稽行」——已勾稽行的文字更正走 merge 後 doc lane,PR 內 TASKS.md diff 只留本任務勾稽)。**規則:gate 判斷的命令絕不過管線;exit code 要嘛直取要嘛 set -o pipefail + 落檔再 tail。**另:jsdom/testing-library 的 findAllByText 在「第一批非空匹配」即 resolve——數量斷言一律 waitFor(queryAllByText→toHaveLength);短 SHA 手動補全=偽造 object id(bf6e399→rev-parse 取全 SHA,別自己編尾巴);GitHub 分支刪除會孤兒化未合併 commit 並取消其 CI run——doc lane 順序鐵律=CI 綠→push main→才刪分支。

**#89(QP1 auto graph plan,本地 reviewer PASS+3 nits、CI 抓 1 漏網消費者、2 輪 Codex 共 4 P2 皆 must-fix;無新 class——四個 finding 環繞「新決策路徑的邊界語意」)**:
(CI,class 2 亞種)**行為變更的 sweep 要 grep「行為 pin」非僅散文**:api-seam 測試 pin MODE_SKIPPED==2,其問句恰點名 seeded 實體 → auto plan 改變路由,CI 紅。教訓:routing/gating 行為改變時,sweep = grep 散文 + grep 行為斷言(MODE_SKIPPED/selected/counts),且以「全 tier 執行」收尾——cherry-pick 觸及檔案漏掉了 API 層消費者。
(R1a,流程紀律非新 class)**本地 reviewer 的 consider-nit 被 Codex 升格為 P2**(強制 graph append 尾端→緊 deadline 先砍它,無聲打敗自身保證)。教訓:guarantee 路徑上的 deadline/順序 nit 沒有 consider 級——push 前就修。**probe 假綠二連**:order pin 首個 probe 在 mutant 下通過,因測試選擇(["semantic"])讓 append 與 ordered-insert 同形——mutation probe 的輸入必須「可判別」(改選 graph 之後的模式才分得開;triage-1 commit 訊息有載);同 session 稍早另一 probe 因 grep 管線靜默無輸出被誤信,重跑可見輸出才確認(session-only:trace 上無佐證,無獨立 commit/comment 可查)。**規則:probe 結果必須看到 FAILED 字樣,且先驗證輸入真的能區分兩個實作。**
(R1b,class 9 對偶的再現——deny 面過寬即過度阻擋)遮蔽由「名稱包含」改「跨度重疊認領」:York+New York 並存=path 模板存在的理由,名稱級包含整個吃掉 York。已連結名的重複出現仍認領(防子名從第二次提及溜入)。
(R2a)**async 保證需要真實 await 點**:純 CPU 掃描對 asyncio.timeout 隱形(class 12 的 asyncio 亞種——框架機制的精確語意:timeout 只在 await 邊界搶佔)。修法=純 stage 抽出、兩種組合共用(class 5 預防),async 路徑分段 + sleep(0) 取消點;parity 用執行測試釘住。
(R2b)**跨文字系統的邊界語意**:拉丁短名(US/IT)需詞界、CJK 是 containment;str.isalnum() 對「區」為 True——通用 alnum 檢查會誤殺全部 CJK(class 8 的文字系統亞種:邊界規則要按 script 分別推導,並誠實標出殘餘=獨立同形 token 不可判)。

**#90(H13 watcher --anchor,0 輪 Codex 首輪 +1;無新 class)**:harness 自我修復的正面示範——會咬 loop 的工具缺陷(triage-without-poke 使每次 watch 永遠 exit 10)在兩次手寫 inline 繞過後正式立案修掉。可複用點:(a) **同一個「anchor」概念可以有兩種 tie 語意**,取決於 anchor 是「對話的一方」(poke:同秒=回覆,未處理)還是「事件自身的時間戳」(--anchor:同秒=自己,已處理)——設計 timestamp 述詞時先問 anchor 指的是誰;(b) 執行級測試的假 gh 按「參數內容」路由 + 世界在 env vars,是 shell 感測器類工具的可複製測法(receipts 前例的 API 版);(c) exit-10 訊息印出事件時間戳=把「下一步需要的輸入」放進上一步的輸出,工作流工具的介面紀律。

**#91(H11 測試環境衛生,0 輪 Codex 首輪 +1;本地 gate-2 抓 2 真 blocker;無新 class)**:兩個 blocker 同一母題——「我的驗證宣稱了環境使之為假的事」:(1) **env-prefix 陷阱**:monkeypatch 裸 `REDIS_URL` 被 Settings 的 `env_prefix="GRAPHRAG_"` 默默忽略 →「在 db 15 全綠」為假(實際跑在 db 0 且 flush 了 dev worker 佇列——fixture 要防的傷害恰好由 fixture 造成;佇列恰空而倖免)。防線=**指向環境的 fixture 必須自驗**(setenv 後立刻 assert 解析出的設定真的指到宣稱處,錯名 fail loud)——「測試通過」≠「測試跑在我以為的地方」(class 5 檢查者分岔的環境亞種);mutation probe 用「錯 env 名」證明 assert 咬得住。(2) **lived-in ↔ fresh 對稱性**:為「髒環境」建的掃除器在「乾淨環境」上炸(CI 新鮮庫無 migrate 前置,DELETE 撞 UndefinedTable)——H11 自己的教訓打回自己:髒與淨都不是不變量,兩個方向都要守(to_regclass 逐表守衛+空庫實測)。掃除器經濟學:session-START 是唯一在任何 kill 後仍會跑的鉤子;前綴常數要帶「必註冊否則殘骸永存」註記(開發期就漏了 uxc1b-,靠列殘骸清單抓回——session-only:PR body/commit 均無此節記錄,僅 conftest 的 `uxc1b-` 前綴本身為部分佐證)。(2) 對映=class 1「枚舉整個面」的環境狀態亞種:環境的「狀態集」{lived-in, fresh} 也是要枚舉的面,守了一態漏另一態即缺格。

**#92(SS1a inspect facets,0 輪 Codex 首輪 +1;無新 class——#20 型「預防鏈生效」數據點,連三 PR 零輪)**:catalog 模式在設計期全套用即零輪:GOV4 allowlist 機制原樣推廣(修法即機制,機制即下個任務的地基)、單一共用 helper(class 5)、DDL parity contract test(class 16 兩 gate 共用語料——解析 CheckConstraint sqltext 而非重述字面)、pin 生命週期反轉(GOV4/#87 教訓:facet 落地時過渡「全拒」pin 必須翻面且各留註解)、accept-pin(class 9 對偶)、live mutation probe。開放 vs 封閉詞彙的判準=「DDL 有無 CHECK」:有 CHECK 即封閉(parity 綁定),無 CHECK 即開放(只拒空白)——詞彙政策跟著 SoR 走,不自行發明。**流程(第 4 次 cwd 咬人,這次咬死整條 chain)**:背景 chain 繼承 foreground 殘留的 cwd(web/),`git add api/...` 在第一步就 pathspec 失敗、整條 commit→PR→CI→poke 靜默死亡,靠 chain 輸出的 tail 才發現。**規則升級:每條背景 chain 的第一個命令必須是 `cd <repo-root>`(絕對路徑),不信任繼承的 cwd。**(session-only:chain 失敗輸出僅在 task output 檔,PR/commit 無痕——#92 的 commit c2ef970 是重跑後的成品。)

**#93(CFG1 registry SoR+多專案 gateway/DR-012,gate-2 1 blocker+Codex 7 輪(R1-R5/R7 must-fix、R6 誤報反駁);新增 class 28/29)**:
(gate-2,class 12 的 anyio 亞種)**cancel scope 綁 task**:child lifespan 在請求 task 進入、由 shutdown task 退出 → anyio `Attempted to exit cancel scope in a different task`;修=**host-task-per-child**(`tg.start(host)`,同一 task 擁有 lifespan 進出兩端,`stop` Event 放行)。判別紀律:fake child 必須自帶真 `create_task_group()` 才咬得住(啞 fake 對錯兩版都綠);live probe(CTRL_BREAK 優雅關機)補 harness 外實證。
(R1)**startup.complete=綠燈語意**:資源(task group)備妥前送綠燈=首請求 500 窗;修=群組進入後才 send。**probe 位置紀律(false-green 第二現場)**:斷言要放在 send callback 內同步驗——放在 `started.wait()` 之後,排程會讓假修也綠。
(R3=class 28)**身分切段必須在未解碼表示上做**:ASGI `scope["path"]` 已解碼,`/mcp/a%2Fb` 與 `/mcp/a/b` 不可分——對解碼 path 切段=編碼分隔符產生結構(a 存在時服務到錯專案)。修=對 `raw_path` 切段、段內單獨 `unquote`、解碼後含分隔符=與 unknown 同答 404。規則:凡以分隔符切身分段的面(URL path、檔名、複合鍵),切段一律在 encoded 表示,解碼只作用於段內;pin 要用「兩個專案都存在」的判別輸入(smuggle 必 404 且錯的那個永不 mount)。
(R4)**簽名遷移=枚舉全部 stub 站點**(class 16 oracle 弱化×#86 家族遷移合流):`read_and_fingerprint_eval_inputs` 改簽名,一個 monkeypatch 站點未遷 → stub TypeError 被 preflight 廣義 except 攔成 job failed,測試斷言恰好也是 failed=**oracle 從「指紋比對」靜默降級為「任何爆炸」**。防線:改任何被 stub 的簽名先 grep 全部 stub 站點;**雙向 mutation probe**=舊 stub+關守衛(綠→假綠實證)、新 stub+關守衛(紅→判別實證),單向 probe 證不了降級。
(R5)**錯誤遮蔽×構造順序**:actionable 守衛(registry policy 讀取)必須先於「會因無關原因失敗」的資源建構(無 OPENAI_API_KEY 的 client factory)——否則雙壞場景報的是遮蔽錯。pin 順序+失敗路徑釋放(policy 失敗 dispose engine)一起;mutant=搬回順序,恰好改拋遮蔽的 RuntimeError。
(R6,誤報判讀)**框架生命週期粒度證據鏈**:Codex 把 gateway host 持有的 app-lifespan 誤當 policy 讀取所在;反駁靠可檢驗的鏈——SDK 源碼(per-session `Server.run` 進 `lifespan(self)`)+既有判別測試(#58 isolated runtimes)+模組 docstring 的 next-session 語意,不是主張。回一條鏈,Codex 下輪即收。
(R2→R7=class 29)**同一身分的兩個投影必須同源單讀**:R2 把 pin 修成鎖下重算,留下「§27 記錄(未鎖指紋)≠ job pin(鎖下指紋)」的縫——PATCH 擠進兩讀之間:按實際被接受 policy 重試 409(該 replay 而未 replay);policy 在 TTL 內改回=靜默 replay 一個按不同輸入評分的 job。終局=**記錄≡pin by construction**:produce 內先鎖(`get_project for_update=True`)、單讀算指紋,pin 與記錄同取此值(`run_idempotent` 增 `rekey`:勝方 produce 後改存接受身分;None=原樣)。**鎖序紀律**:不得為修 race 把鎖前移到 reserve 之前(projects→idem 反轉 trigger 端點的 idem→projects,同 key 跨端點可誘發 deadlock)——寧可事後 rekey。判準:凡「某值同時餵兩個身分」處,先問兩者是否同一次讀導出;不是=有縫。
**輪次結構**:7 輪全獨立面(gateway 路由/啟動序/測試 oracle/構造序/生命週期誤報/idempotency 身分),無同 class 重複——catalog 預防鏈只對「已見 class」預防;**新機制(gateway、雙指紋)第一 PR 的輪次高是結構性的**,對策是設計期把新機制的每個面過一遍 catalog 判準(切段表示、綠燈語意、身分同源),不是等審查。**流程**:R6/R7 兩輪由 reply-and-resolve 與 must-fix 混流,watch 的 `--anchor` 紀律(H13)全程無誤報;背景 chain 首命令 `cd` 規則(#92)本 PR 全程遵守,零 cwd 事故。

**#94(CTR1 DR-002 打包契約 v1.3/DR-013,gate-2 1 nit+Codex 1 輪 2 P2 皆 must-fix;class 24 補兩面,無新 class)**:五面契約(SRC2/GOV2/GOV3/SS1b/RB1)+MCP rider 一回合議定,DR-009/DR-010 打包模式第三次成功套用。兩個 must-fix 與 gate-2 的 nit **同屬 class 24 的一個母題,分三次被抓**——這正是打包回合的特徵失敗模式:
(class 24 補面 a)**每個新 request body 的「固定語意」都要 `additionalProperties:false`**:SourceUpdate(gate-2 抓)、RetryRequest(Codex R1 抓)是同一 class 的兩個 body——body 的散文說「僅 X」(僅 enabled/僅 failed-items)但 schema 開放,`{"uri":…}`/`{"mode":"all"}` 即 contract-valid,runtime 被迫二選一(接受即忽略/拒絕合法 body)。**一個 body 抓到≠掃完**:打包回合開工時列出**全部**新 request body,對每個問「散文有沒有承諾固定/封閉語意?有就 `additionalProperties:false`」——否則每個 body 各一輪(此處 gate-2+Codex 各撿一個,因未在設計期統一套用)。DocumentMetadataInput(server-stamped 欄位)是同 pattern 的先例。
(class 24 補面 b,新面)**廣告的狀態必須是端點自身形狀可產生的**:McpInfo `url` required-but-nullable + `path_addressable:false` 廣告「不可定址專案」狀態,但 `GET /projects/{project}/mcp` 自身 path-keyed 於 `{project}`——含 `/` 或 `.`/`..` 的名字連這端點(及任何 `/projects/{project}/…`)都到不了,null 分支**結構性不可達**。class 24 原本是「散文承諾→結構 pin」(讓承諾可執行);這是其**對偶**:別做 schema 形狀能表達、但端點路由永遠產生不出的承諾(vacuous promise)。判準:凡 required-but-nullable 或列舉了某狀態旗標的 response 欄位,問「這端點的**定址/路由形狀**真的能產生這個分支嗎?」不能就 narrow(此處:reachable⇒addressable⇒url 恆存,砍 path_addressable)或改掛非-path-keyed 面(list 端點)。修法選 narrow,因 Console 早已用同一 isPathAddressable 規則在 client 端自判該狀態(dead server 欄位)。**流程**:契約 PR 走「app.openapi() 回傳凍結 artifact」設計,contract-first 落地是既定流;codegen(web schema.ts)每次改契約同 PR 重生成(class 18 投影 lockstep),ErrorCode +3 由 api/errors.py 的 lockstep 測試強制同步(改契約 enum 不同步 runtime enum=紅)。**元教訓**:打包契約回合把「每個新 schema × class-24 兩面(固定語意 body 封閉 + 廣告狀態可達)」列成設計期檢查表一次過,否則同 class 每面一輪。輪次:CFG1 7 → **CTR1 1**(打包回合設計期紀律见效,單輪收斂)。

**#95(SRC2 source soft-disable runtime,gate-2 PASS(1 non-blocking nit)+Codex 2 輪 2 P2 皆 must-fix;無新 class,補強 class 1/2/26/27)**:CTR1 契約已凍,首個 runtime 落地。兩輪各一面,均為既有 class 的實例:
(R1,class 1 契約驗值的 runtime-parity 面;code-reviewer.md §8 亦記「runtime-validator-equivalence:bool-passes-int→strict」)**runtime validator 必須等於凍結契約的值域,尤其是 gate 狀態變更的欄位**:`SourceUpdate.enabled: bool` 用 Pydantic 寬鬆布林,`"false"`/`0`/`"0"` 被強轉——契約 type:boolean,本端點正是翻轉該狀態,畸形 `"false"` 靜默停用來源而非 400。修 `Field(strict=True)`。**流程教訓**:gate-2 已標此為 non-blocking nit(「符合 sibling 慣例、契約-projection 是 CTR1 範圍」),我延後;Codex 升為 must-fix——**gate-2 對「runtime 驗證 vs 凍結契約」的 parity nit,當該欄 gate 一個變更動作時應主動當 must-fix 修掉**(一行成本,Codex 必升級)。`enabled` 是本庫第一個 request-body 布林,strict 立正確先例而非偏離(無其他布林欄可偏離)。
(R2=class 27 的**narrow 對偶** × class 2/26)**加一個「參與述詞」必須枚舉該述詞的全部評估點,不只執行器**:SRC2 引入 `enabled`=「此源是否進 build」。該述詞有**兩個評估站**——backend 執行器 `_load_sources(enabled_only=True)` 與 **FE 的建置資格 gate**(Import 頁 hasTextSource→ontology block、unresolvable→run 停用,存在目的就是預測執行器行為)。我只改了 backend,FE gate 仍算在原始清單→停用一個壞源(SRC2 的救援目的)不能解除封鎖:UI 與實際 build 分岔,soft-disable 看似壞掉。class 27 是「放寬可達集重推導下游守衛」,這是它的**收窄對偶**:收窄「參與 build 的集合」時,凡「預測 build 會納入什麼」的 gate 都要同步收窄。修法=FE gate 改算 `buildableSources = filter(enabled !== false)`(缺 enabled=啟用,鏡射契約 absent-means-enabled 與 migration default true);來源「清單」渲染仍用原始清單(停用列可見以重新啟用)——**gate 用 enabled 子集、display 用全集**是關鍵的兩面分工。防線:凡新增改變「什麼參與操作 X」的 filter/述詞,grep 每個計算「X 資格/會不會發生 X」的站點(執行器 + 所有 pre-flight gate),FE 的 predictive gate 是與 executor 對稱的第二站。**輪次**:SRC2 2(strict-parity、跨層述詞鏡射)——runtime 落地的輪次來自「契約已定但 runtime 有契約管不到的兩面:寬鬆強轉、跨層一致性」。

**#96(SS1b-api server-side q + totals runtime,gate-2 1 blocker(FAIL→PASS)+Codex 首輪 +1 零findings;無新 class,補強 class 16/22)**:CTR1 契約第二個 runtime 落地。**Codex 零輪**——因 gate-2 先抓住唯一實質缺陷,對外只花一次 +1。
(gate-2 blocker=class 22「翻譯即斷言/oracle 閉合」的**caseless-fixture 亞種**)**驗大小寫不敏的測試,fixture 必須帶大小寫變化**:「case-insensitive over ASCII」的片段用 CJK(`海洋劇場`/`劇場`)——CJK 無大小寫,`ilike`↔`like` 對它完全等價,且該片段還有一行 dead discarded request——所以**無任何測試能判別 ilike vs like**,`ilike→like` 迴歸靜默過關(Rule 9:不會因邏輯改變而 fail 的測試是壞的)。修法:種 ASCII 帶大小寫實體(`Ocean Hall`)、以反向大小寫查(`OCEAN hall`)斷言仍中;`like` 回 0 即敗。防線:斷言某「不敏感/正規化」性質時,fixture 必須含被正規化掉的那個維度的變異(大小寫不敏→帶大小寫差;重音不敏→帶重音;空白正規化→帶多空白),否則 oracle 對該性質全盲——這是 class 16「別調弱自己的 oracle」在**正規化述詞**上的具體化。mutation probe 必跑 `ilike→like`。
(補強 class 16 兩 gate parity 的正面示範)**萬用字元當字面**的測試一度假綠:`50% off` vs `fifty off` 搜 `50%`——但 `fifty` 不含子字串 `50`,故未逃逸的 `%` 也只中一列,測試無論有無 escape 都綠。修正=判別資料 `a%b` vs `axyzb` 搜 `a%b`:literal escaping 只中 `a%b`、未 escape 的 `%a%b%` 兩者皆中。**教訓**:測「X 被當字面而非語法」時,反例列必須是「若 X 當語法就會被誤中」的那種(`axyzb` 專為 `a%b` 的 wildcard 解讀而設),否則 probe 假綠。此二者同一母題:**測正規化/轉義性質,資料必須讓「錯誤解讀」產生可觀測的不同結果**。
(class 27 收窄對偶的正確運用)**count 與 page 必須同 scope 同述詞、cursor 除外**:total 是搜尋集全集,故 `fetch_count(table, *filters)` 吃搜尋+facet 述詞但**不**吃分頁 cursor 述詞(cursor 只窄化本頁);count 與 page 同源 `_select`(build scope)+同 `_reject_raw_sql` 紀律——一個數不同列的 total 是謊言。**流程**:SS1 owner 已切 SS1a/SS1b,SS1b 再因跨 backend/FE/perf 自切 SS1b-api/SS1b-fe(planning-slice 紀律,#84);背景 chain 首命令 cd、staged 明確 7 檔避免掃入 untracked data/(reviewer nit)。輪次:SS1b-api Codex 0(gate-2 攔截 = 對外零輪的正面數據點,連同 #90/#91/#92)。

**#97(GOV3-api ontology proposal 審核 endpoint runtime,gate-2 3 blocker(全 FAIL→PASS)+Codex 3 輪 R1/R2 must-fix、R3 reply-and-resolve;無新 class,補強 class 16/#84 silent-brick;GOV3 切 api/fe)**:CTR1 契約第三個 runtime,`accept 把型別加入 config` 這一個寫入動作連挨 gate-2 三審+Codex 三輪,全在「accept 閘必須恰等於 build 的接受」這一點上收斂。
(R1 + gate-2 R1 blocker=class 16「別調弱自己的 oracle」× #84 silent-brick)**用了 build 管不到的第二個閘=可 drift 的假平價**:accept 首版手寫 `_valid_ontology_block`(只驗 entity/relation 兩清單非空)+ parity 測 corpus——gate-2 抓「parity corpus 沒覆蓋 divergence 角(unknown key、bogus proposal_policy),假綠」;Codex R1 抓「config PATCH 不驗(#84),accept fallback 捏造 partial 塊→下次 build 拒→200-then-silent-brick」。**終解=消滅第二個閘**:accept 直接走 build 自身 `_load_ontology`(公開 `ensure_ontology_buildable` 包裝,惰性 import 破 module-top cycle)。**教訓:當「我的檢查必須等於別處的檢查」,最強的不是雙 gate parity 測(仍可 corpus 漏角),是「根本只有一個 gate」——能重用就別重寫;重用不了(cycle)才退而求雙 gate+corpus,且 corpus 必含「弱式會漏」的角。**
(R2=class 17 寫入側 fresh-resolution × class 8 表示邊界)**驗證前正規化=修補壞輸入**:R1 修好後,accept 仍 `list(block.get(list_key) or [])` 先正規化被接受側清單再驗——缺清單變 []→append→[type]、字串 "Person"→字元標籤,修補後的塊過關,靜默採納 build 會拒的原 config。修=**先驗「當前」config(任何讀取/正規化之前)**,過關後才讀已知有效的清單、append、再驗結果。判準:read-modify-write 一個外部可畸形的結構,**先驗原值再改**,別讓「取值即正規化」(`.get(k) or []`、`list(str)`、`int(x)`)把非法輸入洗成合法——正規化屬於驗證「之後」。與 #84 R5/R6/R7 fresh-resolution 同源:寫入側對 fresh read 導出,但這裡多一刀=導出「之前」先驗 fresh read 本身可用。
(R3=class 10/#84 R10 跨寫者版本缺口,reply-and-resolve)**新寫者參與既有無版本併發缺口≠新 bug**:accept 是第一個「review 動作寫 config」的路徑,故第一個撞上 config PATCH 的跨寫者 lost-update。但 accept 自身鎖下 atomic;clobber 源自 `update_project` 無版本整欄覆寫,任兩 config 寫者同款互蓋=#84 R10 已立案的 version-token 缺口(DR-002)。triage=reply-and-resolve:寫入點加註記 + followups 帳記 config 版本任務(GOV3 accept 列參與者)。**判準:一個新路徑「參與」既有結構缺口 vs「引入」新缺口——前者若自身正確且缺口跨全體、修法自成任務,reply-and-resolve 附可檢驗理由(update_project 無版本檢查=結構事實)並立案。**
**輪次結構**:GOV3-api gate-2 3+Codex 3,全落在同一寫入動作的接受判準;**「我的閘=別處的閘」型任務,設計期就該問「能不能只用別處那個閘」**,能就零 parity 測、零 drift、少三輪。GOV3 切 api/fe(採納/拒絕 UI 與 GOV2 治理面同落地)避免 over-claim。

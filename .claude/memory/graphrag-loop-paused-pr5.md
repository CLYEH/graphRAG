---
name: graphrag-loop-paused-pr5
description: loop 流程教訓 + 審查 lesson classes 目錄(LOOP step 8 retro 用)——PR/任務即時狀態一律查 TASKS.md 與 GitHub,不要信任本檔的狀態敘述
metadata: 
  node_type: memory
  type: project
  originSessionId: 3cfe1f1b-4e4e-4d2f-940f-12a9f9d7def4
---

**PR/任務的即時狀態請一律查 `TASKS.md` 與 GitHub(`gh pr view`/`gh pr list`)。這份筆記只保留跨任務可重用的流程教訓,任何「目前狀態」敘述在下次 push/merge 後就會過期,不可當作現況依據。**

- **教訓(監聽 Codex)**:Codex 的「changes wanted」走 **`pulls/N/reviews`(PR review + inline threads)**,不是 issue comment;+1 走 issue reaction。watcher 必須同時輪詢 reactions、reviews、comments 三個管道。
- **merge 授權(使用者 2026-07-03 定案,取代先前「由使用者跑 merge」教訓)**:agent 可自行 `gh pr merge`——mismerge 由 PreToolUse hook(`require-codex-approval.sh`:要求 head commit 之後的 +1、無 `eyes` 殘留、無未解 Codex thread,查詢失敗即擋)機械性防止。auto-mode 分類器若仍擋,向使用者轉述此授權即可,不要繞過 hook。此為「+1 已存在」的正常路徑;+1 拿不到時仍走下方「額度用盡」bullet 的 stop-and-ask,由使用者本人決定。
- **Codex P2/P3 意見判讀政策(使用者 2026-07-03 定案;已由 H2 寫入 LOOP.md step 7,以該文為準)**:P2/P3 級意見先判讀必要性——**必要**=違反 DESIGN 凍結保證/契約或文件內部矛盾/會誤導未來實作/真 bug;**非必要**=超出 DESIGN 文字的假設性強化、風格偏好、無互通性依據的凍結要求 → 回覆給出**該準則對應的可查核理由**(LOOP step 7 逐條列出:無條文要求/無行為差異則明述;可調項引 🔧 條文並指出無互通性依據;過度收緊引定義該合法情境的 §/DR)後 resolve 不修;講不出理由=該修。判讀模糊時 stop and ask。+1 門檻不變。
- **教訓(壓 review 輪次)**:Codex 傾向一輪只給一條;收到意見後要**主動掃整個 diff 的同類問題**一次修完,否則會一輪一條擠牙膏(P1 因此走了 7 輪)。
- **教訓(review race)**:push 後 Codex 可能仍對「舊 head」留 thread、隨後才對新 head +1;判讀 thread 前先確認它評的內容是否已被現 head 修掉,是則回覆指向修正 commit 後 resolve。
- **Codex 額度用盡時的收斂路徑(使用者行使過,H3/PR #9)**:規則的逃生閥是「stop and ask」——當其他 gates 全綠、剩餘未經 Codex 的 delta 小且已被本地執行級審查驗證時,由**使用者本人**決定 merge(從 web UI 或自己的終端;本機 hook 只認 +1,會擋 agent)。這是 owner 決定,不是 agent 例外。
- **教訓(執行級審查)**:純推理的 code review 連過兩輪都沒發現收據機制根本跑不起來(git 拒絕零位元組 index);審 shell/hook 這類基礎設施時 reviewer 要**實際執行**,並補執行級測試(tests/test_receipts.py 模式)。
- 慣例備忘:TASKS.md 勾選一律含在該任務 PR 內;PR 等 gates 期間可從 main 開下一個獨立 task 分支(LOOP.md 已明文)。
- **doc-only fast lane(H3 起)**:純 `*.md` 變更不開 PR、不經 Codex —— `docs/<id>` 分支 → doc-reviewer(sonnet)PASS → CI 綠 → fast-forward 進 main;push-gate hook 機械擋非 .md。等 Codex 用 `scripts/watch-codex.sh <pr>`(exit 0=+1/10=有意見/20=timeout/30=Codex 額度用盡——別再等,額度重置後再 `@codex review`),不要手寫 watcher。
- **並行分支的兩個坑(PR #15 實測)**:(1)review receipt 是單一槽位——task/* 與 docs/* 並行時互相覆蓋 PASS 章,push gate 會 fail-closed 擋下(對:再請 reviewer 蓋章;錯:自己蓋)→ H5 立案改 per-branch;resume 的 reviewer 給了 PASS **不會自動蓋章**,要明說「蓋章並回報 tree hash」。(2)doc lane 先進 main 會讓等待中的 PR 變 not-up-to-date → update-branch 產生新 head → 需要**新的 +1**(一輪 Codex 來回);等待中 PR 快 +1 時,doc-lane 的 main push 晚一點再做。

## 審查發現的 lesson classes(LOOP step 8 post-merge retro 的比對目錄;首次盤點=PR #2–#11,#11 當時尚未合併、僅計入其本地審查發現)
1. **契約驗值**(P1 七輪;**#11 再犯**:空白 disambiguator 鑄出不同鍵;**#12 再犯**:`must_have_valid_paths: false`／`groundedness_min: 0` 可當一個 case 唯一的「斷言」,讓 ≥1-assertion 下限被無作用值虛滿;**#13 再犯(跨欄位亞種)**:`default_mode: sql` × `text_to_sql.enabled: false` 自相矛盾的 config 通過驗證——同 PR 作者自己抓到了 enabled×空白名單這對,卻漏了 default_mode×enabled 這對同種矛盾):schema 只驗結構不驗值 —— 每個 required 欄位都要有型別/範圍/長度約束;識別子禁空字串;各 result 型別有內容最低要求;**每個 optional 欄位(不限字串)都要問「有沒有哪個值等於什麼都不主張?」**——no-op 值(false、0、空集)要嘛禁掉(const/exclusiveMinimum 通常就夠)要嘛明定語意;**跨欄位也要掃:列舉所有 (selector, 被 gate 的能力) 欄位對——default/toggle、enabled/whitelist、mode/子塊——逐對驗證矛盾組合被 if/then 拒絕,抓到一對≠掃完整類**。防線:contract 測試必含 rejection cases(mutator 模式)+ optional 參數的空白/no-op 等價測試 + 每個欄位對的矛盾組合 mutation;**reviewer 想以「schema 表達不了」為由把值級風險延後到 runtime 前,必須先實際試寫 schema 證明表達不了**(#12 的兩例都一行就表達了;#13 的跨欄位矛盾一個 root allOf if/then 就表達了)。**#15 再犯(DB CHECK 亞種)**:pipeline_runs 的 kind×build_id——作者以「kind 是開放詞彙、CHECK 無法列舉」為由只凍結 §27.7 點名的 ingest 一對;Codex 指出 §27.7 的 null 邊界是**窮盡式**(僅純來源驗證可 null)→ 修正=改從**例外側**表述(`build_id IS NOT NULL OR kind='source_validation'`),開放詞彙異議即消失。追加兩條:(a)spec「邊界」條目點名允許例外時,點名的例外就是唯一例外,約束照窮盡讀;(b)「表達不了」的舉證義務同樣適用 DB constraint——先試著把規則從例外側/反面寫一行。
2. **入口一致性**(#7/#8 多輪;**#11 再犯**:§17 的 `needs_review` vs `review_status` 的 `unreviewed` 詞彙分家;**#12 局部再犯但本地 gate 咬住**:新增第三個凍結契約時 DESIGN §26/CLAUDE.md 的 DR-002 列舉沒同步,code-reviewer 在 push 前抓到):規則改了但舊敘述殘留在其他入口(CLAUDE/LOOP/memory/索引)——改規則時 **grep 全部 tracked *.md** 掃同款措辭;**對凍結集合「新增成員」時,grep 既有成員的名字**(如 `mcp_response.schema.json`)找出所有列舉點一併補;**DESIGN 對同一概念給了兩套詞彙時,實作要同時接受/映射兩者並註明**。防線:doc-reviewer checklist 第 2 條。
3. **規則自洽**(#8 兩輪):meta 規則自身矛盾(要求提出不可能存在的證據=實質改寫分類)。防線:寫規則時逐分支驗證「每個允許情境都給得出所要求的證據」。
4. **工具語意**(#9 兩輪 + alembic cp950):CLI/API 輸出語意陷阱 —— rename 摺疊(`--no-renames`)、分頁預設 30 筆(`--paginate`)、文字比對≠值比對、locale 編碼。防線:凡依賴工具輸出做判斷,先實測該工具的邊緣行為。
5. **檢查者/消費者分岔**(#10 + H1 CI-skip):探測器檢查的目標與實際消費者的行為不一致(probe port ≠ driver port;skip 造成綠燈)。防線:checker 的參數一律從消費者同一來源導出。
6. **spec/實作逐字對齊**(P3 本地 blocker,同 PR 兩例:relation `norm(type)`、disambiguator 空白語意):凍結規格(fingerprint、契約)在 DESIGN 與程式要逐字一致並互相引用;凡「版本化凍結物」變更即升版。防線:code-reviewer checklist 第 5 條 + DR-007 模式;**審凍結規格模組時,先把 DESIGN 條文和 docstring 並排 diff**。
7. **執行級驗證**(H3 mktemp 死鎖):見上方「執行級審查」教訓 —— 基礎設施腳本要跑過才算審過。
8. **邊界語意 × 表示誤差**(#12:`is_eval_regression` 的「恰好等於門檻可容忍」被浮點減法打破 —— `0.8-0.5=0.30000000000000004`,naive `<` 把合約明文容忍的 exact-threshold drop 判成 regression;本地 reviewer 有執行驗證但只驗了幸運組合 `0.9-0.1` 就概括「不脆弱」):凍結規則帶邊界語意(恰好等於、off-by-one、上下限)時,**驗證要用對抗性取值**(二進位不可精確表示的小數、極端值),單一順手樣本驗過≠邊界成立;實作面比較浮點分數要帶明確 tolerance。防線:reviewer 對邊界宣稱至少驗兩組非精確表示組合;機械化=property-based tests(→ H4)。
9. **防護面完整性 × 過度阻擋**(C1b 立案,7 輪中的 3 輪:round 4–6):建一道「拒絕危險構造」的 guard 時,危險構造常有多個 sibling API 產生同效果 —— raw SQL 逃逸走 `text()` → `literal_column()` → 巢狀在 `or_`/`and_` 內 → `op()`/`bool_op()` 自訂運算子字串,同一道 guard 走了三輪(round 4 已一次收 `text()`+`literal_column()` 兩個 sibling,仍漏了巢狀深度與 custom_op)。這是 class 1「枚舉整個面、一個實例≠整類」母題換到程式 API 防護層。**但多一個 class 1 沒有的對偶**:收緊 guard 會**過度阻擋**合法 sibling —— round 6 一刀砍掉所有 `custom_op`,連每張核心表都要用的 JSONB `->>`/`@>`/`?` 都擋掉,只因加了「安全運算子仍通過」的正向測試才抓到。防線:(a) 看到一個被禁的 constructor,grep library surface 找出所有 sibling 並在測試裡組合/巢狀它們(× 所有巢狀深度);(b) 拒絕型 guard 的測試**必含正向 acceptance case**(Rule 9)—— 只驗「攻擊被擋」對過度阻擋是 false-green;(c) 判別 gate 在「值/字串」而非「型別」(round 6 修正=用 PG §4.1.3 運算子字元集判 opstring 安全,而非一律禁 custom_op)。已寫入 code-reviewer checklist 第 7 條。
10. **綁定時檢查 ≠ 不變量(TOCTOU)**(C1b round 7 立案):在 construction/binding 當下驗過的前提,若底層狀態會被並發改變,對「之後的」操作並不成立 —— writer 綁定時 build 是 building,activation 後它還能寫進已成 live 快照的 build。修法:把 recheck 摺進**產生變更的那句 statement**(原子)或持正確的鎖,別「綁定驗一次、之後信任」。防線:(a) 凡「驗證後回傳一個之後會被重複使用的 capability 物件」,問「這前提會不會在兩次使用之間被別人改掉?」;(b) 並發交錯要在**真基礎設施上實測**(C1b 用 `INSERT..SELECT..WHERE EXISTS(status='building' FOR SHARE)`:FOR SHARE 與 activation UPDATE 的 row lock 互斥,activation 阻塞到寫入交易結束 —— execution-verified,非推理);(c) MVCC 下「純 recheck」仍會 race 未提交的 activation(讀不擋寫),要嘛原子語句、要嘛鎖。已寫入 code-reviewer checklist 第 7 條。
11. **請求級不變量要一次掃完整個生命週期**(C8/PR#37 立案,deadline saga 5 輪:R1 latency reconcile→R3 hybrid wall-clock→R4 standalone sweep→R5 binding 納入→R7 剩餘預算貫穿):當需求是「對一個 REQUEST 的不變量」(延遲預算、配額、追蹤完整性),「每段各自有 cap」≠「請求有 cap」——各段跑滿自己的預算、或在段落交接時**重新起算**新預算,總量即複利爆表。防線:設計時列舉請求生命週期的**每一段**(binding/acquisition、selection、各 mode/phase、discovery、assembly),逐段對照不變量一次掃完;跨段交接傳「剩餘量」,不傳「全額」。已寫入 code-reviewer checklist 第 7 條。

**#17 大規模再犯(class 1 的 DDL 亞種;9 輪 Codex + 1 輪本地 FAIL,把 review 額度燒到用盡)**:整個 class 1 目錄原樣轉移到 DB constraint 層 —— nullable escape(evidence_hash NULL 逃過 dedup unique index:PG 把 NULL 視為互異)、每型別 provenance 最低要求、凍結身分鍵未設 unique(entity_key/merge_key=sorted pair/(document,ordinal) 位置鍵)、FK 未帶 scope 欄位(跨 build 混版可表示,DR-006 該結構性禁止 → **composite FK (id, build_id[, project])**,父表補 unique 目標)、contract minimum 的值域(offsets ≥ 0、span/ordinal 不反轉、quote ≤ 契約凍結的 512 —— DESIGN 標 🔧 但契約已凍值時**契約優先**)、hash 公式輸入完整性(evidence_ref 是 §27.4 hash 的來源分辨成分 → 全型別必填)、每個 child FK 都要 supporting index(PG 不自動建)。**防線=事前掃表清單(寫 schema/migration 時、first push 前逐格過,見 code-reviewer checklist §DB)**:(a) 每欄:NOT NULL 對齊契約 required、enum CHECK 對齊凍結詞彙、識別子/hash 輸入/契約 minLength 禁空、契約 minimum→值域 CHECK、定義性 sanity(end≥start、位置≥0);(b) 每表:每把凍結身分鍵一個 unique(含對稱鍵用 LEAST/GREATEST 表達式索引);(c) 跨表:composite FK 對齊 build/project、父表 unique FK 目標、每個 child FK 一個 supporting index;(d) 條件型:每個 (type, 依型別必備欄位) 對逐一 CHECK;(e) 發射路徑:對每個 result_type 把凍結 source_ref 需求逐欄回溯到 NOT NULL 儲存欄。**另兩個新教訓**:宣稱全稱的測試(「every child FK…」)必須枚舉全部實例——漏掉反例的全稱測試=false-green(本地 reviewer FAIL 抓到);**額度經濟學**——同類問題一輪一條會把 Codex 額度燒乾,事前清單掃完再 first push,把 N 輪換成 0-1 輪。

**#19(C1b,7 輪 Codex + 1 輪本地 FAIL,新增 class 9/10)**:DR-006 Postgres repo 層的能力邊界硬化 —— 連線私有化(round 1–2)、writer factory 驗證(2)、讀寫型別分家(3)、raw-SQL predicate 拒絕面(4–6)、寫入 TOCTOU 原子化(7)。**上方「教訓(壓 review 輪次)」的「擠牙膏/主動掃同類」再犯**:predicate 注入面走了 4/5/6 三輪;round 4 我已主動掃 sibling(text+literal_column 一起修)卻漏了「深度」(巢狀,round 5)與「另一 sibling」(custom_op,round 6)—— 完整的首輪掃應同時涵蓋 {所有 sibling constructor} × {所有巢狀深度} × {過度阻擋的正向測試}。**本地 FAIL 一次**(round 6 blanket custom_op 過度阻擋),靠「新增正向 acceptance 測試」的要求(class 9 對偶)抓到 —— 且我與 reviewer 各自獨立抓到同一 JSONB 假陽性類。收斂良好處:round 7 TOCTOU 修法在寫 code 前先於 live PG 驗四個性質(語句被接受、building 可寫、active 拒絕、activation 阻塞),避免推理型修法再開一輪。

**#20(C1c,0 輪 Codex,首輪 +1)**:class 9/10 + checklist 第 7 條在**設計期**預先套用(Cypher 面=結構性消除 splice point 而非 guard;跨庫 TOCTOU=PG row lock 錨定,live 實測)+ reviewer 唯一 actionable nit(模板掃描鍵在 clause 關鍵字會漏未來 CALL/UNWIND 模板)在 first push 前修掉 —— C1b 7 輪 → C1c 0 輪,額度經濟學與 lesson-class 目錄的預防效果首次得到量測驗證。無新 lesson class。

**#21(C1d,4 輪 Codex、6 個 finding,全 must-fix)**:與 #20 的 0 輪對照出核心教訓——**「family pattern 轉移」≠「per-surface 掃描」**:C1c 的面與 C1b 一一對應所以 0 輪;C1d 有四個「新面」,每個 finding 都是目錄裡**已有的 class** 套在新面上被漏掉:(1) `ensure_collection` 跳過 revalidation 的 deliberate-gap 理由(「不寫 build-tagged 資料」)被推翻——collection 建立會**凍結共用 schema**,副作用超出資料本身 → class 10 精煉:「write license 過期=停掉全部副作用」,deliberate-gap 理由必須列舉**完整 effect set**;(2) raw project key 直接當 Qdrant collection 名 → class 1 值域缺口新亞種:**從 contract 字串組合 identifier**(契約只要求非空,store 有自己的字元/長度限制)→ sanitize+content-hash 派生;(3) 40-bit hash 後綴 → **class 1 值域再一亞種「派生值的值域要按 threat model 定大小」**:守 INVARIANT 的派生值,其輸入是 contract-valid=攻擊者可影響,標準是**對抗性非意外性**(local reviewer 用意外模型審過它;Codex 供出真實 forged 碰撞對,40-bit 在暴力搜尋下數秒即破)—— 審 review 時先明講這值抵抗的是意外還是對抗,再判強度;(4) (point_type × source-id key) 跨欄位對 → class 1 跨欄位亞種再現,修法=unrepresentable(單一 source_id、key 由 type 派生);(5) ensure_collection 只驗存在不驗 schema 相容 → class 5 再現(存在≠消費者需要的性質);(6) source_id 用 str 承接 row id → 型別化(uuid.UUID)讓錯值變 type error。**local reviewer 同型失誤 ×4**(它自己承認:每次都「驗了狹窄的局部性質而非消費者面向的完整保證」)——Codex 每輪冷眼進場恰好抓的就是被排除的 surface/threat model。防線已寫入 code-reviewer checklist 第 7 條新增三條(per-surface inventory / name-the-threat-model / preflight 驗消費者性質)。無新 class;class 1(值域:identifier 組合、threat-model 定值域大小、跨欄位對、row-id 型別化)、class 5(存在≠消費者需要的性質)、class 10(完整 effect set)各得新亞種。

**#22(C2,1 輪 Codex、2 個 P2,皆 must-fix;pipeline 首個任務、首個消費 C1 repo 家族的一步)**:額度經濟學持續生效——C1d 4 輪 → C2 1 輪(兩 P2 同輪一起掃),per-surface inventory「事前答完八格」把輪次壓下來;但兩個 finding 都是**目錄已有的 class 打在 inventory 尚未點名的面上**:(1) `report.documents` 把「已存在(skipped)」的 document 從交給 `clean_document` 的 tuple 裡漏掉 → **class 5 的資料 handoff 亞種**:crash 後重跑(doc 已 commit、chunk 未寫)ingest 把 doc 判 skipped 卻不轉交,clean 收不到 → build 永久留一個沒切塊的 document,而 clean 的收斂式冪等正是為這個 retry 而建。差別於原 class 5(probe/consumer 埠位)——這是**生產者→消費者的資料轉交完整性**:「消費者能處理收到的」≠「消費者收到全部該收的」,skip/no-op 分支最容易漏轉;(2) `read_csv_rows` 守了 pk(缺/空/重複)卻放行空白 `table` → **class 1 複合識別子亞種**:§27.2 citation 是 (table, pk),守一個成員≠守整組,半個 citation 不可溯源。**local reviewer 在 C1d/C2 兩個「有新面」的任務連續同型失誤**(#20 無新面故無此失誤;它承認:驗了狹窄局部性質——clean 能收斂、pk 有驗——而非完整的生產者→消費者 handoff 與 sibling 欄位完整性)。防線=checklist 第 7 條 per-surface inventory 補**複合識別子逐成員**子句 + 新增**handoff 完整性(每個分支都要把消費者要的往下轉,尤其 skip 分支)**一條。無新 class;class 5(資料 handoff 完整性)、class 1(複合識別子逐成員)各得新亞種。

**#23/#24(C3a,2 輪 Codex、各 1 個 P2,皆 must-fix;#23 因分支改名被 GitHub 關閉,#24 為同 diff 後繼)**:兩個 finding 都是 **class 1 打在「函式參數形狀」這個 inventory 沒點名的面上**(inventory 只掃過 store/DB 面):(1) `source_ref = f"{table}:{pk}"` 天真拼接可碰撞(`('a:b','c')` vs `('a','b:c')`)且 mention/evidence 都以它去重 → 碰撞=靜默掉資料 → **class 1 複合識別子再一亞種「無損編碼」**:fingerprint `_join` 早解過同一題,新消費面又漏——凡 join 出來的值被拿去 dedup,就必須 length-prefix;(2) mappings dict key(路由 document)× `StructuredMapping.table`(命名 citation)兩欄位主張同一事實,typo 對=整表誤引 → **class 1 跨欄位亞種再現(#13 species)**,修法=進門先驗 key==table(拒絕勝於默選一邊)。防線=checklist 第 7 條 per-surface inventory 增「**模組自己的 API 參數形狀也是面**」+ 無損編碼與同事實雙欄位一致性兩子句。**流程教訓三則**:(a) governance CI 要求 `task/<id>` 分支恰勾 TASKS.md 的 `<id>` 項——**切片任務先把分支名對到子項再開 PR**(C3a→`task/C3a`);(b) **GitHub 分支改名不會 retarget 已開的 PR,會直接把它關掉**(#23 陣亡,Codex 重審一次)——改名要在開 PR 前做;(c) 使用者定案:**push 後先 `gh pr checks` 修 CI 再 triage Codex**(CI 快、Codex 慢,見 [[graphrag-ci-before-codex]])。另:一次誤把 C3a commit 打在 main(push-gate fail-closed 擋下,乾淨回復)——commit 前先確認 `git branch --show-current`;一次誤自蓋 receipt(H5:要請 reviewer 蓋)——已回頭請 reviewer 重蓋。無新 class;class 1 得兩亞種(無損編碼、參數面跨欄位)。

**#25(C3b,5 輪 Codex、各 1 個 P2,皆 must-fix——C1b 之後最貴的 PR;中途額度用盡(bot 連覆兩次 limits 訊息)直到重置)**:五個 finding 全部打在**同一個新面「不受信 LLM 輸出」**上,且每一個都是目錄已有 class:(R1) envelope 欄位錯型別逃出 failure boundary(一個壞 chunk 中止整個 pass)→ class 9(boundary 列了 JSONDecodeError 卻漏了同一輸入的 sibling 失敗模式);(R2) 欄位缺席被當「沒找到東西」→ skipped 非 failed,躲過 §27.7 retry → class 1 no-op 值亞種(缺席=另一種 no-op);(R3) 葉層 scalar 被 str() 強轉 → Python repr 鑄進 canonical_name/fingerprint → class 1 葉層值域;(R4) 純空白 quote 通過 truthiness+DB `<>''` CHECK → 不可稽核的證據 → **class 1 最老亞種(#11 空白≠空)再犯**——模組對每個 name/type 都 strip-檢查了,唯獨 quote 用 truthiness;(R5) endpoint join 用精確 tuple 而 store 用 entity_key 正規化 → class 5 再現,修法=同一把身分函式(join 鍵直接用 fingerprints.entity_key)。**元教訓:R1–R4 是同一棵值樹被一輪掃一層**(envelope→陣列→葉→葉語意)——首輪就深度優先掃完整棵樹+每個葉套完整值域清單 {缺席, 錯型別, 空, 空白, 詞彙外, 不可定位},四輪縮成一輪;R5 補一條:凡從不受信輸入導出的 join/dedup 鍵,必須用 store 自己的身分函式。防線=checklist 第 7 條新增「untrusted-input value tree」bullet。**流程收穫**:owner 指示下 watcher 增 exit 30(額度用盡偵測,執行級驗證過)+ 排程在額度重置時間自動 re-poke——等待不再盲目;CI-first 全程維持(每輪 poke 前 CI 先綠)。無新 class;class 9/1/5 各得亞種(class 1 ×3:no-op 缺席、葉層強轉、空白 quote)。

**#27(C3c,0 輪 Codex 首輪 +1;新表+migration)**:預防鏈首次在「新表」任務上全程生效——DDL 清單 first push 前掃完,local reviewer 一次 FAIL 擋下兩個 blocker,Codex 零輪。兩個 blocker 都值得目錄化:(1) **class 1 條件對亞種「IFF 要寫成雙分支 disjunction」**:`(status='proposed') = (by IS NULL AND at IS NULL)` 在 decided 側只要求 NOT(兩者皆 NULL)=至少一個非 NULL——匿名決策(有時間無人)與無時決策(有人無時間)都過;正確寫法=顯式雙分支 `(proposed AND 皆 NULL) OR (非 proposed AND 皆 NOT NULL)`(sibling 先例:review_ledger 兩欄直接 NOT NULL)。凡把 IFF 綁在「合取」上,false 分支就被弱化——寫 CHECK 時逐 corner 展開(2^n 格)驗證;(2) **#17「全稱測試要枚舉全部實例」再犯但被本地攔下**:我的「both directions」測試只驗了弱 CHECK 恰好會拒的兩個 corner,對「不會匿名」的宣稱是 false-green——修正後的四 corner 測試在弱 CHECK 下確實 FAIL(可辨別性驗證過)。防線:checklist 第 6 條條件型子句補「IFF 顯式雙分支」。另:migration 已在本地套用過再改 CHECK 時,要 downgrade→upgrade 重套,否則整合測試打在舊約束上(執行級驗證又抓一次)。無新 class;class 1 得一亞種。C3 家族完結:C3a 2 輪、C3b 5 輪、C3c 0 輪——per-surface 儀式跑完整的任務收斂到 0。

**#28(C4,5 輪 Codex、8 個 finding 皆 must-fix——實體解析的語意接縫)**:全部落在「ER × 凍結身分/審核模型」:(R1a) 有 disambiguator 的同名實體被 1.0 分自動合併 → **class 1 新亞種「鍵的組成本身是斷言」**:entity_key 內嵌外部 id 就是在說「這兩個是刻意分開的」,消費者只拿鍵做相等比較、卻無視組成所斷言的語意;修法=`entity_key(type,name) != stored_key` 精確判定 disambiguator 存在(公式凍結所以判定精確),同名異鍵對僅限 ledger 明令合併;(R1b) 同 build 內 approve 無法救回先前 reject → class 5(precedence 函式與套用程式碼分岔);(R2a) **class 9 對偶再犯**:R1a 的保護把單邊 id 的同名對也擋死(id-less mention 本來就該併進 id 實體)——收緊 guard 的同一輪必須掃 positive acceptance,目錄早有此規則,語意層再犯一次;附加語意:單邊合併時 id 側強制為 canonical(身分強度>提及數);(R2b) relation ledger 在 re-mint 前跑 → class 5 時序亞種(checker 跑在 consumer 的值誕生之前)→ re-mint 內重套;(R3) rejected 關係跳過 re-mint cascade → 修法原則「**身分無條件、狀態才有條件**」;(R4a) defer 沒擋 auto-merge → class 1 詞彙×分支矩陣缺格;(R4b) 舊簽章的裁決跨 re-mint 靜默沿用 → class 6 spec 對齊(§27.3:裁決鍵在指紋上,指紋死了裁決不轉移)→ needs_review;(R5) **本 PR 最貴的教訓**:R4 自己引入 needs_review 狀態卻沒重掃所有 status 分支——approve 無法把 needs_review 救回投影。**我在 R3 總結裡親筆寫下「加狀態要重走所有分支」、下一輪就違反**:mid-PR 筆記≠防線,防線=機械性的 state × verdict 全矩陣表(已入 checklist)。無新 class;class 1 ×2、5 ×2、6、9 各得亞種+一條 meta 規則。

**#29(H8,3 輪 Codex;起因=owner 現場抓到 watcher 沒偵測到額度用盡)**:poke→watch 競態——Codex 秒回,limits 回覆落在 poke 與 watcher 啟動之間,對所有 `> START` 過濾器隱形,盲輪詢到 timeout。修法演進:bootstrap 向後看+**取代語意**(最新 quota 訊息 governs,除非更新的 +1/review/poke/非 quota bot 留言取代它;15 分鐘視窗第一版被實況數據幾分鐘內推翻——**中斷比任何視窗都長,取代才是對的規則**)→ 三輪 Codex 全是 bootstrap 自身的邊界:只抑制不呈報(同競態的另一半→bootstrap 全面化:poke 後最新 bot 事件按 poll loop 同規則分類,+1 帶 burst 優先權——approval 附帶的 all-clear 留言晚 1 秒被實測抓到)、無 poke 錨(空錨=epoch:零 poke=零 ack,一般化而非特例)、同秒 tie(created_at 一秒解析度,tie=回覆)。**工具程式碼吃到與產品程式碼相同的 class 1/8 邊界審查——理所應當**;執行級驗證自己先抓掉兩個 bug(視窗謬誤、主機沒有獨立 jq——本 repo 慣例=gh 內嵌 --jq)。probe-codex.sh:額度中斷恢復迴圈(poke→分類→睡→重試,封頂,真回應立即傳播),實戰接住 00:12 reset(2 分鐘內)。

**#31(C6a,4 輪 Codex、各 1 個 P2,皆 must-fix;首個「檢索讀取/發射面」任務——讀不受信投影(Qdrant)、發射凍結契約 §16)**:四個 finding 全是目錄已有 class 打在**新面「retrieval read/emit」**上——per-surface inventory 只掃過 write/store 面,沒點名「從投影讀值→對 SoR 覆驗→發射契約」這條讀側面。(R1)payload 的 chunk_id/entity_id 非 UUID → `uuid.UUID()` raise 中止整個 query → **class 9/1 讀側「不受信投影 value tree」**:#25 的 untrusted value tree 在 write 側(LLM 輸出),這是同題的**讀側對偶**——投影因 §19 drift/corruption 不可信,每個 payload 值到輸出前都要 validated-or-DROPPED(丟棄不 raise),丟棄計入 PARTIAL_RESULTS(§22);修法=單一 `_payload_uuid` 三處解析點共用。(R2)空 build 從沒 embed → index 的 lazy collection 從沒建 → semantic query 對缺失 collection 的 Qdrant 404 直接 propagate 成 500 → **class 5 生產者狀態亞種**:lazy-creation 生產者合法產出「無 collection」狀態,消費者必須降級為空而非 500;修法=vector repo 對 404 回 `[]`/`0`(非 404 re-raise,別把 outage 洗成空答)。(R3)壞 canonical_id/text(非字串 truthy)直接進 `RetrievalResult.id`/`text`,dataclass 不強制 annotation → `to_dict()` 發出非字串 → **整個 §16 response schema-invalid** → **class 1/9 讀側值域→契約**:身分欄位一律取 validated uuid(不取 raw payload 字串)、optional 顯示欄位 `_payload_str` 壞值 coerce None(hit 仍可引用,只丟顯示欄)——一輪掃完**所有** payload→輸出的值(不只 Codex 點名的 canonical_id)。(R4)實體被 resolution 改成非 active 後 forward-only 投影留下 stale point → `mentions_by_entity` 只按 project/build/id 濾、沒濾 status → 非 active 實體被當有效結果發射 → **class 5 SoR 覆驗不對稱**:chunk hit 已覆驗 SoR 存在(row 不在就丟),entity hit 卻沒覆驗 active status;修法=join 加 `status=='active'`,非 active → 零 mention → `_entity_result` 當 drift 丟。**元教訓:retrieval enrichment 讀不受信投影、發射凍結契約——每個跨 projection→output 的值都要 validated-against-SoR-or-dropped,且要對**所有 result_type × 所有 SoR 不變量(存在、status)**對稱掃完**;forward-only 投影意味 derived store 會留 SoR 已排除的 row。**local reviewer 通過原始 C6a 並漏掉全部四個**(同 C1d/C2 的「驗窄局部性質而非消費者面向完整保證」失誤)——讀側面是新 inventory face。防線=checklist 第 7 條新增「retrieval read/emit surface」bullet。**流程**:watcher bootstrap 向後掃在每輪重新浮出「已解決的舊 comment」(exit 10 假陽性,因 resolved thread 的 comment 早於 watch 起點)——benign,直接改用 reaction/unresolved 直接輪詢繞過。無新 class;class 9/5/1 各得讀側亞種。C6 家族開場:C6a 4 輪(新面),預期 C6b–e 隨面對齊收斂。

**#32(C6b,19 輪 Codex + 2 輪本地 FAIL——史上最貴,遠超 C1b 的 7 輪;NL→SQL guardrail,被守面=整個 SQL 文法)**:class 9「防護面完整性」的語言級最大再犯——reject surface 的 sibling 不再是 library API 而是**查詢語言的每個文法子句**,一輪一條擠了 19 輪:column-alias list(`FROM t AS a(c1,…)` 可把資料欄改名到 `__row_pk` 偽造 citation)、TABLESAMPLE(WHERE 前任意抽樣=靜默漏列)、OFFSET(靜默跳列)、非整數 LIMIT(`2::int` 繞過列數上限)、positional ORDER BY(`ORDER BY 1` 排到隱藏 citation 欄)……防線=**文法驅動的事前掃描**:守查詢語言時按文法子句清單逐格過(FROM 修飾:sampling/alias-col-list/qualification;SELECT 修飾:DISTINCT/INTO/locking;列選:WHERE/OFFSET/LIMIT 運算式形;排序:positional/named;set-op/CTE/subquery/join;函式;placeholder)× 每格的過度阻擋對偶正向測試。**對照 C1c:參數化模板結構性消除整個面=0 輪;C6b 的 NL→SQL 依 §27.6 凍結必須走 guard,面=全文法——面越大,事前掃的報酬越高**。R16(casted LIMIT)、R17(positional ORDER BY)兩個 base P2 皆 Codex 發現;但**我的首版修法各含一個 gap,由本地 reviewer 於 push 前 FAIL 攔下、修正併入同一 commit**——所以 commit 標題只見「(Codex P2)」、git/GitHub 上無獨立的本地 FAIL 痕跡;本地審查的證據只存在 session 內,retro 記載這類事實時必須寫明「證據在哪、為何 trace 上看不到」,否則事後查核者只能推翻它(本條初稿正因此被 doc-reviewer FAIL 一次):R16 我用 `not is_string` 當 gate,放行 float/scientific literal(`LIMIT 2.5` → `int()` 500)=class 5 proxy 檢查(要守 `is_int` 這個消費者真正依賴的性質,不是「非字串」);R17 我用 bare isinstance,漏 `Paren(Literal)` 括號 ordinal(PG 認 `ORDER BY (1)` 為 positional)=class 9 nesting-depth 規則在語法樹上再犯。對照 #31(本地 reviewer 漏掉全部四個),這次它在 fix-review 層真實生效。其餘亞種:R18 非字串 pk 被 `->>` 強轉 text,`isinstance(pk,str)` 跑在強轉之後=看不穿 → **檢查要放在強轉發生的層**(SQL 內 `jsonb_typeof='string'` gate,不是 driver 端 isinstance)=class 5;R14 `statement_timeout` 是 per-statement 不是 per-phase(class 4 工具語意)→ N 表 N 倍 deadline,批次成單一 statement(附帶 `AS t` 撞 SQLAlchemy `Row.t` 屬性=另一則工具語意,整合測試抓到);R15 `SET LOCAL` 自身失敗發生在 try 安裝前 → aborted txn 無 rollback 毒化連線池=class 9 failure boundary 要涵蓋 setup 自身失敗(#25 R1 同型);R13 >63-byte JSON key 被 PG 靜默截斷成錯欄名=**store 的表示極限也是值域**(class 1);R19 probe row 只在 policy cap 綁定時有意義,超取讓 PG 對「抓回但未被請求」的列評估 WHERE/cast(class 4 執行語意——與 R14 同主題:工具在邊界上的實際行為≠直覺)。R8 使用者定案 connection ownership(per-phase txns,loaned-clean)。流程:owner 於 19 輪後介入「太多輪,收斂」→ 授權邊際意見 reply-and-resolve(實際每條皆真 bug 皆修);merge 由 owner 明示同意後執行(auto-mode 分類器誤攔一次 +1 已在的 merge——依上方 merge 授權 bullet 轉述即解)。執行級驗證全程生效且多次推翻直覺(jsonb 函式 rendering、`Row.t`、Paren AST、identifier 截斷皆先在 live PG/sqlglot 驗過才修)。無新 class;class 9(文法面、failure boundary)、class 5(proxy 檢查、強轉層)、class 4(工具語意×3:statement_timeout scope、Row.t、probe-row 執行語意)、class 1(表示極限)各得亞種。C6 家族:C6a 4 輪 → C6b 19 輪(SQL 文法=史上最大面)→ C6c(Cypher 模板,C1c 已證 0 輪路徑)預期回落。

**#33(C6c,5 輪 Codex、8 個 P2 皆 must-fix;參數化 Cypher 模板=結構性消除語言文法面)**:預測部分兌現——C6b 19 輪的 guard-文法 finding 在 C6c 是 **0 個**(無 API 收 query text,唯一動態槽=validated-int hop 插值),8 個 finding 全部落在「不可信投影的 read/emit 覆驗」與「資源上限」兩層:(R1a) subgraph 只 cap entities、edges 不 cap 且 fetch 無界=**§21 cap 的防護面不對稱**(class 9:cap 也是 guard,每個 result 來源都要枚舉);(R1b) path 只試第一對 endpoints(多 disambiguator 名字=多 seed 要窮舉)——修法預先套用 R14:全 pair 共用單一 deadline;(R2) SoR 驗證在 pair 迴圈**外**→ stale 候選終止整個搜尋(checker 位置錯:驗證必須在迴圈內、失敗 continue);(R3a) neighbors 只驗**目標**實體、不驗**走過的邊**——path 模板驗每條邊而 neighbors 不驗=read/emit 對稱缺口(#31 R4 的 SoR 覆驗不對稱,再深一層:值→status→**邊存在→可達性**)→ 修法=SoR-active 節點+關係上重算 BFS 可達性,投影距離不信;(R3b) **同 PR 內的同 class 再犯**:R1b 才修過 path 的 per-pair deadline,neighbors 的 per-seed 迴圈同型漏掃(seed_count × timeout)——**修 finding 時要先問「同 class 還打在哪些 sibling 面/參數」**;(R4a) R2 的深化:stale 的「最短」路遮蔽同 pair 較長的 active 路 → shortestPath 模板加 rel/node-local 排除謂詞(live 驗證 pushdown:排除 stale 邊後回較長 active 路)+ 同 pair 重試迴圈(排除集單調增→終止);(R4b) **round-3 本地 reviewer 的 optional nit 被 Codex 升級成 P2**(TRUNCATED 在 drift+clip 並發時被吞;fetch 時判斷)——此為 session-only 事實(本地審查的 waive 不留 commit/comment 痕跡;trace 上唯一佐證=修正落在 round-3 修復 commit 90ed539 的下一個 commit 80638c5)——教訓:指向正確性/精度的 nit 一律立即修,「no change required」不是不修的理由,是 reviewer 客氣;(R5) 非字串 entity 在 `.strip()` 先於 isinstance → 500——hops 才修過同型,**同函式的 sibling 參數沒掃**(class 9 最小再現)。**元教訓兩則**:(a) 「stale 投影」的完整覆驗面={值 corrupt、節點 status、邊存在、可達性、最短性}——#31 的 read/emit bullet 只列到 status,traversal 級(後三項)是 C6c 補上的新深度,已寫入 checklist;(b) 修 finding 的同時掃 same-class siblings(跨模板、跨參數)——R3b/R5 各多花一輪,都是掃一下就能省的。無新 class;class 9(cap 對稱、sibling 參數)、class 5(checker 位置、覆驗深度)、class 4(per-phase deadline ×2)各得亞種。C6 家族:C6a 4 → C6b 19 → C6c 5(文法面消除生效;殘餘=投影不可信的深度)。

**#34(C7,1 輪 Codex、1 個 P2;本地 reviewer 2 輪 FAIL 先攔——Leiden 社群摘要)**:核心 class=「**錨定冪等/可重現性的派生值必須是集合的純函數,不能依 fetch 順序**」(class 4 工具語意:PG fetch order 非 rerun-stable × class 1 值域):(本地 FAIL 1)leidenalg 對頂點編號順序敏感——reviewer **實證** 20 次重標記中 4 次同圖同 seed 分割不同——而頂點編號來自無序 fetch → rerun 重算出不同 member set、skip 失效、重複報告;修=排序後編號。(本地 FAIL 2)我的「強化」測試仍 false-green:雙三角形圖是**排列不變**的(reviewer 枚舉 720 種排列=同一分割;revert sort 測試照過)——**決定性/排序類主張的測試 fixture 本身要有鑑別力**(模稜圖:環+弦),且**提交前先跑 revert-probe**(sort 註解掉→測試必須 FAIL;本次在 session 內執行級證明後才過)。(Codex R1)prompt 的 relation 樣本未排序就切片——同 class 的 sibling(頂點編號才修過、同函式旁的切片沒掃),**sibling-sweep 規則寫進 checklist 的同一天就再犯**:規則要在「實作/修復當下」執行,不是等 review 提醒;修=穩定 tuple 排序後切片,測試=prompt 在 relations 反序輸入下 byte-identical。兩次本地 FAIL 為 session-only 事實(FAIL 與修正皆折入 push 前的單一初始 commit 7462d36,trace 上無法區分修前/修後版本;20 次重標記與 720 種排列的實證數據僅存在本次 session——Codex R1 的修正則是可見的 commit ec8095f)。結構面收穫:圖來源=PG SoR(完全不碰投影,C6c 的 staleness 整類結構性迴避);LLM value tree/失敗邊界/§27.7 stable ref 首輪即過(C3b 模式移植成功)。無新 class;class 4/1(fetch-order 純函數化 ×2)、#17 全稱測試(fixture 鑑別力+revert-probe 前置)各得亞種。輪次:C6a 4 → C6b 19 → C6c 5 → C7 1——本地執行級審查(20 重標記/720 排列枚舉)攔住了 Codex 會抓的最大項。

**#35(C6d,2 輪 Codex、各 1 個 P2;global mode——SoR 直讀,無投影層)**:兩個 finding 都在修正我的「SoR 可信」推理邊界:(R1) `member_entity_ids` 是**無 FK 的裸 uuid[]**(僅非空 CHECK),發射 entity refs 前未對 build-scoped entities 接地——手寫/畸形列可鑄出 ungrounded 或跨 build 引用 → **信任邊界是逐欄位的,不是逐 store 的**:SoR 內無約束的參照欄(裸 uuid[]/text ref)與投影值同等不可信,發射前必須接地(class 1「映射他表的 id 要驗證」#21 亞種);伴隨教訓:**整合 fixture 用隨機 uuid 當 member=fixture 沒滿足受驗性質**(Codex 由此看穿)——檢查驗什麼,fixture 就要供什麼;(R2) 接地查詢單一 IN 對全部 claims=每 id 一個 bind,撞 PG 32767 bind 上限 → 大 build 整個 global 掛掉 → 分批(1000/批,決定性排序,seam 處 union)——**協定/表示極限也是值域**(#32 的 63-byte identifier 教訓在 bind 協定上的 sibling)。測試紀律的落地:R1 修復即用 predicate-honest fake(fake 只回答 IN 實際點名的 id,union-at-seams 斷言不可能 false-green);上一輪本地 nit(id tiebreak 覆蓋)按新規則立即修並跑 revert-probe(session-only:修正折入 push 前的單一初始 commit 6cb2f4d,trace 上無法區分修前/修後版本)——#34 的兩條新紀律首次全程執行,C6d 收在 2 輪。無新 class;class 1(逐欄位信任邊界、fixture 滿足受驗性質、bind 協定上限——表示極限即值域,與 #32 的 63-byte identifier 同 class)各得亞種。輪次:C6a 4 → C6b 19 → C6c 5 → C7 1 → C6d 2。

**#36(C6e,1 輪 Codex、1 個 P2;hybrid router+RRF 融合+routing trace——C6 家族收官)**:finding=**doc/impl 內部不一致**(LOOP 準則的 must-fix 類):docstring 承諾「任何 out-of-vocabulary selector 輸出 → fallback 全模式」,實作卻只在有效交集為**空**時 fallback——混合答案(`["semantic","teleport"]`)保留有效半邊、靜默縮窄檢索。修=**部分有效=整體失敗**(untrusted 輸入的 value tree 新格:MIXED membership):答案中任一模式不在 offered available 集(幻覺或 real-but-gated 皆屬不遵約)→ 全答案不信任 → 全 available 模式執行、extras 記入 routing reason;gate 絕對(distrust 回傳=available 集,結構上排除 gated)。教訓:對 untrusted 選擇型輸出,「交集後繼續」是靜默縮窄——**寬度優先的 fallback 必須以整個答案為單位**,且 doc 承諾的失敗規則要逐字對照實作(這正是 doc/impl 分岔被 Codex 一眼抓到的原因)。結構面:C6e 首輪即過的部分——selector 值樹 7 個壞形狀、gating 先於選擇、mode 崩潰降級(§22 逐模式邊界)、RRF 決定性、debug/trace 真實性、fixture 的 §27.2 合規(chunk ref 帶 offsets——schema 驗證在單元測試裡自己抓到)——#31-#35 的教訓全部前置消化。無新 class;class 9(untrusted 值樹的 MIXED-membership 格)得亞種。輪次:C6a 4 → C6b 19 → C6c 5 → C7 1 → C6d 2 → C6e 1。C6 家族完結:檢索五模式+summarize 全數落地,per-surface 儀式的收斂曲線成立。

**#37(C8,9 輪 Codex、12 個 P2、2 個本地 blocker;per-project MCP server——§9 facade)**:收斂曲線在**新 surface class** 上回彈(檢索模式的教訓不自動涵蓋 facade/wiring/lifecycle 面)。主敘事=**deadline saga,5 輪才收斂**(R1 latency reconcile→R3 hybrid wall-clock→R4 standalone sweep→R5 binding 納入→R7 剩餘預算貫穿):同一個邏輯需求「§21 cap 約束整個 MCP 呼叫」被 Codex 一輪一格地逼出來。**新 class #11:請求級不變量要一次掃完整個生命週期**——當需求是「對一個 REQUEST 的不變量」(預算/配額/追蹤),設計時就列舉請求生命週期的每一段(binding、selector、per-mode、discovery、assembly)逐段對照不變量,而不是等審查者逐格開票;「per-phase 有 cap」不等於「整體有 cap」(各段各自滿額+重新起算=總量爆表)。其他 findings:R1A 各 factory 獨立查 active=綁定級 TOCTOU(class 10 亞種;修=單次 lookup);R2 wheel 不含 contracts=部署形態分岔(class 7 執行級驗證:實際 build wheel 驗證);R6 成功 payload 漏 build_id(staleness 可偵測性;class 2 亞種:同一工具的 success/error 形狀不對稱=同面多路徑的入口一致性);R8 store 例外家族→typed STORE_UNAVAILABLE(class 9;「NOT Exception」守住 bug 照樣 loud);R9 **便利工廠收裸 uuid=圍籬→紀律的降級**(class 9 亞種:對受圍籬面新增旁路入口,旁路必須攜帶等強證明;修=ActiveBinding capability proof,InitVar 封 dataclasses.replace 偽造——本地 reviewer 驗證過的硬化)。**Process failure(owner 抓到,非流程抓到)**:integration CI 連紅 5 push——call-through 測試進真 lifespan 要 OPENAI_API_KEY,本機 .env 有、CI 沒有=**本機綠≠CI 綠的環境分岔**;而 watcher 印了 `integration fail` 卻只等「非 pending」就去看 Codex(CI-first 規則被腳本邏輯繞過)。修=factory fake(測 seam 不測 vendor client)+watcher **fail-stop on CI red**(紅燈即 exit 1,絕不進 Codex 段)(watcher 行為屬 session 記錄,attribution 以 commit `82b7983` 訊息為 trace 錨點)。本地 reviewer 亦攔 2 blocker(semantic top_k 門檻缺口=sibling 不對稱;round-5 Fix B 零測試)。輪次:C6a 4 → C6b 19 → C6c 5 → C7 1 → C6d 2 → C6e 1 → **C8 9**。教訓總結:新 surface class 開工前,把催化的不是「同 class 檢查」而是「這個 surface 的不變量是什麼、它的 scope 是哪個生命週期」先寫下來。

**#38(C9,8 輪 Codex——1 P1+9 P2、3 個本地 blocker;build lifecycle:activate/rollback/diff/prune)**:又一個新 surface class(狀態機×並發 admin 面),曲線再度回彈。主敘事=**class 10 在同一 PR 連開五格**:R4 prune snapshot TOCTOU(修=FOR UPDATE 認領,單一 tx 涵蓋投影+PG 刪除)→R5 crash 窗口(preflight 是 bind-time 知識;修=鎖內重驗 drift)→R6 rollback 目標選擇在序列化之外(修=pg_advisory_xact_lock 專案級生命週期鎖,選擇+晉升同鎖同 tx)→R7 非終態 victim(building=live pipeline;修=_PRUNABLE 終態白名單,兩處檢查同步)→R8 排序 proxy 偏差+時鐘混源(修=archive 時 backfill 位移時刻+**單一時鐘源**:排序性時間戳全走 PG now(),app clock 與 DB clock 混用會被 skew 重排歷史)。**class 10 深化:狀態機×並發面要做「操作×狀態×交錯」矩陣掃描**——單點修一格,Codex 就逐輪開下一格;設計時自掃矩陣(每個操作對每個狀態的裁決+每對操作的交錯)能壓掉大半輪次。其他:R1 **P1**=REL 邊謂詞不 mirror 投影 write shape(class 5 檢查者/消費者分岐:自寫 Cypher 而非對齊正典 _RELATION_COUNT;acceptance 缺「有投影關係」cell 讓零計數謂詞溜過→**guard 的 acceptance 要覆蓋每個 population cell**)。R2=diff 跨專案混算(build-only 表需成員驗證)+merge_candidates 漏列(class 9 枚舉;修時全面盤點 build-scoped 表並記錄排除理由)。R3=NULL activated_at 謂詞(class 1 亞種:值域 nullability cell)。本地 reviewer 3 blocker:**drift 母體=投影母體而非原始列數**(class 9 over-block dual 的最佳例——merged/rejected 留在 SoR 是設計;acceptance=真投影 resolved build 必過)、round-4 Fix 零測試、docstring 殘留被棄方案(換方案要回頭掃敘述)。輪次:C6a 4 → C6b 19 → C6c 5 → C7 1 → C6d 2 → C6e 1 → C8 9 → **C9 8**。與 #37 合看:新 surface class 首 PR 輪次高是結構性的——開工前寫下「surface 的不變量+生命週期+狀態機矩陣」是唯一已知的壓縮手段。

**#39(C10,17 輪 Codex——3 P1+22 P2、5 個本地 blocker 觸發點;eval harness——史上最長 PR)**:裁判面(scoring gates production)的每個啟發式格都是 gate 完整性問題,收斂失敗的解剖:(a)**關係匹配語意鏈**(R2 方向→R4 連接段→R7 端點恆等;R2 同輪另有不相干的 answer_regex casefold 修正)與(b)**targeted-lookup 鏈**(R11 建立→R14 雙庫→R15 精確探針→R17 降級圍堵)——每修一格就開下一格,反應式補格是裁判面的反模式。**教訓(class 11 深化之二):評分/裁判面先寫完整語意規格——身分模型(什麼算同一個端點/邊)、雙庫一致性(SoR∧投影)、降級行為(§22)、可比性(suite+模型指紋)——否則語意是被審查者逐格逼出來的**。其他要點:R16A builds.eval 專欄早在 migration 0001(class 6:寫碼前先讀 schema——我發明了 metrics['eval']);gate 決策表被逼到 fail-closed 全格(unscored/bootstrap/failed>0/指紋缺失全擋,唯一 vacuous=無 active 時的回歸比較);answer_similarity 誠實不發(golden 無參考答案欄);本地 reviewer 5 blocker 多為判別性測試缺失(revert-probe 紀律持續發威)。**Process 事故(owner 抓到 #2,trace 錨=PR#39 comment 18:08Z)**:Codex 核可有**兩種格式**——push 觸發的自動審=+1 reaction;poke 觸發的審=「Didn't find any major issues」comment(無 reaction)——watcher 只盯 reaction/threads,對乾淨 comment 全盲,還在 PASS 後繼續 poke 觸發新輪。修=三態 watcher(+1/新 threads/乾淨 comment 指名 head)+**乾淨 review 後絕不再 poke**。**Owner 收斂指令(常設)**:輪次過多時逐條判「必修 vs reply-and-resolve」,劃清 task 邊界,不無限上綱;must-fix=gate 完整性/資料正確性,邊角遞進→resolve+記 follow-up。輪次:4→19→5→1→2→1→9→8→**17**。

**#40(C11 observability §18/§19,8 輪 Codex——11 P2、1 本地 blocker + 2 次本地 reviewer 攔假綠、1 邊界 resolve;Track 1 最終任務)**:兩個新 surface(recorder 寫路徑、health 讀路徑)都用「語意規格先行」開場(C10 教訓落地——module docstring 開頭即完整語意),但 Codex 仍逐輪逼出**兩條完整性鏈**:(a)**pending_review 佇列**——round4 補 §6 待審池(ontology proposals)→ round8 補 §17 全狀態(needs_review 實體/關係 + deferred merge;第四項 needs_review_relations 首版無判別測試——status/review_status footgun 會 false-green——**同樣被本地 reviewer FAIL 攔下**,補判別 cell + revert-probe),**教訓:「X 佇列」型指標的分母是 spec 定義的整個狀態機,不是你先想到的那張表——開工前把 §17 待審狀態全列出,否則同一類 false-dark 會分兩輪逼出**;(b)**recorder 寫入完整性**——JobStatus 詞彙(**唯一的本地 blocker**——succeeded 違反凍結 enum,成功路徑必炸卻被「每個測試都注入 failure」遮蔽,Codex 首審前即由本地 reviewer 攔下)→ dedup 撞唯一索引 → verbosity 讀 settings(廣告了卻沒接線的 dead config)→ loaned-clean 圍籬(無條件 rollback 會毀呼叫方未提交寫入——改用 C6b sqlreader refuse-loud idiom)→ build 綁定 FOR SHARE(無 FK,cross-project/pruned build_id 誤掛——class 10 TOCTOU 直接套用)→ failed 支配去重(§27.7 retry 讀持久化 failed 列,first-wins 會讓 skipped 蓋掉 failed 而漏重跑)。其他:health status_light 抽純函式 + 16 格決策表全釘;drift 重用 lifecycle 同一 checker(class 5 不分岔);drift 探針對 Build-failed 短路 + 對 store 故障降級成 STORE_UNAVAILABLE warning(「不可達=未測量,非 drift」,同 eval 燈的只報實測事實原則);**NULLS LAST(Codex round 6 找到的 bug):coalesce(now()) 讓未啟動 build 排最新、遮蔽較新回歸候選;我的修正卻踩 class 8 false-green——假 conn 繞過 SQL ORDER BY,revert 不破任何測試,被本地 reviewer FAIL 攔下 → 補真庫判別 + 親自 revert-probe;同類掃到 list_builds 一併修**。收斂紀律奏效:round3(orchestrator 是 BA1/BA2 交付物)以 task 邊界 reply-and-resolve 不動碼;其餘全是真資料正確性 bug,無边角過度上綱。**里程碑:Track 1 核心引擎全數完結**(C1–C11);下一站 Track 2 BA0(API skeleton)。輪次:4→19→5→1→2→1→9→8→17→**8**。

**#41(BA0 API skeleton,4 輪 Codex——6 P2、0 本地 blocker;Track 2 起點)**:第一個 FastAPI 任務,Codex 全 6 條開在**同一個 surface——error-envelope 邊界**,是一條完整的「例外處理器完整性」鏈:(1)HTTPException 漏 Starlette 的 `{detail}`(內建 handler 比我的 Exception handler 更 specific,繞過它)→(2)validation error 的 `exc.errors()` ctx 帶原始 ValueError(非 JSON)→ JSONResponse 序列化炸成 500 →(3)ApiError.details 帶 UUID/datetime 同樣炸 →(4)5xx 回顯 `exc.detail` 洩漏後端故障資訊 →(5)丟棄 `exc.headers`(405 Allow / 未來 WWW-Authenticate/Retry-After 協定提示)→(6)保留 HTTP status 卻發 coarse code(503 配 INTERNAL,但契約保留 503 給 STORE_UNAVAILABLE——client 依 error.code 分派會看到與 status 矛盾的類別)。**教訓(新 lesson class 候選:框架邊界的預設行為完整性)**:當你在框架的錯誤/回應路徑上「宣稱一個不變量」(這裡=「沒有預設 shape 會到 client」),必須列舉框架能產生的**所有**出口——多型 handler 優先序(specific > generic)、內建序列化的隱藏失敗(非 JSON 值)、框架附加的 side-channel(headers)、以及你自己的映射與更底層契約的一致性——否則同一宣稱會被逐一出口逼破。收斂手法:第 2 輪起把序列化**集中到 `_error_response` 一處**(jsonable_encoder 整個 body),一次覆蓋所有 handler,而非逐 handler 補;狀態→碼用單一來源 `_HTTP_STATUS` 反導（唯一才映射,歧義 fallback coarse)。**正面**:契約即發布 schema(app.openapi 直吐凍結 yaml,DR-002 結構性防漂移);ErrorCode 與契約 enum、status map 與契約 prose 都做 lockstep 測試;auth placeholder 不 reject（凍結 enum 無 auth code，避免 DR-002 破例）。0 本地 blocker——skeleton 邏輯簡單,但 error 出口是我開工時沒完整列舉的（本可靠「框架出口清單」在實作前避免這 4 輪）。輪次:…→17→8→**4**。

**#42(BA1a projects/sources registry,3 輪 Codex——4 P2、0 本地 blocker;BA1 首切片)**:先做了兩個**任務邊界**判斷——(a)BA1「+trigger ingest/build」與 DESIGN line175「BA2 arq/jobs/SSE」衝突,用 AskUserQuestion 讓 owner 定案「triggers→BA2」;(b)BA1 本身太大,切成 BA1a(schema+core CRUD)/BA1b(routers+idempotency)。**核心教訓——引入控制面父表(projects)覆蓋既有裸-text-keyed 資料(builds/review_ledger/… 無 FK)時,Codex 會把「這個父/子關係的每個破口」逐輪掃完**,全是同一資料完整性類:R1 delete 只刪 projects 列→builds 及 build-scoped 資料被孤兒化+同名重建會撈到 stale active build(active_build_id 由 builds 解析)→加 ProjectHasBuildsError refuse;R2 同理 review_ledger(DR-003)/ontology_proposals(DR-007)/pipeline_runs 也 carry-forward→delete 時 purge(builds 仍 refuse,因多店清理=C9/BA8);R3 migration 建空 projects 表→既有 project 從 registry 消失但 builds 仍解析→**backfill**(從四張非-build-scoped project-keyed 表 UNION 回填)+ delete guard 的 **count-then-delete TOCTOU**。**收斂判斷(對齊使用者「劃邊界」指示)**:in-scope 的破口(delete guard/purge/backfill)當輪修+revert-probe;結構性根因=FK `builds.project→projects.name`(RESTRICT)一次關掉 orphan+TOCTOU,但 retrofit 會炸掉所有「直接插 builds 無 projects row」的既有 build 測試,且需 build-creation 變 registry-aware=**BA2** 的事→reply-resolve 為可查核 deferral,存 [[graphrag-ba2-registry-fk]] 追蹤(backfill 已備妥 FK 前置)。**正面**:自審抓到自己的 false-green(offline DDL `"ON DELETE CASCADE" in ddl` 恆真,因既有 C1a 表都有→改斷言整條 `REFERENCES projects (name) ON DELETE CASCADE`);非-build-scoped 控制面用 plain async funcs(非 build-scoped repo,後者會 reject 這些表);PATCH sentinel 抽純函式單測。**新 lesson class 候選:引入父表覆蓋既有無-FK 資料時,先列「delete 孤兒/carry-forward + upgrade backfill + 寫入 TOCTOU/FK」三面完整性清單**。輪次:…→8→4→**3**。

**#43(BA1b projects/sources routers + §27 idempotency + opaque cursor,0 輪 Codex 首推 +1;本地 reviewer 2 輪:1 FAIL(2 blocker)→ PASS)**:BA1 第二切片,本地執行級審查在 push 前攔下兩個 blocker、Codex 零輪。**核心技術教訓(class 8 表示誤差最佳例):我加的結構性 CHECK 揪出一個所有行為測試都放行的潛伏 bug**——reserve-first 冪等的預留列以 `response=None` 寫進 JSONB 欄,SQLAlchemy 預設把 Python None 編成 JSONB `'null'` **字面值(一個 JSON 值)** 而非 SQL NULL,於是 `response IS NULL` 為 false、`status IS NULL` 為 true;我為「reserved(雙 NULL)xor committed(雙填)」加的 `CHECK ((status IS NULL)=(response IS NULL))` 立刻在每次 reserve 上爆(3 個原本綠的冪等測試同時紅)。這既證明 CHECK 是 load-bearing(結構約束抓到行為測試漏的表示層錯),也暴露真 bug:未加 CHECK 前,並發的 replay-after-reserve 會讀到 response→None/status→None 而 `int(None)`→500。修法=`postgresql.JSONB(none_as_null=True)` 讓 None 綁成 SQL NULL(真 response body 恆為物件,絕不存裸 JSON null)。**教訓:凡 nullable JSONB/JSON 欄用 Python None 表「缺席」,None 預設是 JSON null 不是 SQL NULL——欄型要 `none_as_null=True`(或 insert 用 `sa.null()`);而「用結構約束把不變量寫死」會回頭替你抓到 ORM 表示層的沉默錯譯**(#27 的 IFF 雙分支 CHECK 同源:CHECK 是設計期抓 bug 的槓桿,不只運行期防線)。**Blocker 2(class 6 spec 對齊 / class 1 值域 nullability cell):PATCH `{"config": null}` → 500**——DTO `config: dict|None` 放行 null,`model_dump(exclude_unset=True)` 產 `{"config": None}`,registry `SET config=NULL` 打在 NOT NULL 欄 → IntegrityError(create_project 有 catch、update_project 沒有)→ 通用 handler → 500,把 client 錯誤誤標成 server fault。根因=**凍結契約的 per-field nullability 要逐欄對齊**:契約裡 config 是 `type: object`(非 nullable)、display_name/description 是 `["string","null"]`(nullable-clearing)——我讓三者共用同一 `X|None` 形狀,抹掉了 config 與其他兩欄的差異。修法=共用 `NonNullConfig = Annotated[dict|None, AfterValidator(reject null)]` 套在 create+update(omitted 走 default 不觸發 validator→保持 unchanged;explicit null→400);**registry 自己的簽章早已寫明 `config: dict | _UNSET`(從不 None)——DTO 沒對齊到 registry 的型別合約**。兩個 blocker 都由本地 reviewer round-1 FAIL 抓到(config-null 是 client-triggerable 500;poisoned-key 不變量無測試——reserve-first 全設計所繫的「失敗寫入不留毒鍵」竟無測試守著,補整合測試:keyed POST 失敗→斷言 0 列殘留→同鍵重試 re-run 而非 replay NULL-status 列)。**流程/harness 教訓(可重用):async TestClient × live PG 的整合 harness**——`httpx.AsyncClient + ASGITransport` + override `db_conn` 為單一連線、每請求一個 `begin_nested()` savepoint、外層 txn teardown rollback(單一 event loop,零殘留);BA2+ 直接沿用。**覆蓋率門檻的結構張力**:fast-suite 的 85% coverage gate 不含 integration,而 BA1b 的 api 層本質是 integration-tested,純加 integration-only 碼會把總覆蓋壓破 85→補**component 測試(stub registry、真 app,測 router 編排/狀態碼/envelope/錯誤映射,快)**+ 純邏輯 unit 測試把 gate 拉回。**flagged 未決(劃清邊界、不上綱)**:ProjectExists/HasBuilds → VALIDATION_ERROR(400) 仍是佔位——凍結 ErrorCode enum 無合適的 409/conflict 碼,加碼是 DR-002 契約變更=BA1b 範圍外;隔離在 registry_errors.py 單點、已 flagged 待 owner 定案(reviewer 確認不阻擋)。無新 class;class 8(JSONB null 表示誤差)、class 6/1(per-field nullability 對齊契約與 registry 型別)、#17 全稱測試(poisoned-key 不變量補測)各得亞種。輪次:…→8→4→3→**0**(BA1a 的父/子完整性清單移轉+本地執行級審查把 Codex 壓到零輪)。

**#44(BA2a jobs table + core job repo + delete-project active-jobs guard,1 輪 Codex——2 P2 皆 must-fix;0 本地 blocker;BA2 首切片;code-architect 出 4-slice 藍圖後開工)**:BA2 是全案最大整合任務(把 Track 1 的 C1–C11 串成可跑的 build pipeline 並由 HTTP 驅動——全案無人建過端到端 orchestrator),依藍圖切成 BA2a(本:jobs 表+repo+delete guard)/BA2b(orchestrator+builds FK+registry-aware build 建立)/BA2c(arq worker)/BA2d(triggers+job endpoints+SSE)**(此為當時 4-slice 藍圖;#45 前已重切為 5-slice——BA2b=builds FK+sweep、BA2c=orchestrator+registry-aware build、BA2d=arq worker、BA2e=triggers+endpoints+SSE;一律以 TASKS.md 為準)**。兩個 Codex P2 都是**目錄已有 class 打在「切片邊界畫錯」的面上**:(1) **class 2 完整性再犯(新增領域錯誤要追到所有消費者)**:我在 `delete_project`(一個 BA1b router 早就在呼叫的函式)新增了 `ProjectHasActiveJobsError` guard,卻沒接 `translate_registry_error`+router 的 except → DELETE /projects 撞到新 guard 直接掉進 uncaught handler 變 **client-triggerable 500**(同屬 client-triggerable 500 症狀,但根因不同 class——BA1b config-null 是 class 6/1,此處是 class 2 完整性)。**教訓:對「共用函式」新增領域錯誤時,該函式的每個既有入口(尤其 HTTP)的翻譯/處理都要一起補——我對 local reviewer 說「本切片無 HTTP」劃錯了邊界,因為 delete_project 本身就被 HTTP 消費;新錯誤的完整性面=該函式的所有呼叫點,不是我這次改的檔案集合**。(2) **class 10 TOCTOU(count-then-delete)+ class 8 假綠測試(鎖測試的窗口遮蔽)**:delete_project 先 count active jobs=0 再 DELETE,中間窗口有並發 create_job commit 會被 CASCADE 靜默刪掉(jobs.project 是 CASCADE,BA2b 的 builds-FK-RESTRICT 關不掉這條);修法=delete_project 首句 `SELECT … FOR UPDATE` 鎖 projects 列,並發 create_job/add_source 的 FK 插入取 FOR KEY SHARE 會被擋到 delete txn 結束(兩序皆序列化,無殘窗)。**但我第一版鎖測試假綠**:在 delete_project **回傳後**才用第二連線 probe——此時 DELETE 自身的列鎖已在,無論修法在不在都 55P03,revert-probe 拿掉 `.with_for_update()` 測試照過(class 8 假綠當場被 revert-probe 抓到)。**教訓:鎖/TOCTOU 測試必須 probe 在「修法所保護的那個窗口內」,不是操作完成後——後續的附帶鎖(此處=DELETE 自身)會遮蔽 bug**;修正版用 monkeypatch 把 `count_active_jobs` 暫停在「取鎖後、DELETE 前」,在該窗口 probe `FOR KEY SHARE NOWAIT`→55P03,revert-probe 確認可辨別(拿掉鎖→窗口內列仍空→probe 成功→測試失敗)。**流程正面**:jobs.error 首用即帶 `none_as_null=True`(BA1b 教訓前置生效);delete guard 用 FOR UPDATE 序列化(C1b/C9 的 row-lock TOCTOU 修法移植);Codex 兩條當輪一起收(額度經濟學),reply+resolve+revert-probe 後 +1。無新 class;class 2(新錯誤→所有消費者翻譯完整性)、class 10(count-then-delete→取鎖前置)、class 8(鎖測試窗口隔離)各得亞種。輪次:…→4→3→0→**1**。

**#45(BA2b builds→projects FK RESTRICT + fixture sweep,1 輪 Codex——1 P1 must-fix;1 本地 nit;BA2 第二切片)**:兌現 BA1a deferred 的 FK(存 [[graphrag-ba2-registry-fk]]),把 orphan+TOCTOU 面結構性關掉。**主教訓(新:收緊型 migration 要先和解既有列,且 fresh-DB CI 遮蔽此失敗)**——Codex P1:在已到 0009 的庫上,`builds.project` 一直到本 migration 才受約束,故 0007 backfill 之後、0010 FK 之前插入的 build 可能引用無 `projects` 列的 project → `ADD CONSTRAINT` 撞 FK 違反、擋住升級。**CI 打在全新空庫,永遠不會有這種孤兒列,所以檢查看不到**(class-5 亞種:檢查者的 DB 狀態=空庫 ≠ 消費者的既有 dev/prod 庫;M→N 兩個 migration 之間是無約束窗)。修法=0010 在 `create_foreign_key` 前**重跑** builds→projects backfill(`INSERT … WHERE project<>'' ON CONFLICT DO NOTHING`,同一 alembic 交易內原子);空-project build 刻意排除(projects.name 有非空 CHECK、無碼產生、FK 正確地讓真正無效資料 fail-loud)。測試=downgrade 到 0009→插孤兒 build→upgrade 到 head 必須成功且已註冊該 project(revert-probe:拿掉 backfill 則失敗;sync 測試因 alembic env.py 自跑 asyncio.run 不能巢狀在 async loop,DB 操作走 asyncio.run,finally 無條件還原 head)。**本地 nit(#17 再犯但被本地審查咬住)**:`builds.project` FK 無 supporting index——`one_active_build` 是 partial(WHERE status='active'),擋不住 projects DELETE 的 RESTRICT RI-check(任何 status);#17 的「每個 child FK 一個 supporting index」早在 checklist §DB,**本地 reviewer 在 first push 前抓到**(review 層預防生效),但我實作當下沒主動套 checklist——**教訓重申(#34 母題):DB 清單要在實作/首推當下逐格過,不是等 review 提醒**;修法=加 `builds_by_project` index(表定義+migration+offline-DDL 三處)。**流程正面**:本地 nit 修完 re-stamp、Codex P1 修完 reply+resolve+revert-probe,額度經濟學維持(單輪收);sweep(22 檔補 `ensure_project` seeding)委派 general-purpose subagent、以 check-full 為正確性 oracle,零 FK 失敗;唯一副作用(committing 測試在 dev DB 留 seeded projects 列)判 benign(CI 每次乾淨庫、ensure_project 冪等、其他測試皆 self-relative,pagination 測試已改 self-relative)——本地 reviewer 認同。無新 numbered class;新增「收緊型 migration 和解既有列 + fresh-DB CI 遮蔽」lesson(class-5 亞種,已寫入 code-reviewer checklist §6 Migration bullet + 父表三面 bullet 的 UPGRADE/WRITE-RACE 面)、#17 supporting-index 再犯(review 層攔下)。輪次:…→4→3→0→1→**1**。

**#46(BA2c-1 registry-aware build 建立 + pipeline orchestrator 控制流,5 輪 Codex、7 finding(2 P1+5 P2;6 修、1 P2 reply-and-resolve deferral),findings 每輪 1/2/2/1/1;0 本地 blocker 但本地 reviewer 全程 re-stamp + 抓 2 件 session-only 假綠/漏標;BA2 第三切片、全案首個端到端 build 執行器)**:又一個「狀態機×並發」新 surface(build **執行**狀態機,對照 #38 C9 的 build **生命週期**狀態機),曲線回彈到 5 輪——**核心元教訓:多-commit 的 orchestrator 是 TOCTOU/crash-window 工廠**。run_build 把一次 build 攤成約 9 個獨立交易(job 載入、resolve/create、mark running、6 stage、cancel recheck、build 終態、record_run、job 終態),**每個 commit 邊界都是一個 crash 或 race 窗**,Codex 逐輪逼出(每輪 1–2 格,R 編號依 finding 非輪次):(R1 **P1**)create build 與 attach 到 job 兩段 commit=class 10——crash 在中間留孤兒 building build(job 仍 build_id=NULL、RESTRICT 擋不掉隨專案刪)→摺進單一 txn;(R2 P2)兩 worker 同派一 job 都讀 build_id=NULL 都建 build=class 10——job 列 FOR UPDATE 鎖 + 鎖內重讀 build_id,序列化 build **建立**;(R3 P2)cancel 只在每個 stage **之前**檢查,最後一個 stage 期間的 cancel 無後續 checkpoint→丟失、build 誤標 ready=class 9 防護面完整性(cancel checkpoint 漏了「最後一格」)→loop 後補一次終態 recheck;(R4 **P1**)build 終態 flip 與 job finalize 兩段 commit=class 10——crash 在中間留 job 永久 running 指向已終態 build(retry 撞 BuildNotResumableError→卡死、擋專案刪)→build+job 摺進單一 txn;(R5 P2 **判 deferral**)鎖只序列化 build **建立**、不序列化**執行**,兩並發 dispatch 仍會同跑一個 building build 的 stage=class 10,但**正確修法需 lease/heartbeat**(區分 alive-running vs crashed-running;naive「running 就 no-op」會 reintroduce R4 剛修的卡死——dead worker 的 job 永不 retry)→arq 的 job-timeout/retry 領域(BA2d),當輪 reply-and-resolve + 存 TASKS.md BA2d follow-up(對齊 owner「劃邊界不上綱」:一個「加了反而錯」的 P2 正確 deferral,理由要**可辨別**不是 blanket punt);(R6 P2)step-4f 的 cancel recheck 在**獨立連線**、終態 txn 前讀,cancel 落在 recheck 與 finalize 之間=被接受(cancel_requested=True)卻被忽略(job→done)的 limbo=class 10 cancel 面——**跨連線讀信號本質是 race**;修法=cancel 決策**移進終態 txn、job 列 FOR UPDATE 鎖內讀**(鎖=cutoff)+ request_cancel 的 UPDATE 加 `WHERE status IN active`(輸掉 race 的 cancel no-op、不誤標已終態 job)+ record_run 也進同一 txn(`record_run_in_txn`:從 loaned-clean record_run 抽出的 in-txn 變體,record_run 留作 wrapper 供他 caller)——run+build+job 至此**完全原子**;(R7 P2)record_run 從「任一 failed item→run failed」推斷,但 orchestrator 對 §22 門檻**以下**的 failure 標 build ready→ready build 配 failed run,誤導 §18 Health=class 5 檢查者/消費者分岔(judge surface 讀 run.status)→加 `failed: bool | None` override,build run 的 status roll up 到 **build 結果**(門檻中止才 failed),failed items 與其 failed **step** 仍記錄(§18 drill-down/§27.7 retry 讀 step/item——run↔step 不同粒度的分歧是**對的**,collapse step 會破 retry)。**壓縮手段(未落實的教訓,重複第三次)**:R1/R2/R4/R6 四個 TOCTOU/atomicity finding 若在 first push 前做一次「每個 commit 邊界 × crash/並發交錯」審計(#38 生命週期矩陣在**執行**面的對應),本可一輪掃完而非四輪——**多-commit 狀態機開工時先列每個 commit 邊界並問「crash 或並發 op 落在這裡會怎樣」**(#37 請求生命週期、#38 狀態機矩陣、本次執行=同一預防的第三次)。**本地 reviewer 抓 2 件 session-only 事實**:(a)第一版 cancel-racing 測試**假綠**——並發 request_cancel 的 get_job 在其 UPDATE 前就讀到 'done'、Python 分支先擋掉、根本沒碰到 guard(revert-probe 拿掉 guard 仍過)=class 8;拆成**確定性** guard 測試(monkeypatch get_job 餵 stale 'running' 快照、逼出 UPDATE 打在真 'done' 列)+ NOWAIT probe 證明終態 txn 跨 record 持鎖;(b)新整合測試**漏了 `@pytest.mark.integration`**(該檔用 per-test 標記非 module-level pytestmark)→會在 CI fast lane(無服務)紅——本機 fast-cov gate 因服務在跑而靜默放行,靠 collect-only/deselected 數變化抓到。**新防線兩條**:(i)多-commit 狀態機邊界審計(每個 commit 邊界問 crash/並發);(ii)**live-DB 測試在 per-test 標記的檔案裡要顯式加 integration 標記——fast-coverage gate 會靜默跑未標記的 live-DB 測試(本機服務在)但 CI backend job(無服務)會炸**。無新 numbered class;class 10(多-commit 每邊界一窗:建立序列化/終態原子/cancel 鎖內/跨連線信號讀=race)、class 9(cancel checkpoint 最後一格)、class 5(run status roll up 到 build 結果=judge surface 粒度)、class 8(race 測試假綠:probe 沒落在被守窗口/沒觸及 guard)各得亞種。輪次:…→3→0→1→1→**5**。

**#47(BA2c-2a build-config loader:projects.config JSONB → typed §5/§6 config,2 輪 finding-Codex + 1 輪 +1(2 P2 皆判 must-fix、皆修)、本地 reviewer 首審 2 nit(1 修、1 被 Codex 升級);BA2c 第二切片之首、純函式零 I/O、unit-only)**:owner 定案「複用 dataclass 驗證、不建凍結 contract」的三切片(2a loader / 2b stages / 2c real-LLM)首片。loader 只做「型別化 dataclass 不管的那層」——按固定 key 取每個 block、逐葉型別檢查(bool 非 int 對每個數值葉)、再構造既有 typed 物件讓其 __post_init__ 驗業務規則(class 3 規則自洽:門檻序、非空詞彙、relation 端點引用皆單一來源、loader 不複述),失敗一律包成單一 BuildConfigError。**兩個 Codex P2 是同一母題的兩個位置——「畸形輸入靜默取預設而非 fail-loud」(class 1 值域完整性),差別只在畸形發生的層級**:(R1)`raw.get("ontology")` 對「省略」與「顯式 `{"ontology": null}`」都回 None → null block 靜默吃 absent-default=**#43 per-field-nullability 亞種**(省略≠顯式 null 不可 collapse);修法=四個頂層 block loader 改用 `"key" not in raw` 判省略,present 值一律過 `_mapping`(它本就拒 null=not-a-Mapping),巢狀/葉層(proposal_policy/entity_types/rule/sub-key)早已由 _str/_number/_int/_mapping 擋 null,只這四格漏。(R2)巢狀 rule block 不檢查多餘 key → optional key 打錯字(`disambiguator` 之於 `disambiguator_column`)靜默落 None → extract_structured 鑄 name-only entity key、把同名不同 id 的列 collapse 成一個 entity=**class 9 防護面完整性 × 過度阻擋兩面同時**:守面要涵蓋每個「封閉」巢狀 schema(漏了),但**不可**越過 free-form 契約邊界(頂層 projects.config 由 API verbatim round-trip、須續容忍未知頂層 key)。修法=`_reject_unknown(block,allowed,path)` 掃**所有六個封閉巢狀 block**(ontology/resolution/chunking/EntityRule/RelationRule/mapping-value)一次到位(同類掃全、非只改 Codex 點名的 entity rule),arbitrary-key map(structured_mappings、entities)刻意不掃(其 key 是資料非 schema),`"table"` 特例拒絕續在 _reject_unknown 前(訊息更精準)。**元教訓(lenient-vs-strict 有邊界)**:free-form 容忍只在「契約真的存在」處成立(頂層 API round-trip);把它**均勻**套到封閉巢狀 schema=留一整排 typo footgun。判準:**在契約邊界容忍、在每個封閉 schema 嚴格**——而邊界用「這層的 key 是資料還是 schema」界定(map 的 key=資料→容忍;固定欄位 block=schema→嚴格)。**class 5 檢查者分岔:本地 reviewer 首審把 R2 的 free-form typo 面判成「accepted tradeoff」、Codex 升成 data-correctness must-fix(reviewer 事後自承「supersedes the tradeoff I under-called」)**——教訓:reviewer 標「可接受的取捨」時要再問一次「這取捨有沒有真實資料後果」(此處=entity collapse),有後果就不是取捨是 footgun。**壓縮手段(未落實、與 #46 同構第四次)**:兩 finding 同屬 class-1/9「畸形輸入 × 層級」矩陣,first push 前若做一次「每個輸入位置(block-present-null / unknown-key / wrong-type / out-of-domain)× 每層(頂/block/rule/leaf)是否 fail-loud,除非 free-form 契約在該處明文授權容忍」的完整性審計,兩格可一輪掃完而非兩輪——**validating loader 的完整性面=輸入位置 × 層級的笛卡兒積,逐格問 fail-loud vs 靜默預設**。本地正面:nit-1(_str 葉檢查在 _construct lambda 內→型別錯被雙重前綴)修為 hoist 到 lambda 前;複用 dataclass 驗證使 config.py 零業務規則複述;每格修法皆 revert-probe 證載重(null:還原 raw.get→ontology-null 測試失敗;unknown:拿掉 entity-rule _reject_unknown→disambiguator-typo 測試失敗);config.py 全程 100% 覆蓋、26 測試。無新 numbered class;class 1(省略≠null、unknown-key typo=畸形靜默取預設)、class 9(守面涵蓋每個封閉巢狀 schema × 頂層 free-form 不過度阻擋)、class 5(reviewer 把 data-correctness footgun 誤判成 accepted tradeoff、Codex 升級)、class 3(業務規則單一來源)各得亞種。輪次:…→0→1→1→5→**2**。

歷史脈絡(2026-07-03 更新的快照,僅供追溯,不代表現況):P0/P1 契約與 H1–H3 harness(fail-loud gates、triage 規則、watcher/doc lane/收據/governance)皆已 merge;當時正要開工 P2(build/activation + Alembic)。

相關:[[codex-plus-one-merge-gate]]、[[graphrag-architecture]]
